/*
=====================================================================
 【第 056 题】字符串变换四连：互转、逆序、过滤、回文
 难度：★★★☆☆
 知识点：大小写互转、原地逆序、原地过滤（i 读 j 写）
=====================================================================

【题目描述】
    四个固定字符串，四种变换，全部原地完成：

    一、大小写互转：char s1[] = "Hello World 123"，
        大写变小写、小写变大写，其余字符不动
        （008 的正负 32 手艺）。
    二、原地逆序：char s2[] = "abcdef"。043 的双下标
        相向法原样搬来，只是元素换成了 char——
        j 的初值从哪来，想清楚。
    三、删除所有空格：char s3[] = "a b  c   d"。用 i 读
        j 写的双下标法：i 逐个扫，遇到非空格就抄到
        s3[j] 并把 j 加一，扫完在 s3[j] 补 \0。
        不许开新数组。
    四、回文判断：判断 "level" 和 "hello" 是不是回文
        （正读反读一个样），双下标相向比较。

【输出示例】
    swapcase: [hELLO wORLD 123]
    reverse : [fedcba]
    nospace : [abcd]
    level: yes
    hello: no

【要求】
    1. 四个变换全部原地完成，除单字符暂存变量外
       不许开辅助数组
    2. 逆序和回文的终止条件自己推，别背网上的
    3. 删空格必须一遍扫描完成
    4. 回文判断遇到第一对不等就定论，别傻扫到底

【提示】
    - i 读 j 写里 j 永远追不上 i，所以不会盖掉没读的字符
    - 逆序需要先知道串长：手动数（052 手艺）或 strlen 都行

【吃透自检】
    1. i 读 j 写为什么安全？j 和 i 谁快谁慢，
       什么情况下两者相等？
    2. 删空格后忘记补 \0，打印 s3 会看到什么？亲手试一次
    3. 回文判断的两个下标相遇或交错意味着什么？
       奇数长度和偶数长度停下的位置一样吗？
    4. 大小写互转怎么判断"该不该转"？'1' 这种字符
       为什么必须原样放过？
    5. 附加挑战：忽略大小写的回文判断（"Level" 也算）。
       是先整串统一大小写，还是比较时临时转换？两种都写

===================== 在下面写你的代码 =====================
*/


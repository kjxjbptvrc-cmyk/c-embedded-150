/*
=====================================================================
 【第 042 题】数组边界与 sizeof：C 不替你把门
 难度：★★★☆☆
 知识点：sizeof 求元素个数、数组越界的危险
=====================================================================

【题目描述】
    第一部分：sizeof 求长度。定义 int a[10]，初始化成
    0 到 9。打印 sizeof(a)、sizeof(a[0])，再用
    sizeof(a) / sizeof(a[0]) 算出元素个数。记住这个式子：
    从今往后凡是求数组长度，一律用它，不许手写死数字。

    第二部分：越界实验（在 PC 上做，放心折腾）。
    实验一：越界读。打印 a[10] 和 a[50]——数组根本没有
    这两号元素，但程序多半照跑，打出来的是垃圾值。
    实验二：越界写。执行 a[100000] = 1，随后再打印一句话
    ——大概率那句话你永远看不到，程序已经崩了。
    C 不做边界检查，安全全靠你自己。

    〔嵌入式点〕在 PC 上越界写通常换来一次痛快的崩溃；
    在单片机上它往往不崩——而是默默改掉旁边的变量、
    甚至外设配置寄存器，程序继续跑，几天后才诡异出错。
    这比崩溃可怕得多。

【输出示例】（int 为 4 字节的平台；垃圾值与崩溃表现各机不同）
    sizeof(a) = 40
    sizeof(a[0]) = 4
    len = 10
    a[10] = 6422236       （举例：垃圾值，你看到的多半不同）
    a[50] = -858993460    （举例：垃圾值）
    now writing a[100000] ...
    （多半到此戛然而止——程序已崩溃）

【要求】
    1. 长度必须由 sizeof(a) / sizeof(a[0]) 算出，打印验证是 10
    2. 每步实验前先 printf 一行说明再动手，崩了才知道死在哪步
    3. 越界写放在最后做，崩溃本身就是这一步的实验结果，
       把你看到的现象（报错弹窗、返回码）记在你的代码注释里

【提示】
    - sizeof 的结果是无符号整数，图省事可以先强转成 int 再用 %d 打
    - 预告：sizeof(a) / sizeof(a[0]) 有个致命盲区——数组一旦
      传进函数变成形参，这招就失效了，第 069 题揭晓原因

【吃透自检】
    1. C 为什么不在运行时替你检查下标越界？这背后是它
       "不为用不到的功能付性能"的哲学，你怎么看？
    2. 越界读和越界写，哪个更危险？为什么？
    3. sizeof(a) 的单位是什么？是元素个数还是字节数？
       sizeof(a) / sizeof(a[0]) 为什么恰好等于个数？
    4. 附加挑战：把 a 换成 double a[10] 再跑第一部分，
       三个数各变成多少？公式还成立吗？

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 041 题】一维数组入门：三个初始化实验
 难度：★★☆☆☆
 知识点：数组定义、初始化规则、for 遍历、下标从 0 开始
=====================================================================

【题目描述】
    数组是你手里第一件"批量装数据"的容器。本题分两部分。

    第一部分：初始化实验。定义三个长度为 5 的 int 数组：
      a：完整初始化，int a[5] = {10, 20, 30, 40, 50}
      b：只给一个值，int b[5] = {1}
      c：局部数组，什么都不给
    用 for 循环把 a、b、c 各打印成一行，观察两件事：
    b 的后 4 个元素是什么？c 里面装的又是什么？

    第二部分：循环读入 5 个整数存进数组，一次遍历求出
    它们的和、最大值、最大值第一次出现的下标。

【输入示例】（第二部分）
    33 87 42 65 15

【输出示例】（第二部分；第一部分里 c 的内容每台机器可能不同）
    sum=242
    max=87 at index 1

【要求】
    1. 打印、读入、统计一律用 for 循环 + 下标完成，
       不许定义 x1 x2 x3 x4 x5 这样的散兵游勇
    2. 最大值和它的下标必须在同一次遍历里一起求出
    3. 打印 c 时如实记录你看到的数——那就是垃圾值，
       这次一口气见到 5 个

【提示】
    - 定义 int a[5]; 之后，能用的元素是 a[0] 到 a[4]
    - 记最大值时多带一个变量同步记下标，两者同进同退

【吃透自检】
    1. 长度为 5 的数组，合法下标为什么是 0 到 4？
       a[5] 算哪个元素——它存在吗？
    2. int b[5] = {1} 的补零规则是什么？int b[5] = {0} 呢？
       "部分初始化"和"完全不初始化"差在哪？
    3. 写 a[10]、a[-1]，编译器报错吗？动手试了再回答——
       下一题（042）专门收拾这件事
    4. 附加挑战：一次遍历同时求出最大值和最小值各自的下标

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 057 题】凯撒加密：环绕移位与负数取模
 难度：★★★☆☆
 知识点：字符环绕移位公式、C 负数取模的符号坑
=====================================================================

【题目描述】
    实现经典凯撒加密：字母表整体右移 k 位，走过 z 就绕回
    a。大小写分别环绕，非字母一律原样保留。小写公式：
      c2 = (c - 'a' + k) % 26 + 'a'
    大写把两处 'a' 换成 'A' 即可。

    一、对明文 "Hello, World!" 用 k = 3 加密，打印密文。
    二、写解密。不许照抄一份改符号的镜像逻辑——要么
        调用加密逻辑传 26 - k，要么支持负位移。打印
        解密结果，用 strcmp 验证和原文一字不差（053 手艺）。
    三、负数取模实验：单独打印 -3 % 26 的值。C 的取模
        结果符号跟着被除数走，所以它是负的——这就是
        "负位移必须先加 26 归正"的原因。

【输出示例】
    明文: Hello, World!
    密文: Khoor, Zruog!
    解密: Hello, World!
    strcmp(解密, 原文) = 0，还原成功
    -3 % 26 = -3

【要求】
    1. 加密解密都走环绕公式，不许写"加完再判断超界减 26"
       的补丁式写法
    2. 解密必须复用加密的逻辑（传 26 - k，或负位移先加 26
       归正），不许复制粘贴一份改符号
    3. 非字母（空格、逗号、感叹号）必须原样保留
    4. 密文和示例逐字符核对

【提示】
    - 判断大小写字母用区间比较（008 手艺）
    - 想单独验证环绕：拿 "xyz" 加密 k = 3，应得 "abc"

【吃透自检】
    1. 公式拆开讲一遍：c - 'a' 把字母映射到什么范围？
       加 k 再 % 26 各干了什么？最后加 'a' 呢？
    2. C 的负数取模符号跟谁走？-3 % 26 和 23 差多少？
       为什么"先加 26 再取模"能把结果归正？
    3. 非字母也跟着移位会破坏什么？想想空格和标点被
       加密后，密文还像不像一句话
    4. 附加挑战：k = 13 时加密和解密变成同一个函数
       （ROT13）。验证它，并解释为什么

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 050 题】矩阵运算：加法、原地转置、对称判断
 难度：★★★★☆
 知识点：矩阵加法、原地转置（半三角交换）、对称性
=====================================================================

【题目描述】
    固定两个 3x3 矩阵：
      A = { {1, 2, 3}, {4, 5, 6}, {7, 8, 9} }
      B = { {9, 8, 7}, {6, 5, 4}, {3, 2, 1} }

    一、矩阵加法：C 的每个元素等于 A、B 对应位置之和，
        打印 C（%4d）。
    二、原地转置实验。转置就是沿主对角线翻面：a[i][j] 和
        a[j][i] 互换。先故意写"全范围版"：双层循环 i、j
        都从 0 跑到 2 逐对交换，打印——你会看到 A 纹丝
        不动，像根本没转置。想明白为什么，再改成内层 j
        从 i + 1 开始的正确版，打印验证。
    三、对称判断：判断矩阵是否处处满足 a[i][j] 等于
        a[j][i]。用 A 和 S = { {1, 2, 3}, {2, 5, 6},
        {3, 6, 9} } 各测一次。

【输出示例】
    A+B:
      10  10  10
      10  10  10
      10  10  10
    全范围交换版"转置"后的 A:
       1   2   3
       4   5   6
       7   8   9
    正确转置后的 A:
       1   4   7
       2   5   8
       3   6   9
    A symmetric? no
    S symmetric? yes

【要求】
    1. 转置必须原地完成：只许一个暂存变量，不许开新矩阵
    2. 全范围版和正确版都要留在代码里，先后各跑一次，
       亲眼对比两种输出
    3. 对称判断只许扫上三角（j > i 的格子），发现一处
       不等立刻定论、停止扫描
    4. 判断结果打印 yes 或 no

【提示】
    - 全范围版里每一对元素被交换了几次？数一数就不玄了
    - 转置后的 A 拿去判对称不影响结论：一个矩阵和它的
      转置，对称性同生共死（顺手想想为什么）

【吃透自检】
    1. 全范围版为什么输出和原矩阵一模一样？
       每对 a[i][j]、a[j][i] 一共被交换了几次？
    2. 3 行 4 列的非方阵能原地转置吗？为什么非方阵转置
       必须开新数组？（想想转置后的形状）
    3. 对称判断为什么扫半个三角形就够？把 j 也从 0 扫起
       会出错，还是只是白干？
    4. 正确版的内层 j 从 i 开始（而不是 i + 1）会怎样？
       对角线元素和自己交换有害吗？
    5. 附加挑战：判断"反对称"——a[i][j] 等于 -a[j][i]
       且对角线全 0

===================== 在下面写你的代码 =====================
*/


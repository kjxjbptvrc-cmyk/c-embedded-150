/*
=====================================================================
 【第 058 题】atoi 与 itoa：字符串与数字互转
 难度：★★★★☆
 知识点：手写 my_atoi 与 my_itoa、累积公式、逐位取出
=====================================================================

【题目描述】
    〔嵌入式点〕串口收到的配置命令是一串字符：
    "SET SPEED 115200" 里的 115200 到手时是 6 个字符，
    不是数。字符串与数字互转是解析命令的日常手艺，
    第 144 题的 mini shell 直接要用今天这两个函数。

    一、手写 int my_atoi(const char s[])，处理顺序：
        跳过前导空格；吃一个可选的 + 或 - 号；连续数字用
        r = r * 10 + (s[i] - '0') 累积（012 拆数字的公式
        反过来用）；遇到第一个非数字字符立即收工返回。
    二、手写 void my_itoa(int n, char s[])：负数先记下
        符号、转成正数处理；用 % 10 和 / 10 逐位取出——
        取出的顺序天然是反的（012 里体会过），先把低位
        塞进缓冲区，最后整体逆序（056 手艺）；
        n 为 0 时直接给出 "0"。
    三、对照测试：my_atoi 对比库 atoi（stdlib.h），
        my_itoa 对比 sprintf(buf, "%d", n)。

【输出示例】
    my_atoi("123")=123 atoi=123
    my_atoi("   -456")=-456 atoi=-456
    my_atoi("12ab34")=12 atoi=12
    my_atoi("abc")=0 atoi=0
    my_itoa(-2026): [-2026] sprintf: [-2026]
    my_itoa(0): [0] sprintf: [0]

【要求】
    1. my_atoi 按"跳空格、吃符号、累积、停止"四个阶段写，
       阶段界限分明
    2. "12ab34" 这类脏输入的行为你自己定义清楚，写成注释
       放在函数上方（本题定义：遇非数字停，返回已累积的数）
    3. my_itoa 不许调 sprintf 偷懒，逆序自己写
       （056 的手艺直接搬）
    4. 至少 6 组对照用例，含负数、0、脏输入，逐行打印两边结果

【提示】
    - 累积式 r = r * 10 + (s[i] - '0') 和 012 的逆序累积
      是同一个式子，只是数字的来源不同
    - my_itoa 想绕开"最后逆序"也有路：从缓冲区末尾往前填
      ——先把正向版写对再玩这个

【吃透自检】
    1. 同一个式子 r = r * 10 + d，在 012 里造出的是逆序数，
       在 my_atoi 里造出的却是原数，为什么？
    2. my_atoi 读 "12ab34" 返回 12 是你定义的行为，
       库 atoi 也这么定吗？验证过了吗？
    3. my_itoa 逐位取出的数位为什么天然是反的？
    4. 把 INT_MIN（-2147483648）喂给 my_itoa 会出什么事？
       提示："转成正数"这一步就溢出了——不必修复，
       但必须知道坑在哪
    5. 附加挑战：写十六进制版 my_htoi，"1A" 返回 26，
       串口调试的常客

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 044 题】查找对决：线性 vs 二分
 难度：★★★☆☆
 知识点：线性查找、二分查找、比较次数统计
=====================================================================

【题目描述】
    准备一个有序数组：int a[32]，用循环填入 a[i] = 2 * i
    （即 0, 2, 4, ..., 62）。在它身上实现两种查找并比武。

    线性查找：从头挨个比，找到返回下标，找不到返回 -1。
    二分查找：循环版。low、high、mid 三个变量，循环条件
    low <= high，每轮拿 a[mid] 和目标比：相等就赢了；
    a[mid] 偏小就 low = mid + 1；偏大就 high = mid - 1。

    给两个函数各配一个比较计数器，口径统一：目标值和
    数组元素每比一次记 1 次（二分每轮记 1 次）。
    分别查 40（存在）和 41（不存在），打印下标和比较次数。

【输出示例】
    find 40: linear index=20 compares=21
    find 40: binary index=20 compares=5
    find 41: linear index=-1 compares=32
    find 41: binary index=-1 compares=5

【要求】
    1. 找不到必须返回 -1，并想清楚为什么偏偏选 -1 当暗号
    2. 二分必须是循环版；边界收缩必须写 mid + 1、mid - 1，
       少了这个 1 就可能死循环
    3. 计数器按题面口径埋进两个查找里，输出和示例完全一致
    4. 数组内容用循环生成，不许手敲 32 个数

【提示】
    - 线性查找找到就立刻返回，别把剩下的元素白比一遍
    - 二分按"轮"计数：一轮取一次 a[mid]，记 1 次比较

【吃透自检】
    1. 二分查找的前提是什么？拿乱序数组喂给它会怎样？
    2. mid = (low + high) / 2 在 low、high 很大时有什么隐患？
       改良写法 low + (high - low) / 2 为什么结果相同又安全？
    3. 32 个元素二分最多几轮出结果？和 log2(32) 什么关系？
       数组扩到 1024 个，线性和二分的差距变成多少？
    4. 附加挑战：数组里有重复元素时，让二分返回
       第一次出现的下标

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 059 题】有序数组三板斧：插入、删除、合并
 难度：★★★★☆
 知识点：有序插入（从后往前挪）、删除（整体前移）、
         双下标合并（归并思想）
=====================================================================

【题目描述】
    维护一个升序数组：int a[16]，当前有效个数 n = 6，
    内容 {3, 8, 15, 21, 30, 42}。依次完成：

    一、有序插入：插入 17，插完仍然升序。步骤：先找落点，
        再把落点起的元素从"最后一个"开始逐个右挪一格，
        腾出坑位放 17，n 加一。挪动方向是本题的命门。
    二、删除：删掉值为 8 的元素，后面的整体前移一格，
        n 减一。再试删一个不存在的 99：打印 not found，
        数组必须纹丝不动。
    三、合并：另给 x = {1, 4, 9, 16} 和 y = {2, 4, 6, 8, 10}，
        把两个升序数组合并进 z，保持升序。i、j 两个下标
        分别站在 x、y 开头，谁小谁先进 z（相等时约定 x
        先走）；一边走完后把另一边剩余的整体抄进去。
        这就是"归并"——排序算法的半壁江山，
        先在这儿把地基打牢。

【输出示例】
    插入 17: 3 8 15 17 21 30 42 (n=7)
    删除 8:  3 15 17 21 30 42 (n=6)
    删除 99: not found
    合并:    1 2 4 4 6 8 9 10 16 (共 9 个)

【要求】
    1. 数组容量和有效个数 n 分开管理，每步操作后 n 必须正确
    2. 插入的挪动必须从尾部开始；想不通就拿 3 个元素在
       纸上从前往后挪一遍，看看数据是怎么被吃掉的
    3. 合并主循环条件是"两边都没走完"，循环之后必须有
       两段扫尾（谁有剩余抄谁）
    4. 三步操作各自打印结果和 n，与示例逐个核对

【提示】
    - 找落点：第一个大于待插值的位置就是坑位
    - z 的容量至少是两边之和（4 + 5 = 9 个）

【吃透自检】
    1. 插入从前往后挪会发生什么？第一个被覆盖掉的是谁？
    2. 删除后忘了把 n 减一，打印时会看到什么假象？
    3. 合并漏写扫尾段，什么样的输入能把 bug 暴露出来？
       （想想哪种情况下某一边会提前走完）
    4. 找落点能不能用 044 的二分加速？加速的是"找"
       还是"挪"？整体能快多少？
    5. 附加挑战：合并时顺手去重（两边都有的 4 只进一次），
       应得 1 2 4 6 8 9 10 16

===================== 在下面写你的代码 =====================
*/


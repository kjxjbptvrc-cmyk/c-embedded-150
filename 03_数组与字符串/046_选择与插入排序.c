/*
=====================================================================
 【第 046 题】选择与插入排序：三强同台记分
 难度：★★★★☆
 知识点：选择排序、插入排序、比较与交换次数统计、排序对比
=====================================================================

【题目描述】
    同一组数据 int a[8] = {49, 38, 65, 97, 76, 13, 27, 50}，
    交给三位选手：
      冒泡：直接用 045 的带提前退出版
      选择：每轮从未排序区挑出最小值，换到未排序区开头
      插入：像摸牌，新牌在手，从右往左找位置，
            比它大的牌右挪一格，找到位置落下

    给三位配统一的记分牌，口径如下：
      比较：两个数据值比一次大小记 1（插入排序里手牌和
            有序区元素比一次记 1，含比输离场的最后一比；
            因 j 走到头而停的那次不算）
      交换：冒泡、选择一次三步交换记 1；选择每轮若最小值
            已在原位就不换、不记
      移动：插入排序有序区元素右挪一格记 1，手牌落位不记

    先跑乱序数据打一张对比表；再把数据换成"几乎有序"的
    {1, 2, 3, 4, 5, 6, 8, 7} 重赛一场，看谁翻脸最快。

【输出示例】
    ---- 乱序 {49 38 65 97 76 13 27 50} ----
    bubble: compare=27 swap=15
    select: compare=28 swap=7
    insert: compare=20 move=15
    ---- 几乎有序 {1 2 3 4 5 6 8 7} ----
    bubble: compare=13 swap=1
    select: compare=28 swap=1
    insert: compare=8  move=1

【要求】
    1. 三个排序各自独立成段，每个都先把原数据拷一份再排，
       不许在排过的数组上二次开工（数组不能整体赋值，
       拷贝用循环逐个抄）
    2. 计数严格按题面口径，输出和示例逐个核对
    3. 排序结果本身也要各打印一次，三位选手答案必须一致
    4. 插入排序用"边比边挪"的标准写法：
       while 条件是 j >= 0 且 a[j] 大于手牌

【提示】
    - 选择排序每轮先"选"后"换"：内层只找最小值下标，
      内层结束才交换一次
    - 插入排序把待插的牌先拿出来存进暂存变量，位置腾出来才好挪

【吃透自检】
    1. 一句话说清三种排序的核心动作，各像生活里的什么？
    2. 几乎有序时插入排序为什么占大便宜？它的比较
       在什么情况下能"提前停"？
    3. 选择排序的交换永远不超过 n - 1 次，这说明它适合
       什么场景？（"交换很贵、比较便宜"的场景长什么样）
    4. 带优化的冒泡比较次数看数据脸色，选择却雷打不动
       是 28 次，为什么？
    5. 附加挑战：把口径换成"赋值次数"（一次交换记 3 次，
       插入挪一格记 1 次，手牌取出、落位各记 1 次）重新打表，
       排名有变化吗？

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 045 题】冒泡排序：看着大数沉底
 难度：★★★☆☆
 知识点：冒泡排序、内层边界收缩、零交换提前退出
=====================================================================

【题目描述】
    对固定数据 int a[8] = {49, 38, 65, 97, 76, 13, 27, 50}
    做升序冒泡：相邻两个比，前面的大就交换。要求每一轮
    （外层循环一趟）结束后把完整数组打印成一行——
    亲眼看着最大的数一轮一轮沉到底。

    然后加优化：某一轮从头到尾一次交换都没发生，说明
    已经有序，立刻收工。用一个标志位实现。最后把数据
    换成已排好序的 {1, 2, 3, 4, 5, 6, 7, 8} 再跑一遍，
    验证只打印一轮就结束。

【输出示例】（乱序数据；最后一行的措辞随你，轮数必须对）
    round 1: 38 49 65 76 13 27 50 97
    round 2: 38 49 65 13 27 50 76 97
    round 3: 38 49 13 27 50 65 76 97
    round 4: 38 13 27 49 50 65 76 97
    round 5: 13 27 38 49 50 65 76 97
    round 6: 13 27 38 49 50 65 76 97
    done: 第 6 轮零交换，提前收工

【要求】
    1. 内层比较范围必须随轮数收缩：第 i 轮只比到 n - 1 - i，
       已经沉底的不许再碰
    2. 标志位每轮开始前清零，发生交换就置 1，
       轮末发现还是 0 就 break
    3. 每轮输出和示例逐行核对，一个数都不能差
    4. 换上有序数据后，验证只打印一轮

【提示】
    - 交换动作和 043 是同一招：暂存变量搭桥
    - 打印一轮结果单独写个小循环，别和排序逻辑搅在一起

【吃透自检】
    1. 外层为什么最多 n - 1 轮就一定排好？第 n 轮在忙什么？
    2. 内层边界 n - 1 - i 的道理是什么？偷懒写成 n - 1
       会算错，还是只是白干？
    3. 最好情况（已有序）和最坏情况（完全逆序），
       带优化的冒泡各要比较多少次？
    4. 附加挑战：改成降序，最少要动几个字符？

===================== 在下面写你的代码 =====================
*/


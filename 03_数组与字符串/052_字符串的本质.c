/*
=====================================================================
 【第 052 题】字符串的本质：\0 现形记
 难度：★★★☆☆
 知识点：字符数组、\0 结束符、%s 的工作约定
=====================================================================

【题目描述】
    四个实验，把字符串的真面目看清楚。

    实验一：定义 char s[] = "hello"。打印 sizeof(s)——
    是 6 不是 5，多出来的那个就是结尾符 \0。再手写 while
    循环数字符个数（碰到 \0 停），打印手数出来的长度。

    实验二：逐字符打印。不用 %s，用 while 加 %c 从 s[0]
    一路打到 \0 前面为止，最后自己补换行。

    实验三：执行 s[2] = '\0' 之后再用 %s 打印 s——只剩
    he。后面的 l、l、o 还躺在数组里，但 %s 不认。

    实验四：定义 char t[5]，手动逐个塞进 'w' 'o' 'r' 'l'
    'd' 五个字母——塞满、不放 \0，再用 %s 打印 t。
    大概率 world 后面拖出一截垃圾尾巴（041 见过的垃圾值
    换了个马甲）。

    %s 的约定就一句话：从给它的位置开始一路打，打到 \0
    为止。没有 \0，它就一头冲进邻居家。

【输出示例】（实验四的尾巴内容不定，也可能碰巧干净）
    sizeof(s)=6
    manual len=5
    hello
    he
    world?????

【要求】
    1. 实验一手动数长度，不许用 string.h
    2. 实验二不许用 %s，逐字符 %c 打印
    3. 实验四的 5 个字母必须逐个下标赋值塞进去
    4. 每个实验前打一行标签，输出要能对上号

【提示】
    - 手动数长度：int i = 0; while (s[i] != '\0') i++;
    - 实验四如果碰巧打印正常，多跑几次，或在 t 前后
      再定义几个变量改变内存布局再看

【吃透自检】
    1. 字符串和字符数组是什么关系？字符数组要升格成
       字符串，靠的是哪条约定？
    2. \0 的 ASCII 码是多少？它和字符 '0'、和空格
       是一回事吗？
    3. 空串 "" 占几个字节？char e[] = "" 的 sizeof 是多少？
    4. 实验三之后手动数长度会数出几？sizeof(s) 变了吗？
       ——"长度"和"占地"从此分清
    5. 附加挑战：char t[5] = "world" 这行能编译过吗？
       它和 char t[] = "world" 差在哪？动手验证

===================== 在下面写你的代码 =====================
*/


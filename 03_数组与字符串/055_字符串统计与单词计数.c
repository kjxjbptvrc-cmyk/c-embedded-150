/*
=====================================================================
 【第 055 题】字符串统计与单词计数：状态机初登场
 难度：★★★★☆
 知识点：分类统计、状态变量法（词内 / 词外）
=====================================================================

【题目描述】
    用 fgets 读一整行文本（053 手艺，行末换行先去掉），完成：

    一、分类统计：字母、数字、空格、其他各有多少。
        判断用 008 的区间比较手艺（c >= 'a' && c <= 'z'
        这一类），不许用 ctype.h。
    二、单词计数。单词 = 连续的非空格字符段。必须用
        状态变量法：int in_word 记录此刻"在词内还是
        词外"（0 词外，1 词内）。逐字符扫过去，只在
        "词外变词内"的瞬间把计数加一。

    用下面第二组这种开头一串空格、词间多个空格、末尾
    一串空格的输入验证，全都不许翻车。

    记住这个写法：一个变量记住"我现在处于什么状态"，
    每个字符只负责触发状态切换——这就是状态机思想的
    第一次登场。第 141 题它将正式称王。

【输入输出示例】（两组各跑一次；方括号只为标出空格范围）
    输入: [  hello 42  world, bye  ]
    输出: letters=13 digits=2 spaces=8 others=1 words=4
    输入: [  hello   world  ]
    输出: letters=10 digits=0 spaces=7 others=0 words=2

【要求】
    1. 分类判断手写区间比较，大小写字母都算字母
    2. 单词计数只许一遍扫描、一个状态变量；不许先删空格
       再数，更不许用"数空格个数加一"的歪招
    3. 状态切换两条规则想清楚再动手：词外遇到非空格，
       进词并计数加一；词内遇到空格，出词
    4. 两组示例都要跑出和题面完全一致的数

【提示】
    - 本题的"空格"只算 ' '，\t 之类先不管（自检里聊）
    - fgets 收进来的换行符会污染统计，先去掉再扫

【吃透自检】
    1. "单词数 = 空格数 + 1"为什么不靠谱？举出三种
       翻车输入
    2. 计数为什么挂在"词外变词内"的瞬间？挂在"词内变
       词外"行不行——行末正好在词内时谁会漏数？
    3. 状态变量法为什么天生不怕连续空格？
    4. 把"空格"升级成"空格或 \t 都算空白"要改几处？
       改动这么集中，说明这个写法好在哪？
    5. 附加挑战：顺手统计最长单词的长度并打印

===================== 在下面写你的代码 =====================
*/


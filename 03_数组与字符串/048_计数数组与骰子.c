/*
=====================================================================
 【第 048 题】计数数组与骰子：六万次投掷
 难度：★★★☆☆
 知识点：以值为下标的计数思想、直方图缩放
=====================================================================

【题目描述】
    掷骰子 60000 次，每次点数 rand() % 6 + 1（020 用过的
    老朋友）。用 int cnt[7] 统计每个点数出现的次数——
    点数直接当下标用，cnt[0] 空置不用。

    打印统计表：每行依次是点数、次数、星号直方图，
    星号个数 = 次数 / 500。最后打印六个计数之和，
    验证不多不少正好 60000。再看看六个点数是否
    大致均匀（各自都在 10000 附近晃悠）。

    〔嵌入式点〕"开一排桶、以值为下标往里落"是嵌入式
    统计的常用手法：统计 ADC 采样值的分布看噪声形状、
    统计按键电平翻转的间隔分析抖动，都是这一招。

【输出示例】（随机数使然，你的数值会不同，量级应一致）
    1: 10037 ********************
    2:  9899 *******************
    3: 10102 ********************
    4:  9975 *******************
    5: 10023 ********************
    6:  9964 *******************
    total=60000

【要求】
    1. cnt 必须先清零再统计——041 的垃圾值实验记得吧？
       在垃圾值上继续累加，统计全盘皆错
    2. 星号直方图用循环逐个打印，个数 = 次数 / 500
    3. 六个计数求和验证等于 60000
    4. 次数一列用 %5d 这类场宽对齐，表要整齐（001 的手艺）

【提示】
    - rand 在 stdlib.h 里；想每次运行结果不同就在开头用
      srand 播一次种（查一下用法），不播种也不影响本题结论
    - 打 n 个星号：for 循环里每次打印一个

【吃透自检】
    1. cnt 开 7 个而不是 6 个，浪费一格换来了什么？
       如果非要开 6 个，代码里会多出什么别扭的动作？
    2. 为什么必须先清零？041 的哪个实验早就警告过你？
    3. 星号数 = 次数 / 500 的缩放是干什么的？
       不缩放直接打一万个星号会怎样？
    4. 附加挑战：找出出现次数最多的点数并打印
       （并列时报最小的那个）——041 求最大值及下标的手艺

===================== 在下面写你的代码 =====================
*/


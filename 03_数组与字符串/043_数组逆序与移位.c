/*
=====================================================================
 【第 043 题】数组逆序与移位：双下标相向而行
 难度：★★★☆☆
 知识点：原地逆序（双下标相向）、循环移位、三次逆序法
=====================================================================

【题目描述】
    固定数据 int a[8] = {1, 2, 3, 4, 5, 6, 7, 8}，依次完成：

    第一步：原地逆序。i 从头、j 从尾，相向而行边走边换，
    到中间碰头收工。不许开第二个数组。打印结果后
    再逆序一次，验证能原样变回去。

    第二步：整体循环左移 1 位——第一个元素绕到末尾，
    其余整体前移一格。打印结果。

    第三步（附加挑战）：左移 k 位的三次逆序法。
    先把前 k 个逆序，再把剩下的逆序，最后整体逆序。
    重新初始化数组，拿 k = 3 验证。

    预告：这套"双下标相向而行"的手艺，到指针阶段
    （第 068 题）会换成真正的双指针原样再写一遍——
    动作一模一样，只是换了兵器。

【输出示例】
    原始:    1 2 3 4 5 6 7 8
    逆序:    8 7 6 5 4 3 2 1
    再逆序:  1 2 3 4 5 6 7 8
    左移1位: 2 3 4 5 6 7 8 1
    左移3位: 4 5 6 7 8 1 2 3

【要求】
    1. 逆序不许开第二个数组，只许一个暂存变量
    2. 左移 1 位时第一个元素必须"物理搬家"到最后；
       只在打印顺序上做花招不算数
    3. 左移 3 位必须用三次逆序法实现，
       连做 3 次单步左移不算完成

【提示】
    - 交换 a[i] 和 a[j] 需要第三个变量搭桥
    - 三次逆序法想不通，就拿这 8 个数在纸上推一遍

【吃透自检】
    1. 逆序循环条件为什么是 i < j？改成 i <= j 亏在哪？
       改成 i != j 呢——拿偶数长度的数组推一推，会出大事
    2. 交换时的暂存变量起什么作用？去掉它直接互相赋值
       会丢什么？
    3. 三次逆序法为什么成立？用"两段各自掉头、
       再整体掉头"的说法给自己讲明白
    4. 附加挑战：右移 k 位不写新代码，等价于左移几位？

===================== 在下面写你的代码 =====================
*/


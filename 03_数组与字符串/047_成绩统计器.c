/*
=====================================================================
 【第 047 题】成绩统计器：两遍扫描与落桶
 难度：★★★☆☆
 知识点：数组统计、两遍扫描、计数数组（落桶）
=====================================================================

【题目描述】
    读入 n 个成绩（0~100 的整数，n 不超过 50）存进数组，完成：

    一、平均分（浮点，保留 1 位小数）。
    二、高于平均分的人数。注意：必须先算出平均分，再回头
        扫描一遍数组挨个比。第 021 题滚动求极值时说过：
        没有数组，数据读完就丢，这种"要回头看"的统计
        根本做不了。现在数据都躺在数组里，两遍扫描
        理直气壮。
    三、分数段人数：90 以上、80~89、70~79、60~69、60 以下。
        必须用计数数组加 score / 10 下标映射落桶，
        不许排五个 if 一路 else 下去。

【输入示例】
    10
    78 92 45 88 60 73 95 82 55 100

【输出示例】
    average=76.8
    above=6
    [90-100]: 3
    [80-89] : 2
    [70-79] : 2
    [60-69] : 1
    [0-59]  : 2

【要求】
    1. 成绩存进 int score[50]，实际人数用变量 n 管理
    2. "高于平均分"必须由第二遍扫描得出，和求和的循环
       分成两个清清楚楚的循环
    3. 落桶：score[i] / 10 的值域是 0 到 10，想清楚
       100 分落在哪、怎么归并进"90 以上"档
    4. 平均分用浮点算，别让整数除法吃掉小数部分

【提示】
    - 一个顺手的落桶法：int cnt[11] 先按 score / 10 统计满
      11 个桶，打印时再合并成五档（60 以下就是
      cnt[0] 到 cnt[5] 之和，用循环加）
    - average 是浮点数，第二遍扫描比较时注意类型

【吃透自检】
    1. "高于平均分的人数"为什么一遍扫描做不出来？
    2. 100 / 10 等于 10，你的程序把 100 分归进了哪个桶？
       如果混进一个 105 的非法分，你的桶会发生什么？
    3. 计数数组每个下标的含义是什么？"以值为下标"
       这四个字用自己的话复述一遍
    4. 附加挑战：顺便打印最高分、最低分以及它们各是
       第几个输入的（下标加 1）

===================== 在下面写你的代码 =====================
*/


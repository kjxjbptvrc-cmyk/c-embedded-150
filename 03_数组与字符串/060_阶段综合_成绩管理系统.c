/*
=====================================================================
 【第 060 题】阶段综合：并行数组版成绩管理系统
 难度：★★★★★
 知识点：阶段 3 毕业考——并行数组 + 菜单 + 全套手艺
=====================================================================

【题目描述】
    阶段 3 毕业考。写一个学生成绩管理系统，数据结构限定
    为并行数组：
      int id[50];     学号
      int score[50];  成绩
      int n = 0;      当前人数
    两个数组靠"同一个下标属于同一个学生"这条隐含约定
    绑在一起——这条约定有多脆弱，本题让你亲身体会一次。

    菜单循环（023 的 while(1) 加 switch 加退出标志位套路）：
      1  录入一个学生（学号、成绩；满 50 人拒收；
         学号重复拒收——查重就是查找，直接复用功能 2 的逻辑）
      2  按学号查找（044 线性查找；找到打印，找不到提示）
      3  按成绩降序排序（045 冒泡）
      4  打印全表（每行：序号、学号、成绩）
      5  统计（047 手艺：平均分、最高分及其学号、五档分数段）
      0  退出（标志位收口）

    关于功能 3，先制造一次事故：第一版排序故意只交换
    score、不交换 id。录入示例那 3 个学生后排序并打印
    全表——1001 的成绩凭空变成 92，1002 反而背上 78。
    数据一个没丢，但每一行都在说谎。看清楚了，再把交换
    改成 score 和 id 成对搬家，重测。

    〔并行数组之痛〕以后每加一个属性（姓名、年龄），就要
    多开一个数组；录入、交换、删除每个环节都要跟着多改
    一处，漏一处就是一次新事故。这份痛苦记住它——
    第 091 题的结构体专治此病。

【运行效果示例】（节选。先录入 1001/78、1002/92、1003/65）
    事故版：选 3 再选 4
      1  id=1001  score=92    （1001 凭空多了 14 分）
      2  id=1002  score=78
      3  id=1003  score=65
    修复后：选 3 再选 4
      1  id=1002  score=92
      2  id=1001  score=78
      3  id=1003  score=65
    选 5
      average=78.3 max=92 (id=1002)
      [90-100]:1 [80-89]:0 [70-79]:1 [60-69]:1 [0-59]:0

【要求】
    1. 数据结构就用题面那三样，不许提前用结构体——
       这题的痛苦是故意安排的
    2. 菜单循环、非法选项提示、退出标志位，023 的规矩全要在
    3. 录入必须查满员、查学号重复；查重复用查找的代码逻辑
    4. 事故版排序必须真跑一次、亲眼看过错位输出，再修成
       同步交换；修好前后的输出都留观察记录（写进你的注释）
    5. 统计：平均分用浮点保留 1 位小数；分数段用 047 的
       score / 10 落桶映射，不许 if 链
    6. 每个功能写完先单测一遍，再写下一个

【提示】
    - 排序交换那几行，score 和 id 两组赋值紧挨着写，
      少一组就是事故
    - 打印全表做成独立功能，事故观察和修复验证全靠它

【吃透自检】
    1. 数据错位事故的根因是什么？用一句话说清并行数组的
       隐含约定
    2. 并行数组的维护成本体现在哪些具体动作上？
       在你的代码里至少指出三处
    3. 这个程序复用了前面哪几题的手艺？对着代码列一张
       清单（题号 + 用在了哪个功能里）
    4. 排序之后功能 2 的线性查找还能正常工作吗？为什么？
       （它依赖下标顺序吗）
    5. 附加挑战：加"6 删除学生（按学号）"——059 的删除
       手艺，注意两个数组必须同步前移

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 085 题】经典辨析：数组名与取地址——a 与 &a 貌合神离
 难度：★★★★★
 知识点：数组名退化、&a 的类型、退化的两大例外、经典笔试推导
=====================================================================

【题目描述】
    int a[5] = {1, 2, 3, 4, 5}; 围绕它做四步：

    第一步：打印 a 和 &a——数值一模一样！类型却天差地别：
    a 退化后是 int *（首元素地址），&a 是 int (*)[5]
    （整个数组的地址）。"数值同、类型异"077 你刚见过一回，
    这回轮到一维数组现原形。

    第二步：实锤步长。打印 a + 1 比 a、&a + 1 比 &a 各前进
    多少字节（还是 (char *) 相减那招）。前者 +4（跨一个 int），
    后者 +20（跨整个数组）——068 的老规矩"指向什么，步长就是
    什么"，这次的"元素"是整个数组。

    第三步：经典笔试题，先在注释里画图推导，再写码验证：
        int *p = (int *)(&a + 1);
        问：*(p - 1) 是几？
    推导链：&a + 1 跳过整个数组，落在 a[4] 后面的第一格；
    强转回 int * 后，p - 1 回退一个 int，正好踩在 a[4] 上——
    答案是 5。图至少画出：五个格子、&a + 1 的落点、p - 1 的回退。

    第四步：数组名不退化的两大例外，各用代码验证：
    sizeof(a) 得 20（量的是整个数组，不是指针）；
    &a 取的是整个数组的地址。除这两处，其余场合数组名一律
    退化成首元素地址——069 里传参丢大小的谜，今天全解。

【输出示例】（地址是示意值，你机器上数值不同）
    a      = 000000E3B75FFBA8
    &a     = 000000E3B75FFBA8
    a + 1  比 a  前进 4 字节
    &a + 1 比 &a 前进 20 字节
    sizeof(a) = 20
    *(p - 1) = 5

【要求】
    1. a 与 &a 都用 %p 打印（转成 (void *)），确认数值相同
    2. 两个"前进字节数"用 (char *) 相减打印
    3. 笔试题先画图推导（注释），后代码验证
    4. sizeof(a) 打印出来，注释里写清它与 sizeof(int *) 的区别
    5. 两大例外各用一句话写进注释

【提示】
    - &a + 1 的步长是 sizeof(int[5]) = 20 字节
    - (int *)(&a + 1) 里的强转不能省：&a + 1 的类型是 int (*)[5]，
      不转没法当 int * 用
    - 图不用漂亮：格子、箭头、落点标清楚就够

【吃透自检】
    1. &a 的类型精确写出来。它和 077 的 int (*p)[4] 是不是一路货？
    2. 两大例外背诵：数组名在______和______里不退化
    3. 笔试推导链合上书走一遍：&a + 1 落在哪、强转丢了什么信息、
       p - 1 踩在哪
    4. 换成 double a[5]，推导里哪些数字会变？答案还是末元素吗？
    5. 附加：sizeof(a) / sizeof(a[0]) 求元素个数这招（043 用过），
       为什么一进函数就失效（069）？

===================== 在下面写你的代码 =====================
*/


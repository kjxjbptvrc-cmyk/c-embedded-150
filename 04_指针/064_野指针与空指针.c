/*
=====================================================================
 【第 064 题】野指针与空指针：指针安全第一课
 难度：★★★☆☆
 知识点：野指针的三种来源、NULL 与判空、指针安全三守则
=====================================================================

【题目描述】
    指针指错了地方还去解引用，就是"野指针"事故。今天故意
    闯两次祸，见识现场，然后立规矩。

    实验一（未初始化）：声明 int *p; 不赋任何值，直接打印 *p。
    可能当场崩溃，也可能打出一串垃圾数字——两种结果都算你赢，
    见识过就行。编译器多半会警告，这次先不理它。

    实验二（空指针）：NULL 是值为 0 的特殊地址常量，含义是
    "明确不指向任何东西"。定义 int *q = NULL;
    （1）先用 if (q != NULL) 包住解引用，程序安全走完；
    （2）再去掉判断直接打印 *q——必崩。

    两个实验别同时放开：先跑实验一，看完现象把那几行注释掉
    再跑实验二，否则崩在前面，后面什么都看不到。

    第三种野源今天只记个名字：指针指向的变量已经"消亡"
    （比如函数返回后，它的局部变量就没了）。第 083 题专题伺候。

【运行效果】（崩溃类实验没有固定输出，重点是现象本身）
    实验一：闪退，或打出类似 6422304 的无意义数字
    实验二（1）：打印 q is NULL, skip
    实验二（2）：程序必然崩溃，Windows 弹"程序已停止工作"
                 或直接闪退

【要求】
    1. 两个实验都要真的跑，把你看到的现象一五一十记进注释
    2. 在注释里用自己的话写下三条守则：
       ① 指针定义时就初始化（有效地址，暂时没有就给 NULL）
       ② 解引用之前判空
       ③ 心里清楚它指向的东西还活着没有
    3. 判空写法 if (q != NULL) 必须真实出现在代码里

【提示】
    - NULL 解引用是"好崩溃"：立刻、稳定、现场清晰，一查一个
      准；最怕实验一那种"偶尔正常"，一直藏到发布才炸
    - 电脑上有操作系统兜底，野指针大多被当场击毙；单片机上
      没这层保护，野指针常常不崩，而是默默写坏别的变量甚至
      外设寄存器，出鬼一样的 bug——比电脑上可怕十倍，
      所以嵌入式圈对这三条守则近乎迷信

【吃透自检】
    1. 野指针的三种来源，各用一句话说清
    2. NULL 的本质是什么？它和"未初始化"有什么天壤之别？
    3. 为什么说"必崩"反而比"偶尔正常"友好？
    4. 附加挑战：把实验一里的 p 本身打印出来（打印 p 而不是
       解引用它，这不会崩），看看垃圾地址长什么样

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 062 题】解引用：拿到遥控器，隔空读写变量
 难度：★★★☆☆
 知识点：解引用运算符 *（经指针读、写它指向的变量）、
         核心辨析：指针赋值 p = q 与指向物赋值 *p = *q
=====================================================================

【题目描述】
    061 题里指针只是存了个地址。今天让它干活：星号写在指针
    前面叫"解引用"——顺着 p 里存的地址找过去，操作那个格子
    本身。p 从此是 a 的遥控器。严格按顺序做四步：

    第 1 步  int a = 10; int *p = &a; 打印 a 和 *p（都是 10）
    第 2 步  执行 *p = 99; 再打印 a——a 被隔空改掉了
    第 3 步  定义 int b = 200; int *q = &b; 做实验 A：
             执行 *p = *q; 打印 a、b、*p、*q 四个值；
             紧接着执行 *p = 1; 再打印 a 和 b——看谁动了
    第 4 步  实验 B：执行 p = q; 然后 *p = 55; 打印 a 和 b

【输出示例】
    第1步: a = 10, *p = 10
    第2步: a = 99
    实验A: a = 200, b = 200, *p = 200, *q = 200
    实验A续: a = 1, b = 200
    实验B: a = 1, b = 55

【要求】
    1. 输出数值必须与示例完全一致（行文格式可微调）
    2. 实验 A 之后在注释里画一张图：两个指针各指向谁、a 和 b
       的格子里各是多少，解释"实验A续"里为什么动的是 a
    3. 实验 B 之后再画一张图，解释这次为什么动的是 b
    4. 第 2 步起只许通过 *p 或 *q 改值，不许再写 a = ... 这种
       直接赋值——逼自己习惯"隔空操作"

【提示】
    - 一句话辨析：p = q 换的是"遥控器对准谁"；*p = *q 是
      "把 q 指的值隔空拷进 p 指的格子"，两个指针各指各的没变
    - 卡住了就默念：p 里存地址，*p 是顺着地址找到的格子

【吃透自检】
    1. *p = 99 改的是谁的内存？p 自己的格子动了吗？
    2. 先执行 p = &b，再执行 *p = 5，改的是谁？
    3. 用一句话说清"指针赋值"和"指向物赋值"的区别
    4. 第 061 题遗留问题：定义 int *p 时的星号和今天解引用的
       星号，现在能分清了吗？（定义时是类型的一部分，
       使用时是运算符）
    5. 附加挑战：只用 p、q 和解引用、不引入第三个 int 变量，
       能不能把 a、b 的值互换？体会一下为什么这么别扭
       （脑筋急转弯练完就丢，正经的交换手法第 065 题见）

===================== 在下面写你的代码 =====================
*/


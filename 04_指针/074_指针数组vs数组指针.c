/*
=====================================================================
 【第 074 题】指针数组 vs 数组指针：一字之差，天壤之别
 难度：★★★★★
 知识点：int *a[4]（指针的数组）与 int (*p)[4]（数组的指针）、
         方括号优先级高于星号、右左法则初亮相
=====================================================================

【题目描述】
    C 语言声明界最著名的一对冤家，今天当面对质：

        int *arr[4];      （甲）
        int (*p)[4];      （乙）

    甲：arr 先和 [4] 结合（方括号优先级高于星号）——所以它
        是个数组，装 4 个元素，每个元素是 int 指针。指针数组。
    乙：括号强行让 p 先和星号结合——所以它是个指针，指向
        "4 个 int 的整块数组"。数组指针。

    1. sizeof 对质：先笔算预测 sizeof(arr) 和 sizeof(p) 各是
       多少并写进注释，再打印验证（64 位下应是 32 和 8）
    2. 甲的用法：定义 int a = 1, b = 2, c = 3, d = 4;
       初始化 int *arr[4] = {&a, &b, &c, &d};
       循环打印 *arr[i]——输出 1 2 3 4
    3. 乙的用法：定义 int block[4] = {10, 20, 30, 40};
       初始化 int (*p)[4] = &block;（取的是整个数组的地址）
       循环打印 (*p)[i]——输出 10 20 30 40
    4. 乙的步长：打印 p 和 p + 1——一步前进 16 字节，一整块！
       数组指针的步长是它指向的整个数组

【输出示例】（地址为示意值）
    sizeof(arr) = 32
    sizeof(p)   = 8
    甲: 1 2 3 4
    乙: 10 20 30 40
    p     = 0061FEE0
    p + 1 = 0061FEF0

【要求】
    1. 第 1 步必须先预测后验证，猜错了要在注释里写一句错因
    2. 在注释里用"先跟谁结合"的思路把甲、乙两个声明各拆解
       成一句人话——这是右左法则的第一次实战，第 086 题把它
       系统化成任何复杂声明都能读的万能钥匙
    3. 在注释里回答：(*p)[i] 的括号为什么一个都不能少？

【提示】
    - 拆声明永远先看变量名跟谁结合：没有括号时方括号赢，
      有括号听括号的
    - 乙这种"整块挪动"的本事，正是遍历二维数组的钥匙——
      第 077 题它就要正式上岗

【吃透自检】
    1. 一句话说清甲和乙的区别（说不利索就回去重画图）
    2. (*p)[i] 去掉括号变成 *p[i]，编译器按优先级会把它理解
       成什么？含义变成了什么？
    3. p + 1 为什么前进 16 字节而不是 4 字节？
    4. 甲的 sizeof 在 32 位单片机上会变成多少？为什么？
    5. 附加挑战：int (*p)[4] 能不能直接指向一个 int big[8]？
       为什么类型对不上？声明改成什么样才能指向它？

===================== 在下面写你的代码 =====================
*/


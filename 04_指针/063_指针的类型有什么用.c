/*
=====================================================================
 【第 063 题】指针的类型有什么用：解读内存的"眼镜"
 难度：★★★★☆
 知识点：指针类型决定解引用时"一次读几个字节、按什么规则
         解释"、(char *) 强转、小端序初见
=====================================================================

【题目描述】
    061 题说过：指针里存的只是地址。那声明时写的类型是干嘛的？
    答案：类型决定解引用时"从这个地址开始一次抓几个字节、
    按什么格式解释"。做实验验证：

    1. 定义 int x = 0x12345678;（十六进制写法，四个字节正好
       是 12、34、56、78 四对数字，便于观察）
    2. int *pi = &x; 用 %X 打印 *pi——完整的 12345678
    3. char *pc = (char *)&x;（不同类型指针互指要强转，照做）
       用 %02X 打印 *pc——你大概率看到 78
    4. 对比打印 sizeof(*pi) 与 sizeof(*pc)，
       再打印 sizeof(pi) 与 sizeof(pc)

【输出示例】（前两行是全题重点）
    *pi = 12345678
    *pc = 78
    sizeof(*pi) = 4   sizeof(pi) = 8
    sizeof(*pc) = 1   sizeof(pc) = 8

【要求】
    1. x 必须用 0x12345678 这个值，方便对照哪个字节露了脸
    2. 在注释里回答：同一个地址，为什么 int 眼镜看到 12345678，
       char 眼镜只看到 78？
    3. 在注释里画出 x 的四个字节在内存中的摆放（低地址在左，
       按你的实测结果画）

【提示】
    - char 指针一次只看 1 个字节。看到 78 而不是 12，说明
      低位字节被放在了低地址——这叫"小端序"。x86 电脑和
      绝大多数 ARM 单片机都是小端。今天先见一面，
      第 087 题拿它做整套"字节透视"实验
    - 万一你打出的是 12，说明你在大端机器上，结论反着记即可

【吃透自检】
    1. 为什么 char 指针解引用只读到一个字节？
    2. "78 在前"到底说明了什么？（一句话：低位字节放低地址）
    3. sizeof(pi) 和 sizeof(pc) 为什么一样大？
    4. 把一个 double 的地址强转成 int 指针再解引用，读出的数
       有意义吗？指针类型乱转乱解引用为什么危险？
    5. 附加挑战：试试打印 *(pc + 1)——指针加 1 的完整规则在
       第 068 题才讲，先照猫画虎，看能不能把 56、34、12
       依次都掏出来

===================== 在下面写你的代码 =====================
*/


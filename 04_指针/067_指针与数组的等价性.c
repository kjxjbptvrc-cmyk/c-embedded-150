/*
=====================================================================
 【第 067 题】指针与数组的等价性：a[i] 的真面目
 难度：★★★★☆
 知识点：数组名即首元素地址、a[i] 就是 *(a + i) 的语法糖、
         用指针遍历数组
=====================================================================

【题目描述】
    C 语言最重要的一次"摊牌"：绝大多数场合，数组名 a 会自动
    转换成首元素地址 &a[0]；而你写了几十遍的 a[i]，编译器拿到
    手一律翻译成 *(a + i)。逐条验证：

    1. int a[5] = {10, 20, 30, 40, 50}; 打印 a 和 &a[0]——相同
    2. 循环打印 a[i] 与 *(a + i)（i 从 0 到 4），两列对照
    3. int *p = a;（注意：不用取地址符，a 本身就是地址）
       取 i = 2，把四种写法 a[2]、*(a + 2)、p[2]、*(p + 2)
       打在同一行——四个 30
    4. 纯指针遍历求和：for (p = a; p < a + 5; p++) 累加 *p，
       循环体内不许出现方括号，也不许出现下标变量
    5. 顺带结案：第 053 题 scanf("%s", str) 不加 & 的谜底——
       str 是数组名，本身就是首元素地址，再取地址纯属画蛇添足

【输出示例】（地址为示意值）
    a     = 0061FEF4
    &a[0] = 0061FEF4
    i=0: a[i]=10  *(a+i)=10
    i=1: a[i]=20  *(a+i)=20
    i=2: a[i]=30  *(a+i)=30
    i=3: a[i]=40  *(a+i)=40
    i=4: a[i]=50  *(a+i)=50
    四种写法: 30 30 30 30
    sum = 150

【要求】
    1. 第 4 步必须是"指针从头走到尾"的形态，出现任何方括号
       都算没做到位
    2. 在注释里画内存图：五个格子排一排，标上地址（相邻差 4
       字节）和值，再标出 p 起步时和收工时各指在哪
    3. 在注释里亲手翻译两个式子：b[3] 等价于什么？
       *(c + 7) 等价于什么？

【提示】
    - p < a + 5 是"指针和指针比大小"：p 没走过末尾就继续。
      这种"首地址 + 元素个数"定边界的写法，以后天天见
    - p++ 挪一格是挪一个 int（4 字节），不是 1 字节——
      这条规则第 068 题细说

【吃透自检】
    1. a[i] 的本质翻译是什么？方括号还神秘吗？
    2. a 和 p 的区别：p 能自增、能被重新赋值，a 都不能。
       为什么？（a 更像一个"常量地址"而不是变量——
       第 085 题深挖这件事）
    3. p < a + 5 这种指针比较为什么合法？什么情况下指针比较
       没有意义？（不在同一个数组里的两个指针）
    4. 附加挑战：把第 4 步改成从尾往头遍历（p 从 a + 4 出发），
       想清楚终止条件怎么写才不越界

===================== 在下面写你的代码 =====================
*/


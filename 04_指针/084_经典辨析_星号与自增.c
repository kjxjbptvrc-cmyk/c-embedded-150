/*
=====================================================================
 【第 084 题】经典辨析：星号与自增——*p++ 四兄弟
 难度：★★★★★
 知识点：*p++、(*p)++、*++p、++*p 的优先级与副作用
=====================================================================

【题目描述】
    四个长得像亲兄弟的表达式，行为天差地别，笔试出场率极高。
    逐个做隔离实验，起点统一为：
        int a[] = {10, 20, 30, 40};
        int *p = a;
    （每组实验前把数组内容和 p 都重置，互不污染。）

    规矩：先笔算，把预测写进你的注释，三问答齐——
    表达式的值是多少？做完后 p 指向谁（用 p - a 打下标）？
    数组变没变？然后运行验证，预测错的要写明错因。

        实验一：v = *p++;
        实验二：v = (*p)++;
        实验三：v = *++p;
        实验四：v = ++*p;

    规则讲清（用自己的话写进注释）：
    后缀 ++ 比星号贴得更紧，所以 *p++ 就是 *(p++)——
    表达式先按 p 的旧值取内容，指针随后才挪；
    (*p)++ 有括号护体，挪的是"指向物"的值，p 不动；
    前缀 ++ 与星号同级、从右往左结合：*++p 先挪指针再取值，
    ++*p 先取内容再给内容加一。

    070 你写过 while ((*d++ = *s++))——当时靠背，现在该彻底
    通透了：本题做完，回头把 070 重读一遍。

【输出示例】
    实验一 *p++  ：v = 10，p 指向 a[1]，数组：10 20 30 40
    实验二 (*p)++：v = 10，p 指向 a[0]，数组：11 20 30 40
    实验三 *++p  ：v = 20，p 指向 a[1]，数组：10 20 30 40
    实验四 ++*p  ：v = 11，p 指向 a[0]，数组：11 20 30 40

【要求】
    1. 四组实验相互隔离：每组开始前重置数组和 p
    2. 预测在前（注释）、验证在后（运行）；预测错了不许偷偷
       改预测，留着原话，旁边补一行错因
    3. "p 指向谁"用 p - a 算下标打印（068 的指针相减）
    4. 一个 printf 里最多放一个带副作用的表达式：
       printf("%d %d", *p++, *p) 这种写法本身就是未定义行为，禁止
    5. 注释里画四兄弟对照表：动指针还是动值、表达式值是旧是新

【提示】
    - 拿不准就想 074 的经验：后缀运算符（[]、()、++）比星号贴身
    - "表达式的值"和"副作用生效"是两件事，时间线上未必同时

【吃透自检】
    1. 对照表默写：四兄弟各自动的是指针还是值？值取旧还是新？
    2. *p++ 里，取值用的是 p 的旧值还是新值？副作用什么时候兑现？
    3. 实验二连做两次 (*p)++，数组会变成什么样？先笔算再验证
    4. 工程代码为什么提倡写 *(p++)、加括号甚至拆成两行，
       而不是炫技连写？
    5. 附加：把 070 的 while ((*d++ = *s++)) 对着 "hi" 的拷贝
       逐字符走一遍流程，写进注释

===================== 在下面写你的代码 =====================
*/


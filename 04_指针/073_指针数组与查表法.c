/*
=====================================================================
 【第 073 题】指针数组与查表法：把逻辑变成数据
 难度：★★★★☆
 知识点：指针数组 const char *tab[N]、字符串表、
         查表法替代 if 链和 switch
=====================================================================

【题目描述】
    第一张表——星期表：
    const char *weekdays[7] = {"Sunday", "Monday", "Tuesday",
        "Wednesday", "Thursday", "Friday", "Saturday"};

    1. 读入 0~6 的整数，直接输出 weekdays[i]。非法输入先拦截
       （004 题立下的防御规矩，一天都不能松）
    2. 遍历打印七个字符串和各自的首地址（即 weekdays[i] 的
       值）。观察：七个字符串长短不一，地址间隔参差不齐
    3. 对比方案：char w[7][10] 二维字符数组也能存这七个词——
       整齐（每行固定 10 字节）但浪费（"Sunday" 只占用 7 个）。
       两种方案的内存布局图都画进注释

    第二张表——天数表：const int days[12] 存平年十二个月的
    天数。读入月份 1~12，查表输出天数，替代第 022 题那一大坨
    switch。闰年先不管（022 处理过，本题聚焦查表本身）。
    注意下标错位：一月存在 days[0]，查表用 month - 1。

    查表法宣言：用空间换逻辑。逻辑变成数据后，代码更短、更不
    易错、改需求只动表不动代码。这是嵌入式第一惯用法——
    正弦表、CRC 表、菜单表、波特率表全是它，而且 const 的表
    可以整个放进只读 Flash，一字节宝贵的 RAM 都不占
    （072 的 const 在这儿生根发芽）。

【输入输出示例】（运行三次）
    输入 3       输出 Wednesday
    输入 9       输出 ERROR: out of range
    输入月份 2   输出 28 天

【要求】
    1. 两个查询的核心必须是一次下标访问，不许出现 if 链和
       switch（非法输入拦截的那个 if 除外）
    2. 两张内存布局图画进注释：指针数组版（7 个指针格子 +
       七串字符各安其家）、二维数组版（7 行 10 列的整块方阵）
    3. 两张表都必须加 const（一张查询表凭什么被改？）

【提示】
    - weekdays 的每个元素是一个 const char *，格子里存的是
      地址；七份字符串本体住在只读区——071 刚看过它们的长相
    - 这张字符串表先留着，第 076 题要用二级指针再遍历它一遍

【吃透自检】
    1. weekdays[3] 是什么类型？weekdays[3][0] 又是什么？
    2. 指针数组和二维字符数组怎么取舍？（词条长短悬殊时谁更
       省？需要原地修改内容时只能用谁？）
    3. 查表比 if 链好在哪？至少说出三条
    4. 022 的 switch 版和今天的查表版，改需求时（比如客户说
       八月改成 30 天）各要动几行代码？
    5. 附加挑战："0123456789ABCDEF"[n] 这种写法合法吗？
       用它把 0~15 转成十六进制字符——体会字符串字面量
       本身也是一张表

===================== 在下面写你的代码 =====================
*/


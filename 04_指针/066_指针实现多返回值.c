/*
=====================================================================
 【第 066 题】指针实现多返回值：输出参数模式
 难度：★★★☆☆
 知识点：输出参数（函数经指针参数把结果带出来）、
         "返回值报状态、指针带数据"的接口风格
=====================================================================

【题目描述】
    return 一次只能带一个值。065 学会传地址后，"函数改实参"
    不再是事故而是生产力——把它变成正经的接口设计：

    第一问：写 void stats(int a, int b, int c,
                          int *max, int *min, double *avg)
    一次算出三个数的最大值、最小值、平均值，经三个指针参数
    带出。main 里定义三个变量接结果，调用时传取地址。

    第二问（嵌入式风格）：写 int read_sensor(int *out)
    模拟读传感器：用 rand() % 5 == 0 制造约 20% 的概率失败，
    失败返回 -1；成功则把数据（比如 20 + rand() % 15 当温度）
    写进 *out 并返回 0。main 里循环读 5 次，每次都先判返回值：
    成功才使用数据，失败就丢弃并打印一句。

【输出示例】（第二问成败次序随机，你的结果不同属正常）
    stats: max = 9, min = 2, avg = 5.33
    第 1 次: OK, 温度 = 27
    第 2 次: 读取失败, 丢弃
    第 3 次: OK, 温度 = 31
    第 4 次: OK, 温度 = 24
    第 5 次: OK, 温度 = 22

【要求】
    1. stats 用 (2, 9, 5) 做测试数据，avg 保留两位小数
    2. read_sensor 的调用侧必须是"先判返回值、再用数据"的
       两段式写法，禁止不判就用
    3. stats 里不许出现任何 printf——计算和打印分家，
       打印全部放在 main

【提示】
    - 平均值记得写 (a + b + c) / 3.0，直接除以 3 是整数除法，
      小数被吃掉
    - rand 需要 <stdlib.h>；想每次运行结果不同，在 main 开头
      调 srand((unsigned)time(NULL))，再包含 <time.h>
    - "返回值报告成败、指针参数带出数据"是各家厂商 SDK 里
      驱动函数的标准姿势，以后读 HAL_UART_Receive 这类函数的
      签名会觉得眼熟——今天这题就是它的素描

【吃透自检】
    1. max、min、avg 这三个参数为什么叫"输出参数"？
    2. 这个模式比"只能 return 一个值"强在哪？
    3. 如果调用侧忘了判返回值、拿起 out 里的数就用，失败那次
       用到的是什么？程序会立刻报错提醒你吗？（这才是吓人处）
    4. 附加挑战：给 stats 再加一个输出参数 int *range（最大
       减最小），体会给这种接口扩一个输出有多顺手

===================== 在下面写你的代码 =====================
*/


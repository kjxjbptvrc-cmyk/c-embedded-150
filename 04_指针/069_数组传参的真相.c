/*
=====================================================================
 【第 069 题】数组传参的真相：退化为指针
 难度：★★★★☆
 知识点：数组做参数退化成指针、int a[] 与 int *a 完全等价、
         "指针 + 长度"标准接口
=====================================================================

【题目描述】
    第 042 题预告过：sizeof(a) / sizeof(a[0]) 这招在函数形参上
    会失效。今天让它当场应验，并把真相一次说透。

    1. 写 void probe(int a[100])，函数里只干一件事：打印
       sizeof(a)。main 里定义 int big[100]; 先打印 sizeof(big)
       再调用 probe(big)。两个数对比：400 对 8——形参 a 根本
       不是数组，是指针！C 规定：数组做参数，一律退化成
       指向首元素的指针
    2. 把 probe 的形参依次改成 int a[]、int a[100]、int *a
       三种写法，各编译运行一遍——结果一模一样。方括号里的
       100 纯属摆设，写 3 也照样能收 100 个元素的数组
    3. 结论落地：数组传参必须搭配长度参数。把你 043 的逆序、
       045 的冒泡改造成标准形态：
       void reverse(int *a, int n) 和 void sort(int *a, int n)，
       main 里用同一个数组先逆序、再排序，各打印一次

【输出示例】（sizeof 在 64 位环境是 8，32 位环境是 4）
    main 里 sizeof(big) = 400
    probe 里 sizeof(a)  = 8
    逆序后: 9 7 5 3 1
    排序后: 1 3 5 7 9

【要求】
    1. 测试数组用 {1, 3, 5, 7, 9}，长度在 main 里算好传进去
    2. reverse 和 sort 内部不许出现 sizeof——长度只信参数 n
    3. 在注释里回答：退化之后，probe 里还能写 a[i] 吗？
       为什么照样能用？（067 的等价性在这儿闭环）

【提示】
    - sizeof 的结果用 %zu 打印
    - 别把退化当缺陷：C 绝不偷偷拷贝整个数组，传的永远只是
      一个地址——几 KB 内存的单片机也养得起这种传参。真要
      拷贝一万个元素，得你自己写循环，C 不替你做主

【吃透自检】
    1. 为什么 sizeof(a) / sizeof(a[0]) 在函数里失效？在 64 位
       环境算出来是多少？（8 / 4 = 2，和真实长度毫无关系）
    2. 正确做法是什么？长度该由谁负责算、在哪里传？
    3. "指针 + 长度"两个参数打包出现，为什么是 C 接口的
       标配？（以后看标准库和驱动代码，到处是这个影子）
    4. 附加挑战：写 int sum(const int *a, int n) 求和，
       想想这个 const 在向调用者承诺什么（072 题正式开讲）

===================== 在下面写你的代码 =====================
*/


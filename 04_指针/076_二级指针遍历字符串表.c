/*
=====================================================================
 【第 076 题】二级指针遍历字符串表：带 NULL 哨兵的命令表
 难度：★★★★☆
 知识点：char ** 与字符串表、NULL 哨兵、p、*p、**p 三层拆解
=====================================================================

【题目描述】
    073 你用指针数组装过星期表，075 你摸清了二级指针。
    今天两件事合体，玩法升级。

    定义一张命令表：
        const char *cmds[] = {"help", "led", "reboot", NULL};
    末尾那个 NULL 不是命令，是"哨兵"——它标记表到头了。

    用二级指针遍历整张表：
        const char **p = cmds;
        while (*p != NULL) { 打印 *p; p++; }
    全程不许用下标，也不许另外传"共几条"。
    循环结束后，用 p - cmds 算出命令条数（068 的指针相减，
    在二级指针身上照样灵）。

    进循环之前，先打印一次 p、*p、**p，把三层看个明白，
    并把三层关系图画进你代码上方的注释里，样子大致如下：

        p ──> cmds[0] ──> "help"
              cmds[1] ──> "led"
              cmds[2] ──> "reboot"
              cmds[3] = NULL（哨兵，到此为止）

    p 是"指向指针的指针"；*p 是当前这条字符串的首地址；
    **p 是它的首字符。

    预告：082 题你会发现，main 函数的 argv 就是一模一样的结构；
    144 题 mini shell 的命令表，用的也是这个底子。

【输出示例】（地址是示意值，你机器上数值不同；← 后是讲解不用打印）
    p   = 000000E9D75FF6C0    ← cmds[0] 这个格子自己的地址
    *p  = 00007FF7A2B4C050    ← 字符串 "help" 首字符的地址
    **p = h                   ← 首字符本身
    cmd: help
    cmd: led
    cmd: reboot
    共 3 条命令

【要求】
    1. cmds 表按题面定义，末尾必须是 NULL 哨兵
    2. 进循环前打印三层：p、*p 用 %p（转成 (void *)），**p 用 %c
    3. 遍历只许 while (*p != NULL) 配 p++，每条打印成 cmd: 名字；
       不许下标、不许传条数
    4. 循环结束后用 p - cmds 打印条数
    5. 三层关系图画进你的注释

【提示】
    - *p 的类型是 const char *，正好喂给 %s
    - 哨兵这思路你早见过：052 里字符串自己用 \0 收尾，同一个道理
    - p++ 挪的是"格子"，一格是一个指针的宽度

【吃透自检】
    1. p、*p、**p 的类型分别是什么？精确写出来
    2. NULL 哨兵帮你省掉了什么？（提示：069 传数组时你被迫多传什么）
    3. p++ 让地址前进了几个字节？64 位 Windows 上是多少？
    4. 把哨兵删掉，这个循环会怎么死？（064 说过的越界读）
    5. 附加：往表里加第 4 条命令 "status"，除了表本身，
       其它代码要不要动？体会哨兵的好处

===================== 在下面写你的代码 =====================
*/


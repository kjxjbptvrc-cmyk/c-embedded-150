/*
=====================================================================
 【第 070 题】指针版字符串函数：经典一行 strcpy 推导
 难度：★★★★☆
 知识点：指针风格的字符串处理、*d++ = *s++ 惯用法、
         赋值表达式本身的值
=====================================================================

【题目描述】
    第 054 题你用下标写过 my_strlen、my_strcpy、my_strcmp，
    052 题讲过字符串以 '\0' 收尾。今天全部换成指针风格——
    这是 C 最有辨识度的代码长相。

    1. my_strlen 指针版：让一个游标指针从头走到 '\0'，然后用
       "末指针减首指针"直接得长度（068 指针相减的妙用），
       函数里不许出现计数变量
    2. my_strcpy 推导三步走，三个版本都要写、都要测：
       版本一：循环体三句分开写——解引用赋值一句、两个指针
               各自自增各一句；循环结束后在外面补 '\0'
       版本二：三句合并成一句 *d++ = *s++（先用后加），循环
               条件仍然判"源字符是不是 '\0'"
       版本三：压成经典一行 while ((*d++ = *s++)) ; ——
               拷贝、判断、收尾全在一个括号里
       每完成一步，在注释里写清：它和上一步为什么等价？
       '\0' 是在哪一刻被拷进去的？
    3. my_strcmp 指针版：逐字符比较，遇到不同或到头就返回
       两个字符的差值

【输出示例】
    my_strlen("embedded") = 8
    v1: embedded
    v2: embedded
    v3: embedded
    my_strcmp("abc", "abd") = -1
    my_strcmp("abc", "abc") = 0

【要求】
    1. 三个 strcpy 版本用同一个源串，分别拷进三个目标数组
    2. 版本三的推导必须写进注释：说清循环为什么恰好在拷完
       '\0' 之后停下——这是本题的灵魂，不写等于没做
    3. 目标数组开够大（比如 char dst[32]），溢出问题本题不管

【提示】
    - 钥匙只有一把：赋值表达式本身是有值的，值就是刚拷进去的
      那个字符。拷到 '\0' 那一次，表达式的值是 0——while 的
      条件恰好变假，循环自然收工，一个字符都不多拷
    - *d++ 是"先用 *d，用完 d 再前进"——这类写法的完整拆解
      第 084 题马上到，今天会用即可

【吃透自检】
    1. 经典一行版为什么连 '\0' 一起拷完才停？停下的那一刻，
       d 和 s 各指在哪个位置？
    2. 指针版和下标版哪个更"C 味"？（都对——但别人写的两种
       你都必须看得懂，这才是练它的目的）
    3. my_strcpy 为什么不怕目标数组比源串短？（它根本不知道
       也不管——这既是它快的原因，也是它臭名昭著的隐患）
    4. 附加挑战：给版本三的源参数补上 const（072 题会正式
       要求），再想想把返回值设计成"返回目标串地址"有什么
       好处

===================== 在下面写你的代码 =====================
*/


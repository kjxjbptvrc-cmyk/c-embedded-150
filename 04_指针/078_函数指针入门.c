/*
=====================================================================
 【第 078 题】函数指针入门：函数也是有地址的
 难度：★★★★☆
 知识点：函数入口地址、int (*fp)(int, int) 的声明读法、换绑调用
=====================================================================

【题目描述】
    040 画函数图像那题留过话："想换函数就得改代码，以后有办法。"
    办法从今天开始——先学会把函数的地址拿在手里。

    1) 写两个函数：int add(int x, int y) 和 int sub(int x, int y)。

    2) 打印 add——函数名就是函数的入口地址，和"数组名即首地址"
       神似（067）。打印前转成 (void *)。

    3) 定义函数指针并绑定：int (*fp)(int, int) = add;
       用 fp(2, 3) 和 (*fp)(2, 3) 各调一次——两种写法完全等价。

    4) 换绑：fp = sub; 再调 fp(2, 3)。
       同一个 fp，行为随指向变——你第一次把"行为"存进了变量。

    声明读法（074 优先级规则的续集，抄进你的注释并消化）：
    fp 先和括号里的星号结合——它是个指针；
    往右看 (int, int)——指向的是"函数"，参数两个 int；
    往左看 int——函数的返回类型。连起来读：
    fp 是指向"吃两个 int、吐一个 int 的函数"的指针。

    〔嵌入式点〕单片机上函数躺在 Flash、变量住在 RAM，
    两边地址范围一眼可辨——以后你读 map 文件时会想起这题。

【输出示例】（地址是示意值，你机器上数值不同）
    add 的入口地址 = 00007FF69D3C1450
    fp(2, 3)    = 5
    (*fp)(2, 3) = 5
    fp 换绑 sub 之后：
    fp(2, 3)    = -1

【要求】
    1. add、sub 完整实现，用 return 交结果，别拿 printf 顶替
    2. 打印 add 的入口地址（转成 (void *)）
    3. fp(2, 3) 与 (*fp)(2, 3) 两种调用都要出现，结果一致
    4. 换绑 sub 后再调一次，输出体现"同一个名字，两种行为"
    5. fp 声明的读法按"先括号、再右、再左"写进你的注释

【提示】
    - 赋值写 fp = add 或 fp = &add 都行：函数名自己会"退化"成地址
    - 函数指针转 (void *) 打印时编译器可能小声警告——标准 C 不保证
      函数指针和数据指针通用，PC 上练习无碍，记住这句就行

【吃透自检】
    1. 去掉括号写成 int *fp(int, int)，意思变成了什么？
       （提示：它成了一个函数声明，返回值是 int 指针）
    2. 函数名与数组名：相似处说一条，不同处说一条
    3. fp++ 合法吗？数组指针能加加，函数指针为什么不能？
       （函数不是数组，挪一步没有意义）
    4. fp(2, 3) 和 (*fp)(2, 3) 等价——查一句资料，说说 C 为什么
       两种都允许
    5. 附加：加一个 mul 函数，只改一行代码，让程序最后输出 6

===================== 在下面写你的代码 =====================
*/


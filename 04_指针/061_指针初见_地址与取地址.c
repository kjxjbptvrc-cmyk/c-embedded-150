/*
=====================================================================
 【第 061 题】指针初见：变量的"门牌号"——地址与取地址
 难度：★★☆☆☆
 知识点：内存地址、取地址符 &、用 %p 打印地址、
         指针变量——一个专门存地址的变量
=====================================================================

【题目描述】
    内存像一条长街，每个变量住在街上某间房里，房号就是"地址"。
    这道题不写算法，只做观察实验，把"地址"亲眼看一遍：

    1. 定义 int a = 42; char c = 'A'; double d = 3.14;
       用 %p 逐个打印 &a、&c、&d（打印地址时把参数强转成
       (void *) 是规范写法，先照做）
    2. 把三个地址抄在纸上按大小排一排：谁挨着谁？相隔几个
       字节？（不同编译器摆法不同，观察就好，不强求结果）
    3. 定义你人生第一个指针：int *p = &a; 再打印 p 和 &a，
       验证两者一模一样——指针变量的格子里存的就是地址
    4. 打印 sizeof(p)：64 位电脑上是 8 字节；以后到 32 位
       单片机上它是 4 字节——指针自己也是变量，也占内存
    5. 整个程序运行两次，对比两次打印的 &a 相同吗？

【输出示例】（地址均为示意值，你机器上的数值几乎必然不同，
              位数也可能更长，都属正常）
    &a = 0061FF08
    &c = 0061FF07
    &d = 0061FEF8
    p  = 0061FF08
    &a = 0061FF08
    sizeof(p) = 8

【要求】
    1. 五步全部完成，p 与 &a 两行必须肉眼可见地相等
    2. 在代码下方用注释画一张三列内存表：变量名│地址│值，
       a、c、d、p 四行都要有，地址填你实测打出来的值
    3. 运行两次，把两次的 &a 都记进注释，并写一句：变了没有？

【提示】
    - 电脑上两次运行地址常常不同：操作系统故意做了"地址
      随机化"。而单片机裸机程序里，变量地址在编译时就定死，
      烧一百次电它都住同一间房——这是嵌入式调试的一大便利
    - sizeof 的结果用 %zu 打印
    - %p 打出来带不带 0x 前缀、大小写，各编译器不同，不必纠结

【吃透自检】
    1. &a 这个表达式读作什么？它的值是什么？
    2. p 是一个变量。它自己的格子里存的到底是什么？（一句话）
    3. int *p = &a; 这行里的星号，和以后"使用指针"时的星号，
       含义一样吗？说不清很正常——第 062 题专门讲它
    4. 附加挑战：打印 &p（指针自己的地址）。能不能再有一个
       变量专门存 &p？它该是什么类型？（第 075 题见分晓）

===================== 在下面写你的代码 =====================
*/


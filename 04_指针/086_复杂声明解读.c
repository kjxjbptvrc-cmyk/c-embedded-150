/*
=====================================================================
 【第 086 题】复杂声明解读：右左法则系统训练
 难度：★★★★★
 知识点：右左法则、typedef 拆层、告别变态声明
=====================================================================

【题目描述】
    七个声明，从易到难。逐个用右左法则翻译成人话，
    答案写进你的注释（先自己写，再回头对照 073、074、078）：

        1) int *p1[4];
        2) int (*p2)[4];
        3) int **pp;
        4) int (*f)(int);
        5) int *(*g)(int, char);
        6) int (*arr[3])(int);
        7) char *(*(*x)(void))[5];   ← 只解读，不写码（纯健身）

    右左法则口诀：从名字出发，右一步、左一步，括号是结界——
    括号里的没走完，不许跨出去。
    名字右边见 [ 是数组、见 ( 是函数；左边的星号是指针。

    前四个各写最小验证代码：造数据、赋值、解引用或调用，
    各打印一行，证明你真的会用。

    出路点破：真实工程不写变态声明，用 typedef 拆层——
        typedef int (*op_t)(int, int);
    之后 079 那行函数指针数组声明美化成 op_t ops[4]，一键清爽。
    这招 138 题正式推广。另外：6) 就是 079 的 ops 换了签名；
    5) 读通了，你就有资格围观标准库 signal 的声明（出名劝退）。

【输出示例】（前四个的最小验证，数值可自拟，风格如下）
    *p1[0]   = 42
    (*p2)[3] = 8
    **pp     = 42
    f(10)    = 20

【要求】
    1. 七个声明的人话翻译全部写进注释，格式：原文 → 一句人话
    2. 翻译必须先说清"它本体是数组还是指针"，再说装着或指向什么
    3. 前四个写最小验证代码并各打印一行
    4. 注释里用 typedef 把 6) 改写成两行清爽版
    5. 7) 写出完整行走路线：第一步和谁结合、第二步和谁……直到走完

【提示】
    - 5) 的星号有两拨：括号里那个属于 g 本体，最左边那个属于
      返回值——先分清归属再动嘴
    - 卡住就给里层起个 typedef 名，声明立刻降维
    - cdecl.org 网站能自动翻译声明——但它进不了笔试考场

【吃透自检】
    1. 右左法则口诀默写
    2. 1) 和 2) 只差一对括号，类型差在哪？073、074 各用的是哪个？
    3. 遇到看不懂的声明，你的两件武器是什么？
       （右左法则手工拆 + typedef 逐层命名）
    4. 为什么工程上该用 typedef，而不是让队友当场表演右左法则？
    5. 附加：自己造一个"指向函数指针数组的指针"的声明并解读——
       造得出来才算真会

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 089 题】指针找 bug 专题：六个病例会诊
 难度：★★★★★
 知识点：野指针、悬空指针、只读区、越界、传值陷阱、指针层级
=====================================================================

【题目描述】
    排错能力是理解深度的试金石。下面六个病例都真实高发。
    对每个病例做三件事：① 指出 bug；② 解释后果（必崩？偶尔崩？
    静默出错？）；③ 在代码区写修正版并跑通验证。

    病例一（064 的老朋友）：
        int *p;
        *p = 100;
    病例二（083 刚讲过）：
        int *make(void) {
            int t[3] = {1, 2, 3};
            return t;
        }
    病例三（071 的事故现场）：
        char *s = "hello";
        s[0] = 'H';
    病例四（042 的老坑新看）：
        int a[5], i;
        for (i = 0; i <= 5; i++)
            a[i] = 0;
    病例五（028、065——看你还能不能一眼识破）：
        void swap(int x, int y) {
            int t = x;  x = y;  y = t;
        }
    病例六（075 的层级教训）：
        void bind(int *p) {
            static int target = 42;
            p = &target;
        }
        调用侧 int *q = NULL; bind(q); 之后 q 依然是 NULL

    排查心法三问（背下来，写进你的注释）：
    它指向谁？指向的东西还活着吗？类型和层级对得上吗？

【输出示例】（修正版的验证输出，格式可自拟，每病例一行证据）
    病例一修正：*p = 100（p 已指向真实变量）
    病例二修正：1 2 3（改用调用者提供的缓冲区）
    病例三修正：Hello（改用可写的字符数组）
    病例四修正：清零完成，i 最大只到 4
    病例五修正：x = 2, y = 1（传地址才换得动）
    病例六修正：q 指向 42（传的是 &q）

【要求】
    1. 六个病例的 ①② 写在注释里，③ 写成能跑的代码
    2. 修正要对症：病例一先让 p 指向真实对象；病例二改成调用者
       提供缓冲区（083 的推荐方案）；病例三改可写数组；
       病例四修边界；病例五传地址；病例六改收 int **、传 &q
    3. main 里逐一验证六个修正，各打印一行证据
    4. 三问心法写进注释，并给每个病例标注它栽在哪一问

【提示】
    - "跑起来没报错"不等于"没 bug"：病例四可能安静地踩坏
      隔壁的变量，最吓人的就是这种没动静的
    - 病例六先画图：q 的格子、形参 p 的格子、target 的格子，
      箭头到底搬的是哪一个——075 的图翻出来对照

【吃透自检】
    1. 六个病例各违反了哪条守则？064 立的守则能罩住其中几个？
    2. 三问心法默写；每一问分别能筛出上面哪些病例？
    3. "病例五和病例六是同一个错的两个层级"——同意吗？为什么？
    4. 附加：把六个病例讲给一个没学过指针的人听，
       讲得清才是真懂（费曼检验）

===================== 在下面写你的代码 =====================
*/


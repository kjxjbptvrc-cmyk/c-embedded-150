/*
=====================================================================
 【第 082 题】main 函数的参数：程序第一次听见外部世界
 难度：★★★★☆
 知识点：int main(int argc, char *argv[])、命令行参数、字符串转数字
=====================================================================

【题目描述】
    main 也是函数，也有参数。一直被你写成 main(void) 的它，
    完整形态是：int main(int argc, char *argv[])。
    argc 是参数个数（含程序名自己）；argv 是字符串表——
    076 那个结构的真身登场，而且 argv[argc] 保证是 NULL 哨兵。

    第一步：把 argc 和每一个 argv[i] 打印出来，
    亲眼确认 argv[0] 是什么（程序自己的路径）。

    第二步：命令行加法器。运行 .\adder.exe 12 34 输出 12 + 34 = 46。
    注意：参数是字符串 "12"，不是数字 12——
    用 058 手写的 my_atoi（或库函数 atoi）转成整数再加。

    第三步：健壮性。argc 不是 3 就打印用法提示并 return 1。
    非 0 返回值是在告诉操作系统"我没干成"——001 那个
    "return 0 返回给谁"的问题，今天补上下半句。

    编译运行（在 Cursor 的 PowerShell 终端里）：
        gcc .\082_main函数的参数.c -o adder
        .\adder.exe 12 34

    〔嵌入式点〕这是你的程序第一次接收"外部世界的配置"。
    设备通过串口收一行配置命令、拆开、执行，是同一种思维——
    144 题的 mini shell 会自己造出 argc 和 argv。

【输入输出示例】（PowerShell 里运行两次）
    PS> .\adder.exe 12 34
    argc = 3
    argv[0] = .\adder.exe   （程序自己的路径写法，环境不同样子不同）
    argv[1] = 12
    argv[2] = 34
    12 + 34 = 46

    PS> .\adder.exe 5
    用法：adder.exe 数字1 数字2

【要求】
    1. main 写完整形态 int main(int argc, char *argv[])
    2. 逐个打印 argv[i]，循环条件用 i < argc
    3. 相加前必须做"字符串到整数"的转换，058 的 my_atoi 搬来最好
    4. argc 不等于 3：打印用法并 return 1
    5. 在终端里用不同参数至少跑三次（含一次参数个数不对的）

【提示】
    - IDE 的"运行"按钮通常不带参数，这题必须去终端手敲命令
    - 本题输入保证是非负整数，my_atoi 不用管负号——边界自己声明
    - 挑战版：打印 argv 时不用 argc，改用 076 的 NULL 哨兵遍历

【吃透自检】
    1. char *argv[] 和 char **argv 为什么等价？（069 的退化，一句话）
    2. argv[argc] 是什么？哪一题的哨兵和它一模一样？
    3. "参数全是字符串"意味着拿到数字之前必须做什么？
    4. return 1 和 return 0 各对操作系统说了什么？
       在 PowerShell 里输入 $LASTEXITCODE 能看到上一次的返回值
    5. 附加：升级成任意个数连加：.\adder.exe 1 2 3 4 输出 10

===================== 在下面写你的代码 =====================
*/


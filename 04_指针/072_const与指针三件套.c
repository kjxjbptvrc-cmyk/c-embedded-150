/*
=====================================================================
 【第 072 题】const 与指针三件套：到底谁被焊死了
 难度：★★★★☆
 知识点：const int *p、int *const p、const int *const p、
         "从右往左读"口诀
=====================================================================

【题目描述】
    071 题末尾的 const char * 只是三件套之一。今天看全家福
    （设 int a = 1, b = 2;）：

        const int *p = &a;          （一号）
        int *const q = &a;          （二号）
        const int *const r = &a;    （三号）

    1. 逐个做"编译报错实验"：对每一号分别尝试两种操作——
       改指向的值（如 *p = 5;）和改指向本身（如 p = &b;）。
       哪个能过、哪个报错？一共六次尝试，逐次记录
    2. 把编译器报错信息【原文】抄进注释（报错里多半有
       read-only 字样）。跟编译器学 C，是自学者的真本事
    3. 学"从右往左读"口诀，把三件套各读成一句人话。
       比如一号：p 是个指针，指向 int，而这个 int 是 const
       ——值不许经 p 改，但 p 本身可以换目标
    4. 实战落地：把 070 的 my_strcpy 源参数改成 const char *src
       并重新编译通过——"我只读你"的承诺从此写进函数签名

【实验记录示例】（报错措辞因编译器而异，抄你自己看到的）
    一号 *p = 5;   编译报错: assignment of read-only location
    一号 p = &b;   编译通过
    二号 q = &b;   编译报错: assignment of read-only variable
    二号 *q = 5;   编译通过
    三号：两种操作全部报错

【要求】
    1. 六次尝试的结果在注释里整理成一张小表：
       型号│改指向的值│改指针本身，能改画 O，报错画 X
    2. 三句"从右往左读"的人话翻译写进注释
    3. my_strcpy 加完 const 后，故意在函数体里写一句
       *src = 'x'; 看看报错——体会 const 参数怎么替你挡枪，
       看完删掉

【提示】
    - 口诀：从变量名出发，遇到 const 念"只读"，遇到星号念
      "指向"。谁挨着 const，谁被焊死
    - 分不清一号二号时盯住星号：const 在星号左边，管"指向的
      值"；const 在星号右边，管"指针本身"

【吃透自检】
    1. 三件套各自"能改什么、不能改什么"？合上书报菜名
    2. 库函数 strlen 的参数为什么声明成 const char *？
       换成 char * 会损失什么？
    3. 一号写法能不能指向一个本来不是 const 的变量？
       （能——它约束的是"经由这个指针"的操作，不是变量本身）
    4. 附加挑战：以后你会在寄存器操作里见到 volatile 和 const
       同时修饰一个指针（第 130 题详解），修饰规则和今天完全
       同构——先猜猜 volatile int *p 该怎么读

===================== 在下面写你的代码 =====================
*/


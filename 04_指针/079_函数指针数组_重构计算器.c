/*
=====================================================================
 【第 079 题】函数指针数组：把 switch 计算器重构成查表分发
 难度：★★★★★
 知识点：int (*ops[4])(int, int)、表驱动法、与中断向量表同构
=====================================================================

【题目描述】
    还记得 010 的 switch 简易计算器吗（读入 a op b 做四则运算）？
    今天用函数指针数组重构它，体验"表驱动"编程。

    1) 写四个同型函数：add、sub、mul、div_。
       除法名字带下划线，避开标准库里现成的 div；
       div_ 内部要做除零防御：b 为 0 时打印一行错误并返回 0。

    2) 建两张下标严格对齐的表：
           int (*ops[4])(int, int) = {add, sub, mul, div_};
           const char symbols[] = "+-x/";
       本题约定乘法用小写字母 x——星号在命令行等环境里容易
       被转义抢戏，换个符号是常见的务实做法。

    3) 读入 a op b（scanf 格式串用 "%d %c %d"），在 symbols 里
       找到 op 的下标 i，然后一行完成分发：结果 = ops[i](a, b)。
       找不到运算符就报"不认识"，不许带着错下标去查表。

    4) 对比作业（写进你的注释）：要新增一种取余运算，
       switch 版要改几处？表驱动版要改几处？各自列出来。

    声明拆读（074 的优先级规则，写进你的注释）：ops 先和 [4]
    结合——它是 4 元素数组；每个元素是 int (*)(int, int)，
    即函数指针。

    〔嵌入式点〕单片机的中断向量表，本质就是一张函数指针数组——
    硬件按中断号查表跳转，和你这题同构。

【输入输出示例】（运行三次）
    输入 7 x 6     输出 7 x 6 = 42
    输入 9 - 15    输出 9 - 15 = -6
    输入 8 / 0     输出 error: divide by zero
                        8 / 0 = 0   （防御生效，返回了 0）

【要求】
    1. 四个函数签名统一为 int f(int, int)；div_ 必须除零防御
    2. ops 与 symbols 两张表下标一一对应——顺序错一位，
       加法就成了减法
    3. 分发必须是"查下标 + 一行调用"，主流程不许再写 switch
       或 if 链选函数
    4. 找不到运算符时给一句人话提示
    5. 注释里完成：ops 声明拆读 + 新增取余的两版改动对比

【提示】
    - 找下标可以自己 for 循环扫 symbols，也可以用 string.h 的
      strchr——它返回地址，减掉表头地址就是下标（068 的指针相减）
    - 初始化列表里直接写函数名即可，不用取地址符

【吃透自检】
    1. int (*ops[4])(int, int) 逐层拆读：ops 先和谁结合？
       它先是个什么，装的又是什么？
    2. 表驱动好在哪？一句话填空：把易变的逻辑变成了______
    3. 中断向量表那句话，合上书用自己的话复述一遍：
       中断号相当于本题里的哪个变量？
    4. 两张表靠下标对齐有什么隐患？改一张忘另一张怎么防？
       （086 结尾的 typedef、091 的结构体会给出更稳的答案）
    5. 附加：真把取余加上并跑通，数一数你实际改动了几行

===================== 在下面写你的代码 =====================
*/


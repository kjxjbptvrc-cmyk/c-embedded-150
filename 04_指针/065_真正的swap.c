/*
=====================================================================
 【第 065 题】真正的 swap：028 欠的账，今天还
 难度：★★★☆☆
 知识点：传地址（指针参数）让函数改到实参、值传递的完整真相
=====================================================================

【题目描述】
    第 028 题的惨案还记得吗：swap(int a, int b) 在函数里换得
    热闹，main 里的 x、y 纹丝不动；当时还打印过函数内外地址
    不同——形参只是实参的复印件。题里承诺第 065 题揭晓正解，
    今天兑现。

    1. 把 028 的失败版原样再写一遍：void swap_fail(int a, int b)
    2. 写正解：void swap(int *pa, int *pb)，函数内部借一个
       临时变量，通过解引用完成交换
    3. main 里 int x = 3, y = 5; 先调 swap_fail(x, y) 打印一次，
       再调 swap(&x, &y) 打印一次，对比结果
    4. 打印一条"地址链路"：main 里打印 &x，swap 里打印 pa 的
       值——两个数一模一样！注意分辨：028 打印的是形参 a
       自己的地址（和 &x 不同）；今天打印的是 pa 格子里存的
       内容，两码事

【输出示例】（地址为示意值，你机器上不同，但上下两行必然相等）
    初始:        x = 3, y = 5
    失败版之后:  x = 3, y = 5
    main 里 &x      = 0061FF0C
    swap 里 pa 的值 = 0061FF0C
    真 swap 之后:   x = 5, y = 3

【要求】
    1. swap 内部只许通过 *pa、*pb 操作，不许用全局变量作弊
    2. 在注释里把这句话用自己的话完整复述并解释：
       "这次复制的仍然是副本——但复制的是地址，
        而地址的副本依然指向原来的变量"
    3. 003 题欠的账一起还：在注释里写清为什么 scanf("%d", &x)
       必须传 &x——scanf 想改你的变量，凭的和 swap 是同一个理

【提示】
    - 值传递从来没有被推翻：传 int 复制 int，传地址复制地址。
      变的不是规则，是你递过去的东西
    - 交换两个 int 要借第三个 int；那交换"两个格子的内容"，
      临时变量该是什么类型？想清楚再动手

【吃透自检】
    1. *pa 操作的是谁的内存？说出变量名
    2. 这次传参复制的是什么？（要求 2 那句话必须能脱口而出）
    3. 在 swap 里写 pa = NULL，main 里的 x 会受影响吗？x 的
       地址会变吗？（都不会——但为什么？想通这一条，
       第 075 题的二级指针你就赢在起跑线）
    4. 附加挑战：写 void swap_double(double *pa, double *pb)，
       体会"套路一模一样，只是类型换了"

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 080 题】回调函数：把"做什么"当参数传进去
 难度：★★★★★
 知识点：回调、void (*f)(int *)、可定制的 for_each 与 sort
=====================================================================

【题目描述】
    你以前写的函数，参数全是"数据"。从今天起，参数还可以是
    "行为"——这就是回调。

    1) 写 void for_each(int *a, int n, void (*f)(int *))：
       对每个元素执行一次 f(&a[i])。注意传的是元素地址，
       回调才有资格修改元素（065 的老道理）。

    2) 写三个回调：print_it（打印元素）、double_it（翻倍）、
       zero_it（清零）。同一个 for_each 喂不同回调，三种效果。

    3) 写 void sort(int *a, int n, int (*cmp)(int, int))：
       045 的冒泡骨架原样搬来，唯独"该不该交换"这一问交给 cmp：
       cmp(前, 后) 返回非 0 就交换。再写 asc、desc 两个比较函数，
       同一个 sort 排出升序和降序——045 排序的终极形态。

    4) 040 的伏笔今天兑现：当时想换一条曲线就得改绘图代码；
       现在你懂了——把 f(x) 作为函数指针传进绘图函数，绘图代码
       一行不动。这条不用写码，在注释里写两句体会即可。

    这套"控制流反转"有个绰号叫好莱坞原则：别打给我们，
    我们会打给你——不是你调它，是它在合适的时机调你。

    〔嵌入式点〕厂商 HAL 库的套路"中断来了，调你注册的那个函数"
    就是回调注册；141 题的状态机、145 题的软件定时器，全靠这一手。

【输出示例】（数组初值 3 1 4 1 5）
    原样：3 1 4 1 5
    翻倍：6 2 8 2 10
    清零：0 0 0 0 0
    升序：1 1 3 4 5
    降序：5 4 3 1 1

【要求】
    1. for_each 和 sort 的骨架里不许出现具体业务：不许偷偷
       printf、不许写死升序——行为差异全部来自传入的函数指针
    2. 三个回调、两个比较函数齐全，签名按题面
    3. 打印数组一律通过 for_each(a, n, print_it) 完成，换行自己补
    4. 清零之后没得排了——排序实验前重新初始化数组
    5. 注释里画一张小图：for_each 是骨架，f 是插进骨架的插头

【提示】
    - 回调想改元素，参数就得是 int *，所以 for_each 传 &a[i]
    - cmp 只回答"要不要交换"，交换动作留在 sort 里——分工要干净
    - 冒泡里原来的 if (a[j] > a[j + 1]) 换成 if (cmp(a[j], a[j + 1]))

【吃透自检】
    1. 回调翻转了什么？控制流方向上，调用者和被调用者换了什么位置？
    2. for_each 里"不变"的是什么、"变"的是什么？分别由谁负责？
    3. asc 和 desc 只差一个比较符号——策略被压缩到了多小的地方？
       这对"再加一种排序规则"意味着什么？
    4. 附加：查一眼标准库 qsort 的参数表，对比你的 sort：
       它的比较函数为什么收 const void *？（081 马上揭晓）

===================== 在下面写你的代码 =====================
*/


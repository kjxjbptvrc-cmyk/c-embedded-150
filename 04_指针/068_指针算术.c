/*
=====================================================================
 【第 068 题】指针算术：按元素走，不按字节走
 难度：★★★★☆
 知识点：指针加减的步长是元素、指针相减得元素个数、
         首尾双指针技法
=====================================================================

【题目描述】
    067 题末尾埋了一句：p++ 挪的是一个 int。今天把规则钉死：
    指针加 1，前进 sizeof(指向的类型) 个字节。

    1. 分别定义 int ia[3]、char ca[3]、double da[3]，各取首地址
       存进对应类型的指针，打印 p 和 p + 1，人肉算一下
       十六进制差值：应当分别差 4、1、8 个字节
    2. 指针相减：int *p1 = &ia[0]; int *p2 = &ia[2];
       打印 p2 - p1——得 2（单位是元素个数，不是字节数 8）
    3. 实战：int arr[8] = {1,2,3,4,5,6,7,8}; 用首尾双指针原地
       逆序：left 从头、right 从尾，相向而行，边走边交换。
       第 043 题你用双下标干过这活，今天换成真指针——
       交换两个格子的内容，正好调用你 065 题写的
       swap(left, right)，一行搞定

【输出示例】（地址为示意值，差值关系才是重点）
    int:    p = 0061FF00   p + 1 = 0061FF04
    char:   p = 0061FEFB   p + 1 = 0061FEFC
    double: p = 0061FED0   p + 1 = 0061FED8
    p2 - p1 = 2
    逆序前: 1 2 3 4 5 6 7 8
    逆序后: 8 7 6 5 4 3 2 1

【要求】
    1. 逆序循环的条件用 left < right，循环体内不许出现方括号
       和下标变量
    2. 在注释里画双指针图：八个格子排开，标出 left、right 的
       起步位置和第一次交换后各自的新位置，画两帧即可
    3. 在注释里回答：数组元素是奇数个时，两个指针最后停在哪？
       中间那个元素会不会被换乱？

【提示】
    - 指针相减的结果类型叫 ptrdiff_t，打印用 %td；图省事
      强转成 (int) 再用 %d 也行
    - 想让程序自己算出字节差：把两个地址都强转成 (char *)
      再相减，就是按 1 字节为单位在数了

【吃透自检】
    1. p + 1 到底前进多少字节？公式一句话说出来
    2. p2 - p1 的单位是什么？什么前提下这个减法才有意义？
    3. 指向两个不同数组的指针相减，为什么无意义？
       （两个数组之间隔着什么，谁也说不准）
    4. 附加挑战：不用第二个指针，只用一个指针加一个整数长度，
       还能写出原地逆序吗？写完对比：哪种更顺手，为什么？

===================== 在下面写你的代码 =====================
*/


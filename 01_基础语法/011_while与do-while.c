/*
=====================================================================
 【第 011 题】while 与 do-while：三次机会的开机密码
 难度：★★☆☆☆
 知识点：while 与 do-while 的执行次序差异、重试次数控制
=====================================================================

【题目描述】
    设备固件里到处是这种逻辑：开机自检不过就重试，传感器初始
    化失败就再试一次，最多试 N 次。本题做它的键盘版：
    程序预设整数密码 1234，用 do-while 给用户最多 3 次机会：
        输对      输出 unlocked, tries = 实际用的次数，结束
        输错      输出 wrong password，还有机会就继续读
        3 次全错  最后输出 system locked

【输入输出示例】（运行两次）
    第一次运行依次输入 1111、1234，屏幕上是：
        wrong password
        unlocked, tries = 2
    第二次运行依次输入 1、2、3，屏幕上是：
        wrong password
        wrong password
        wrong password
        system locked

【要求】
    1. 主体必须用 do-while 写
    2. 密码用 int 变量预设在代码里
    3. 解锁时报出的次数必须准确
    4. 在你代码的注释里写清：同样的逻辑改用 while 写，结构上
       要挪动什么？为什么这里 do-while 更顺手？

【提示】
    - "先做一次，再决定要不要继续"——这句话就是 do-while
      的性格
    - 继续循环的条件要同时盯两件事：还没输对、机会还没用完

【吃透自检】
    1. do-while 的循环体至少执行几次？while 呢？ 前者两次，后者3次
       【订正】答错了。"至少"问的是最少可能值，与本题跑了几轮无关：
              do-while 至少 1 次，while 至少 0 次（条件一开始就为假时，
              循环体一行都不执行，直接跳到循环后面）。
    2. 什么场景天然适合 do-while？（读输入、显示菜单这类  需要先输入再判断的用dowhile，如果一来就需要判断，就需要while
       "至少来一次"的活）
       【答对】更精准的判断标准：第一次执行是不是无条件必须发生。
              是→do-while；不一定→while。
    3. 死循环怎么写？回想第 005 题里 while(1) 对固件的意义 死循环应该不是看tries，而是看password的值，就是一个永远不等于password，但是我不知道有哪些
       【订正】答偏了。故意写的死循环就是把条件写成恒真，三种写法：
              while(1)、for(;;)、do{...}while(1)，不用去找"永远不相等的值"。
              你说的那种是"意外死循环"，见下面【犯过的错】第 2 条。
    4. 附加挑战：锁定时在 system locked 之前多输出一行
       wait 30 seconds（真正的延时以后学，先用打印占位）  这什么意思?
       【解答】真实设备连错几次不让你立刻重试，会先冷却一段时间（手机锁屏
              就是这样），这题要你把这个行为体现在输出里。真延时要调
              Sleep() 之类的函数，还没学到，所以先用一行 printf 占位，
              假装那 30 秒过去了：在打印 system locked 之前加一行
              printf("wait 30 seconds\n"); 不用真等。

===================== 在下面写你的代码 =====================
*/


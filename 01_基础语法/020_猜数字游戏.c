/*
=====================================================================
 【第 020 题】猜数字游戏：第一次"借用"库函数
 难度：★★★☆☆
 知识点：rand 随机数、循环加分支的综合、交互式程序
=====================================================================

【题目描述】
    程序开局生成一个 1~100 的随机数当谜底，让用户反复猜：
        猜大了输出 too big，猜小了输出 too small，
        猜中输出 bingo! tries = 总次数，游戏结束。
    随机数这次直接"借用"，库的机制以后自然会懂：
        文件开头加 #include <stdlib.h> 和 #include <time.h>
        main 开头执行一次 srand(time(NULL));
        取随机数写 rand() % 100 + 1

【运行效果示例】（假设谜底是 42；每行是一轮问答）
    guess: 50      too big
    guess: 25      too small
    guess: 37      too small
    guess: 43      too big
    guess: 40      too small
    guess: 42      bingo! tries = 6

【要求】
    1. srand 只在开局执行一次，绝不放进循环里
    2. 主循环用 while(1) + break 或 do-while 都行，猜中才离场
    3. 次数从 1 算起：第一把就猜中输出 tries = 1
    4. 附加：加上"最多 7 次"的限制，7 次没中输出 game over
       并揭晓谜底——为什么偏偏是 7？做完自检第 3 条你就明白

【提示】
    - rand() 返回一个很大的非负整数：% 100 把它压进 0~99，
      再 + 1 平移到 1~100
    - 先打印提示再 scanf，交互才像一场对话

【吃透自检】
    1. 不写 srand 会怎样？为什么调试阶段这个"缺点"反而是
       优点？
    2. 推导 rand() % 100 + 1 的取值范围；rand() % 101 呢？
    3. 用二分法猜 1~100，最多几次必中？和 2 的幂什么关系？
    4. 附加挑战：想要 5~50 的随机数，表达式怎么写？


===================== 在下面写你的代码 =====================
*/


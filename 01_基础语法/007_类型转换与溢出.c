/*
=====================================================================
 【第 007 题】类型转换与溢出：预测机器的"意外"
 难度：★★☆☆☆
 知识点：隐式类型转换、强制转换的截断、有符号溢出的回绕现象、
         unsigned 与负数比较的坑
=====================================================================

【题目描述】
    6 个结果，先动笔预测（写进你代码的注释），再编程验证：

        (1) 7 / 2                （int 除 int） 3
        (2) 7 / 2.0              （int 和 double 混算）3.0
        (3) (char)300            （硬塞进 8 位，剩下什么） 不懂什么意思
        (4) signed char s = 127; 执行 s = s + 1; 之后 s 的值 128,无符号不就是有128吗？
        (5) int big = 2147483647; 表达式 big + 1 的值 -1还是-2147483648 我有点忘记了
        (6) -1 > 1u 的值是 0 还是 1（先想清楚再跑，答案惊人） lu还是1u 我有点分不清，那个u是啥？

    单片机上 8 位变量的溢出回绕是家常便饭：定时器计数到 255
    再加 1 回到 0、环形缓冲的下标回卷……对这类现象你必须有
    直觉，而不是每次都被吓一跳——这题就是在攒这个直觉。

【输出示例】
    7 / 2 = 3
    7 / 2.0 = 3.500000
    (char)300 = 44
    127 + 1 = -128
    2147483647 + 1 = -2147483648
    -1 > 1u = 1

【要求】
    1. 六个都先预测后验证，预测错的保留在注释里并写明原因
    2. (3)(4) 用 %d 打印，(2) 用 %f 打印
    3. 编译器若对 (5)(6) 发出警告，别关掉，读懂它在警告什么

【提示】
    - 300 的二进制是 100101100，(char) 只装得下最低 8 位
    - (6) 的关键：有符号和无符号放在一起比较时，谁被转换了？

【吃透自检】
    1. 为什么 -1 > 1u 为真？-1 被转换成了多大的一个数？
    2. 强转截断保留的是低位还是高位？
    3. 隐式转换的大方向是什么？（往更宽、更能装的类型靠）
    4. (5) 这种有符号溢出在 C 标准里算什么性质的行为？实测
       出的回绕结果为什么不能依赖？
    5. 附加挑战：unsigned char 的 255 + 1 是多少？它和 (4)
       的回绕是同一回事吗？

===================== 在下面写你的代码 =====================
*/


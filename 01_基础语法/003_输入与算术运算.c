/*
=====================================================================
 【第 003 题】scanf 输入与算术运算
 难度：★★☆☆☆
 知识点：scanf 与取地址符 &、算术运算符 + - * / %、
         整数除法与取余、强制类型转换
=====================================================================

【题目描述】
    写一个"整数计算报告器"：
    从键盘读入两个整数 a 和 b（保证 b 不为 0），依次输出
    它们的和、差、积、整数商、余数，以及精确到小数的平均值。

【输入示例】
17 6

【输出示例】
a + b = 23
a - b = 11
a * b = 102
a / b = 2
a % b = 5
average = 11.50

【要求】
    1. a、b 必须用 scanf 读入，不许写死在代码里
    2. 商是整数除法的结果（17 / 6 = 2，不是 2.83）
    3. 平均值必须保留 2 位小数。注意：a 和 b 都是 int，
       直接 (a + b) / 2 是得不到小数部分的，想想怎么办
    4. 输出格式与示例完全一致（提示：% 字符怎么原样输出，
       第 001 题里用过）

【提示】
    - scanf("%d", &a);  别忘了 & 符号
    - 把 int 转成 double 可以用强制类型转换：(double)(a + b)

【吃透自检】
    1. scanf 里的 & 是什么意思？漏写了会发生什么？
    2. 17 / 6 和 17.0 / 6 的结果分别是什么？规则是什么？
    3. (double)(a + b) / 2 和 (double)((a + b) / 2)
       结果一样吗？为什么？
    4. % 取余运算能用在 float 上吗？
    5. 附加挑战：读入一个总秒数（比如 8000），只用 / 和 %
       把它换算成 时:分:秒 输出（8000 秒 → 2:13:20）。
       这个套路在嵌入式里处理时间戳经常用

===================== 在下面写你的代码 =====================
*/


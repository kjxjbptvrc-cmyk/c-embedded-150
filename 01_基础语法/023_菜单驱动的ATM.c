/*
=====================================================================
 【第 023 题】菜单驱动的 ATM：设备主循环的雏形
 难度：★★★☆☆
 知识点：while(1) + switch 菜单循环、标志位退出、余额维护
=====================================================================

【题目描述】
    第 005 题说过 while(1) 是固件的主心骨。设备的人机交互基本
    都长一个样：主循环里"显示菜单、读命令、分发处理"，串口
    调试菜单、电饭煲面板、ATM 全是这一个骨架。本题写键盘版：
    余额初始 1000，循环显示菜单并处理：
        1  查询余额
        2  存款：读入金额，加进余额
        3  取款：读入金额，余额不够要拦截报错
        0  退出：打印 bye，程序结束
    其他数字一律提示 bad choice。

【输入输出示例】（一次运行，菜单打印略去）
    输入 1            输出 balance = 1000
    输入 2 再输 500   输出 balance = 1500
    输入 3 再输 2000  输出 error: insufficient balance
    输入 3 再输 300   输出 balance = 1200
    输入 0            输出 bye

【要求】
    1. 主循环先用 while(1) 写，命令分发用 switch
    2. 余额用一个变量全程维护
    3. 存、取金额必须大于 0，不合法就拒绝——先拦非法输入的
       习惯从第 004 题一路带过来
    4. 亲手踩坑：先在退出分支里只写 break，运行后发现程序根
       本退不出去，把这个现象写进你代码的注释；然后引入标志
       位（循环条件盯着它，退出分支给它清零）真正退出

【提示】
    - 标志位骨架：int running = 1; 循环条件看它，退出分支里
      把它置 0
    - switch 里的 break 只负责跳出 switch，够不着外面的循环

【吃透自检】
    1. switch 的 break 和循环的 break "撞名"了，break 到底
       听谁的？
    2. 标志位方案动了三处代码：定义、循环条件、清零，分别在
       哪一行？
    3. 非法菜单项怎么防？default 里该做什么、不该做什么？
    4. 除了标志位还有什么退出办法？（return 0 一了百了、
       goto……各是什么味道，哪个适合现在的你？）
    5. 附加挑战：给取款加一条"单次不超过 500"的风控规则

===================== 在下面写你的代码 =====================
*/


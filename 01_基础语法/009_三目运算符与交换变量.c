/*
=====================================================================
 【第 009 题】三目运算符与交换变量：一行分支，两数换位
 难度：★★☆☆☆
 知识点：三目运算符 ? :、嵌套三目、交换两个变量的经典套路
=====================================================================

【题目描述】
    任务一：读入两个整数，用三目运算符求较大值输出；再读入
    三个整数，用嵌套三目在一条语句里求出最大值。全程不许出现
    if。
    任务二：a = 3、b = 5，互换两个变量的值并打印验证，要求写
    两种方法：
        方法 1：临时变量中转
        方法 2：算术法 a = a + b; b = a - b; a = a - b;
    做完方法 1 记得把 a、b 复位，再做方法 2。
    其实还有一种既不借临时变量、又没有溢出隐患的写法——异或
    交换法，等第 113 题学位运算时揭晓。

【输入输出示例】
    输入 12 7     输出 max2 = 12
    输入 3 9 6    输出 max3 = 9
    交换部分固定输出：
        before:  a = 3, b = 5
        method1: a = 5, b = 3
        method2: a = 5, b = 3

【要求】
    1. 求最大值只许用三目，嵌套三目必须写在一条语句里
    2. 两种交换都要打印结果验证
    3. 在你代码的注释里回答：算术法在 a、b 都非常大时藏着什么
       隐患？（第 007 题刚见过的那种） 会让加起来的结果不正常，导致最后的a，b都变成回绕后的计算值

【提示】
    - 两数较大值：a > b ? a : b
    - 三数最大：把"两数较大值"整体当一个操作数，再和第三个比

【吃透自检】
    1. 三目运算符的结合方向是什么？a ? b : c ? d : e 相当于
       加了什么括号？
    2. 算术法交换的溢出隐患具体出在哪一步？
    3. 三种交换里你最推荐哪种？从可读性和安全性各说一句。可读性来说第一种最好，安全性的话也是第一条。但是第二种可以减少内存开支
    4. 附加挑战：用一个三目把"分数 >= 60 输出 pass，否则输出
       fail"塞进一条 printf 里

===================== 在下面写你的代码 =====================
*/


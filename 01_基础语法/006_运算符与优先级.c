/*
=====================================================================
 【第 006 题】运算符与优先级：先笔算，再让编译器批卷
 难度：★★☆☆☆
 知识点：算术、关系、逻辑运算符的优先级与结合方向、
         i++ 与 ++i、复合赋值、逗号表达式
=====================================================================

【题目描述】
    下面 8 个表达式，先动笔逐个算出值（全按 int 算），把预测
    写进你代码的注释，再用 printf("%d\n", ...) 逐个打印对账。
    错一个，就把对应那条优先级规则查清楚再往下走：

        (1) 3 + 5 % 2 * 4  7
        (2) 10 / 4 * 4     8     （整数除法，第 003 题的老朋友）
        (3) !5  120  错误，原来！不是阶乘，而是一种运算，把非0变成0 变0变成1
        (4) !0 + 1 2 
        (5) 3 > 2 && 2 > 1  True
        (6) 5 > 3 || 2 > 4 && 0   不清楚
        (7) 2 < 3 == 1  不清楚 
        (8) (2, 4, 6)           （打印时保留这层括号）

    然后做两个自增实验，各自独立写成两条语句：
        实验 A：a = 5; 执行 b = a++;  打印 a 和 b
        实验 B：a = 5; 执行 c = ++a;  打印 a 和 c
    最后预测并验证：a = 4; 执行 a *= 2 + 3; 之后 a 是多少？

【输出示例】（输出排版不限，但每个值必须一模一样）
    (1)=7  (2)=8  (3)=0  (4)=2  (5)=1  (6)=1  (7)=1  (8)=6
    A: a = 6, b = 5
    B: a = 6, c = 6
    a *= 2 + 3 之后 a = 20

【要求】
    1. 预测必须以注释形式留在代码里，算错的不许删，在旁边
       写明错因——这是给自己留的病历
    2. 实验 B 开始前把 a 重新赋回 5
    3. 本题每个表达式最多只动一次某个变量。a++ + ++a 这种在
       同一个表达式里对同一变量多次自增自减的代码是未定义
       行为，是禁区，永远别写

【提示】
    - 取余 % 和乘、除同级，同级从左往右算
    - ! 的优先级比算术运算符还高
    - 逗号表达式的值取最后一项

【吃透自检】
    1. 优先级记不清时最靠谱的办法是什么？（加括号不丢人，
       嵌入式编码规范普遍强制复杂表达式加括号）
    2. 赋值表达式本身有值吗？a = b = 5 为什么合法，先算哪边？
    3. i++ 作为表达式，它的"值"和"副作用"分别是什么？
    4. 附加挑战：给 (6) 手工加满括号，写出与默认优先级完全
       等价的形式，再验证一遍

===================== 在下面写你的代码 =====================
*/


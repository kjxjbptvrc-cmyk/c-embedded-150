/*
=====================================================================
 【第 018 题】最大公约数：看辗转相除一步步收敛
 难度：★★★☆☆
 知识点：辗转相除法（循环版）、最小公倍数、先除后乘防溢出
=====================================================================

【题目描述】
    读入两个正整数，求最大公约数 gcd 与最小公倍数 lcm。
    辗转相除一句话：gcd(a, b) 等于 gcd(b, a % b)，反复替换，
    b 变成 0 时 a 就是答案。本题还要求把收敛过程亮出来：每完
    成一轮替换就打印当前的 a 和 b，亲眼看它跌到 0。

【输入输出示例】
    输入 24 36
    输出
        a = 36, b = 24
        a = 24, b = 12
        a = 12, b = 0
        gcd = 12
        lcm = 72

【要求】
    1. 用循环实现，不许递归（函数都还没学，忍住）
    2. 每轮替换后打印一行当前的 a 和 b
    3. lcm 必须写成 a / gcd * b，不许写 a * b / gcd——后者的
       中间结果 a * b 可能先把 int 撑爆（第 007 题的教训）
    4. 循环会把 a、b 改得面目全非，算 lcm 前先用副本保住原值
    5. 输入不保证大小顺序：24 36 和 36 24 都必须正确

【提示】
    - 第一轮有彩蛋：a 比 b 小时，a % b 就是 a 本身，一轮之后
      两数自动换位——对照示例第一行看
    - 收敛的把柄就一条：余数一定小于除数

【吃透自检】
    1. 辗转相除为什么必然终止？
    2. 先除后乘防的是什么？a / gcd 为什么一定除得尽？
    3. gcd(0, n) 应该是多少？拿你的程序验证一下
    4. 附加挑战：先在纸上手动模拟输入 1071 462 时每一轮的
       a 和 b，再运行核对


===================== 在下面写你的代码 =====================
*/


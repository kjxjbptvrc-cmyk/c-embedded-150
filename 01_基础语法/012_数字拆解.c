/*
=====================================================================
 【第 012 题】数字拆解：数码管眼里的整数
 难度：★★☆☆☆
 知识点：用 % 10 和 / 10 循环拆位、位数统计、逆序数
=====================================================================

【题目描述】
    要在 4 位数码管上显示 3407，固件得先把它拆成 3、4、0、7
    逐位送显——拆位是嵌入式的日常手艺，这题把它练熟。
    读入一个正整数 n（位数不定），输出三样东西：
        1) 位数
        2) 各位数字之和
        3) 逆序数（123 的逆序数是 321）

【输入输出示例】
    输入 3407
    输出
        digits = 4
        sum = 14
        reversed = 7043

【要求】
    1. 只许用 % 10 和 / 10 在整数上做文章，不许转成字符串
    2. 一个循环同时完成三项统计，不许扫三遍
    3. 逆序数必须用算术方法累积出一个真正的整数，不许只是
       倒着逐位打印装样子

【提示】
    - n % 10 抠出个位，n / 10 砍掉个位，两板斧轮流抡
    - 逆序数：每读到一位新数字，想想旧结果该怎么"腾位置"

【吃透自检】
    1. n % 10 为什么恰好是个位？
    2. 循环什么时候该停？如果输入是一位数，三个结果各是什么？
    3. 逆序数的累积公式 r = r * 10 + d 为什么是对的？（每来
       一位，旧数整体向左挪一个十进制位）
    4. 附加挑战：输入 1000，你的逆序数输出 1 还是 0001？
       整数里存不存在"前导零"这回事？

===================== 在下面写你的代码 =====================
*/


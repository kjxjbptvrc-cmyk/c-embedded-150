/*
=====================================================================
 【第 021 题】极值与统计：数据流过，极值常新
 难度：★★★☆☆
 知识点：滚动求最大最小值（不用数组）、累加与平均、哨兵值
=====================================================================

【题目描述】
    设备采样就是这个节奏：数据一个一个流过来，内存里从不保存
    全部历史，但极值和累加量永远是最新的。本题做键盘版：
    先读入个数 n，再依次读入 n 个整数，输出最大值、最小值、
    总和、平均值（保留 2 位小数）。全程不许用数组，读一个
    消化一个。

【输入输出示例】
    输入
        5
        7 -2 19 4 3
    输出
        max = 19
        min = -2
        sum = 31
        avg = 6.20

【要求】
    1. 不用数组：循环里滚动更新各统计量
    2. max、min 的初值不许拍脑袋写 0——用第一个读到的数当
       初值（想想输入全是负数会怎样）
    3. 平均值是整数和除以整数个数，直接除小数会被砍掉——用
       第 003 题的强制转换处理，%.2f 输出
    4. 附加：再写一个哨兵版本：不先报个数，读到 -999 就收工，
       -999 本身不参与统计

【提示】
    - 每来一个新数，跟当前 max、min 各比一次，该换就换
    - 哨兵版的循环条件和读入顺序值得先画一画再写

【吃透自检】
    1. max 初值设 0，输入全是负数时会输出什么？错在哪？
    2. 除了"拿第一个数当初值"，还有别的初值策略吗？各有什么
       讲究？
    3. 本题为什么不用数组也能做？哪类统计没有数组就做不了？
       （比如求中位数——第 047 题数组登场后见分晓）
    4. 附加挑战：n 很大时 sum 可能出什么事？（又是第 007 题）
       换什么类型能多撑一阵？long int

===================== 在下面写你的代码 =====================
*/


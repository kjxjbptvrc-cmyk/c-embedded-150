/*
=====================================================================
 【第 024 题】百钱买百鸡：给循环搜索"减肥"
 难度：★★★☆☆
 知识点：枚举法、用等式约束消元、循环次数实测对比
=====================================================================

【题目描述】
    古题：公鸡 5 钱一只，母鸡 3 钱一只，小鸡 1 钱 3 只。
    100 钱恰好买 100 只鸡，问公鸡 x、母鸡 y、小鸡 z 各几只，
    打印全部方案。要求写两个版本：
    版本一（暴力）：x、y、z 三层循环各自从 0 数到 100，同时
        检查"只数等于 100"和"钱数等于 100"两个条件。
    版本二（消元）：由只数等式直接令 z = 100 - x - y，z 不再
        循环，双层循环搞定。
    两版各放一个 long 计数器在最内层循环体第一行，无条件加 1，
    最后打印两个计数，看看差距有多悬殊。

【输出示例】
    cock = 0, hen = 25, chick = 75
    cock = 4, hen = 18, chick = 78
    cock = 8, hen = 11, chick = 81
    cock = 12, hen = 4, chick = 84
    count1 = 1030301
    count2 = 10201
    （两版打印出的 4 组方案必须一模一样）

【要求】
    1. 钱数条件：z 必须是 3 的倍数，且 5 * x + 3 * y + z / 3
       恰好等于 100（也可以把整个等式乘 3，连除法都省了）
    2. 两版循环边界都取 0 到 100（含），这样计数才对得上示例
    3. 版本二算出来的 z 可能是负数，记得拦截
    4. 计数器必须无条件自增，统计的是"循环体进了多少次"

【提示】
    - 版本二快在少搜了一整个维度——这就是"约束消元"：变量
      能由等式直接推出来，就别再花一层循环去猜它
    - 先跑版本一，把 4 组方案抄下来，再拿版本二对答案

【吃透自检】
    1. 优化的本质是什么？z 为什么"不配"拥有自己的循环？
    2. 两个计数分别是多少？大约差几个数量级？
    3. 循环上界还能怎么收紧？100 钱最多买几只公鸡、几只母鸡？
    4. 附加挑战：按第 3 条收紧后，版本二的计数会降到多少？
       先笔算，再运行验证

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 002 题】变量、数据类型与格式化输出
 难度：★☆☆☆☆
 知识点：int / float / double / char / unsigned、变量定义与赋值、
         格式符 %d %u %f %c、精度控制 %.1f、字符的本质
=====================================================================

【题目描述】
    某传感器模块要上报一组数据。请定义"类型合适"的变量保存
    下面这些数据，再按【输出示例】的格式打印出来：

    - 采样编号：1024（整数）
    - 芯片温度：36.75 摄氏度（小数）
    - 传感器等级：字符 'A'
    - 累计计数：4294967295（32 位无符号数的最大值）

【输出示例】（等号两侧各留一个空格）
id = 1024
temp = 36.8
grade = A
grade_ascii = 65
count = 4294967295
【要求】
    1. 每个数据必须先定义变量再打印，不许把数值直接写死在 printf 里
    2. 温度保留 1 位小数输出（36.75 保留 1 位后显示 36.8）
    3. grade 打印两次：一次打印字符本身，一次打印它的 ASCII 码，
       两次用的是同一个变量
    4. count 必须用无符号类型保存，并用对应的格式符打印

【提示】
    - 保留 1 位小数：%.1f
    - 同一个 char 变量，用 %c 打印是字符，用 %d 打印是整数

【吃透自检】
    1. float 和 double 有什么区别？各占几个字节？
    2. 如果把 4294967295 存进普通 int，再用 %d 打印，
       会输出什么？动手试一试，想想为什么
    3. char 的本质是什么？'A' 和 "A" 有什么区别？
    4. 附加练习：用 sizeof 把 char、int、float、double
       在你机器上占的字节数都打印出来
    5. 如果用 %d 去打印一个 float 变量，会发生什么？试一试

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 010 题】switch 与简易计算器：一道算式，四路分发
 难度：★★☆☆☆
 知识点：switch / case / break / default、case 穿透现象、
         除零防御
=====================================================================

【题目描述】
    用 scanf("%d %c %d", &a, &op, &b) 一次读入一道算式（数、
    运算符、数之间用空格隔开，比如 17 * 5），用 switch(op)
    分发到加减乘除四个分支，按示例的格式回显算式和结果。
    两道防线：除法分支先检查除数是否为 0，是就拦截报错；
    op 不是加减乘除四个符号之一时走 default 报非法运算符。

【输入输出示例】（运行三次）
    输入 17 * 5    输出 17 * 5 = 85
    输入 9 / 0     输出 error: divide by zero
    输入 7 $ 2     输出 error: bad operator

【要求】
    1. 必须用 switch，不许改写成 if / else if 链
    2. 除零判断放在除法分支内部，先判后除
    3. 实验：故意删掉加法分支的 break，输入一道加法算式运行，
       观察输出多了什么，把现象和原因写进你代码的注释，再修好.我发现如果故意删掉break，case依旧会去判断条件，但是会把符合条件的下一个语句也给运行了？这是为什么？
    4. 输出格式与示例严格一致：运算符两侧各留一个空格

【提示】
    - "%d %c %d" 里 %c 前面那个空格是故意的，它让 scanf 跳过
      空白字符去抓运算符
    - case 标签后面只能是整型常量——字符常量恰好就是整型常量

【吃透自检】
    1. 漏写 break 会发生什么？这个现象叫什么？
    2. switch 能不能直接判断"介于 3.0 和 3.7 之间"这种区间？
       区间判断该用什么写法？（回想第 004 题）
    3. char 能不能作 switch 的表达式？为什么？
    4. 附加挑战：把 default 挪到所有 case 的前面，程序行为会
       变吗？先预测再动手验证

===================== 在下面写你的代码 =====================
*/


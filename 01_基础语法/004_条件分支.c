/*
=====================================================================
 【第 004 题】条件分支：电池电压监测报警器
 难度：★★☆☆☆
 知识点：if / else if / else、比较运算符、逻辑运算符 && ||、
         先拦截非法输入（程序健壮性）
=====================================================================

【题目描述】
    某单片机通过 ADC 读到电池电压 v（单位：伏，小数）。
    从键盘读入 v，按下面的规则输出对应状态（只输出一行）：

    v < 0 或 v > 5.0        输出  ERROR: invalid voltage
    3.7 <= v <= 5.0         输出  OK: power good
    3.0 <= v < 3.7          输出  WARN: low battery
    0 <= v < 3.0            输出  FATAL: shutting down

【输入输出示例】（运行 4 次，分别输入不同的值）
    输入 3.8    输出 OK: power good
    输入 3.2    输出 WARN: low battery
    输入 1.5    输出 FATAL: shutting down
    输入 6.0    输出 ERROR: invalid voltage

【要求】
    1. 用一条 if / else if / else 链完成，非法值放在最前面判断
    2. 区间判断用逻辑运算符组合，例如 v >= 3.0 && v < 3.7
    3. 任何输入都只输出一行

【提示】
    - 读入小数存进 float 用 scanf("%f", &v)
    - C 语言里不能写 3.0 <= v < 3.7 这种数学写法，
      它不报错，但结果是错的

【吃透自检】
    1. 3.0 <= v < 3.7 在 C 里实际是怎么算的？为什么结果不对？
    2. if (v = 5.0) 和 if (v == 5.0) 有什么天壤之别？
       前者是 C 语言最经典的坑之一
    3. else if 链是从上往下依次判断的。如果把 FATAL 那条
       挪到最前面且只写 v < 3.0，输入 -2 会输出什么？错在哪？
    4. && 和 || 的"短路求值"是什么意思？
    5. 附加挑战：不用 else，只用 4 个独立的 if 实现同样的功能
       （每个 if 的条件都要写完整），体会 else if 的意义

===================== 在下面写你的代码 =====================
*/


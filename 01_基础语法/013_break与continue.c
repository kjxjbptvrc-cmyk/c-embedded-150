/*
=====================================================================
 【第 013 题】break 与 continue：循环的两种"刹车"
 难度：★★☆☆☆
 知识点：break 与 continue 的差异、它们与循环层级的关系
=====================================================================

【题目描述】
    三个小任务，各自独立：
    任务一：求 1~100 里所有"不是 7 的倍数"的数之和，
            必须用 continue 跳过 7 的倍数。
    任务二：找 1000 以内第一个能同时被 13 和 17 整除的正整数，
            找到立刻 break 并输出。
    任务三：用 while(1) 配 break 实现：反复读入整数并累加，
            读到 0 停下，输出总和。

【输入输出示例】
    任务一输出     sum1 = 4315
    任务二输出     first = 221
    任务三输入     3 5 12 0
    任务三输出     sum3 = 20

【要求】
    1. 任务一必须出现 continue，不许改写成用 if 条件包住加法
       的绕行版本——这题练的就是它
    2. 任务二循环条件写到 1000，靠 break 提前离场
    3. 任务三循环条件必须是 while(1)，唯一出口是 break

【提示】
    - continue 只是跳过本轮剩下的语句，循环还活着；break 是
      直接散伙
    - 同时被 13 和 17 整除，等价于被两者的乘积整除（它们互素）

【吃透自检】
    1. break 一次能跳出几层循环？ 只能跳出单层
    2. continue 在 for 里跳回哪一步，更新表达式 i++ 还执行吗？ 跳到i更新后的，但是我其实一直不确定 像i++这种应该是单次循环结束后再加1还是满足循环条件的后就加1
       换成 while 且 i++ 写在循环体末尾时，continue 会酿成什么
       经典事故？ 不太懂什么意思？
    3. 双层循环里想直接跳出外层怎么办？（第 023 题会用"标志
       位"正面解决一次） 双层break吧
    4. 附加挑战：把任务一改成"跳过所有含数字 7 的数"（17、70、
       77 都算），需要借第 012 题的什么手艺？

===================== 在下面写你的代码 =====================
*/


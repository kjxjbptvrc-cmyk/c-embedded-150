/*
=====================================================================
 【第 017 题】水仙花数与完数：地毯式枚举
 难度：★★★☆☆
 知识点：枚举法、拆位复用（% 10 和 / 10）、真因子求和
=====================================================================

【题目描述】
    两趟地毯式搜索：
    任务一：找出 100~999 的全部水仙花数——各位数字的立方和
            等于自身（如 153 = 1 + 125 + 27），一行输出。
    任务二：找出 1~1000 的全部完数——真因子（不含自身的因
            子）之和等于自身，每个按因子分解式打印。
    三位数拆位是第 012 题的手艺（% 10、/ 10 两板斧），直接
    复用；立方就写 d * d * d，别去请浮点的 pow 出场。

【输出示例】
    narcissistic: 153 370 371 407
    perfect:
    6 = 1 + 2 + 3
    28 = 1 + 2 + 4 + 7 + 14
    496 = 1 + 2 + 4 + 8 + 16 + 31 + 62 + 124 + 248

==================== 本题学习笔记 ====================

【一、这题真正在练什么】
    枚举法（地毯式搜索）的标准骨架：
        外层 for 枚举候选 -> 内层算出特征值 -> 比对 -> 输出
    三个任务（水仙花数、完数、玫瑰花数）共用这一副骨架，
    区别只在"特征值"怎么算。认出骨架，题就只剩填空。

【二、两样核心手艺】
    1. 拆位三件套（第 012 题的复用）
           int t = i;            // 必须用副本
           while (t > 0) {
               int d = t % 10;   // % 10 取末位
               t /= 10;          // / 10 去末位
           }
       循环几次由数字自己的位数决定，不写死。

    2. 真因子判定
           if (n % d == 0)   ->   d 是 n 的因子
       枚举范围 d 从 1 到 n / 2（理由见第五节）。

【三、这次踩的坑（重点复习）】
    坑 1：拿循环变量本身去拆位 —— 死循环
        错法：外层 for(i...)，内层直接 i /= 10。
        后果：i=10 时被内层打回 1，外层 i++ 变成 2，程序
              在 1~10 之间无限往复，永远到不了 153。
        铁律：for 的循环变量在循环体内只当只读用，要改就
              先拷一份副本出来改。

    坑 2：累加器忘了每轮清零
        int sum = 0; 写在循环外面，只归零过一次，之后每个
        i 的结果都往同一个 sum 上叠。
        铁律：声明时的初始化，不等于每轮的重置。累加器要么
              直接声明在循环体内（最省心），要么在循环体开
              头显式 sum = 0。

    坑 3：把位数写死
        for(j=1; j<=3; j++) 暗含了"恰好三位"的假设。范围一
        旦越界（比如枚举到 1000）就会少算一位。换成
        while(t > 0) 之后，附加挑战的四位数一个字都不用改。

    坑 4：跨段复用同一个变量
        附加挑战那段又借用了最外层的 sum，能跑对纯粹是因为
        任务一收尾时它恰好被清成了 0。各段该用各自的局部
        变量，别指望上一段替你善后。

    坑 5：格式里的空格
        写成 "%d= 1"，等号前少一个空格。字符串字面量里的空
        格是肉眼最难发现的错误，核对时把自己的输出和示例上
        下贴着比。

【四、搞混的概念（本题最重要的一条）】
    ★ 因式分解，不等于真因子求和 ★
        质因数分解：12 = 2 * 2 * 3
            —— 乘法、只出现质数、同一个质因子可以重复
        真因子列表：12 的真因子是 1, 2, 3, 4, 6
            —— 加法、包含合数、唯独不含自身
        题目要的 "6 = 1 + 2 + 3" 是后者。所以根本不需要什
        么数据结构去"表示分解"，一个 n % d == 0 就判完了。

    ★ 素数不需要特判 ★
        素数 p 的真因子只有 1，求和得 1，不等于 p，枚举法
        自动就把它排除了。额外写素数判断是多余的分支。
        枚举法的价值恰恰在于"不做任何预判，一律硬算"。

【五、n / 2 这个上界是怎么来的】
    因子总是成对出现：d 能整除 n，那么 n/d 也能整除 n。
    真因子要求 d < n，于是配对的那个 n/d 至少是 2，
    两边同除，得到 d <= n / 2。
    所以 n/2 到 n 之间必然是空的，不用去扫。
    （n 为偶数时最大真因子正好是 n/2；n 为奇数时最小的质
      因子至少是 3，最大真因子至多 n/3，只会更小。）

    延伸：同一个"因子成对"的事实，第 016 题用的是它的另一
    半 —— 只要试除到 i * i <= n 就够，因为 d 和 n/d 里必有
    一个不超过根号 n。本题限定 n/2 是为了写法简单，代价是
    O(n)；配对法是 O(根号 n)。1000 以内看不出差别，n 上亿
    时就是能跑完和跑不完的区别。

【六、两趟法：把判定和输出拆开】
    矛盾：因子要一个个打印，可只有全部加完才知道该不该打。
    解法：第一趟只求和判定，通过了再原样跑第二趟专管输出。
    重复枚举一遍看着笨，但省掉了数组（041 之后才学到），而
    且把两件事彻底解耦，比硬凑成一趟好读得多。

    分隔符比元素少一个（加号问题）的两种解法：
        通用解：用标志变量，不是第一个才先打 " + "。
        本题解：n > 1 时第一个真因子必然是 1，这是白送的。
                所以先打 printf("%d = 1", n)，内层从 d = 2
                起步，之后一律 printf(" + %d", d)，一个 if
                都不用写。

【七、自检题答案】
    1. 见第五节。
    2. 153 = 1^3 + 5^3 + 3^3 = 1 + 125 + 27 = 153，对上了。
    3. 三位数的立方和最大 3 * 9^3 = 2187；四位数的四次方和
       最大 4 * 9^4 = 26244。int 上限约 21 亿，差着六个数量
       级，本题完全不用担心溢出。（对比第 007、016 题那种
       会溢出的场景，心里要有这条量级的尺子。）
    4. 玫瑰花数只需动两处：枚举范围改成 1000~9999，立方
       d*d*d 改成四次方 d*d*d*d。其余一字不改 —— 前提是拆
       位用的是 while(t > 0)，而不是写死三次。

【八、正确结果对照（下次自测用）】
    水仙花数 100~999   ：153 370 371 407
    完数     1~1000    ：6、28、496
    玫瑰花数 1000~9999 ：1634 8208 9474

======================================================

【要求】
    1. 水仙花数的拆位用 % 和 / 完成，不许用字符串
    2. 立方用乘法写，不许 #include <math.h>
    3. 完数的因子只枚举到 n / 2
    4. 分解式格式与示例严格一致：等号、加号两侧各一个空格，
       最后一个因子后面没有加号

【提示】
    - 先写好"判断某个数并按格式打印"的内核，再套外层枚举
    - 加号比因子个数少一个——想想第一个因子怎么特殊对待

【吃透自检】
    1. 真因子为什么枚举到 n / 2 就够？n / 2 与 n 之间为什么
       不可能再有真因子？因为最大的因子是由2*这个因子，超过这个因子就要变小数，不在讨论范围内
    2. 手算验证 153：三个立方各是多少，和对不对？
    3. 本题的立方和最大能到多少？离 int 上限还远吗？
    4. 附加挑战：找四位数的"玫瑰花数"（各位数字的四次方和
       等于自身），代码要动哪几处？

===================== 在下面写你的代码 =====================
*/


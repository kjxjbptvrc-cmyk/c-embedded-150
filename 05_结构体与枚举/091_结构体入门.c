/*
=====================================================================
 【第 091 题】结构体入门：把散装数据焊成一个整体
 难度：★★★☆☆
 知识点：struct 定义、成员访问、三种初始化、结构体整体赋值
=====================================================================

【题目描述】
    还记得第 060 题的成绩管理系统吗？id 一个数组、score 一个
    数组，排序要同步交换，想加个新属性，所有函数全线返工——
    那种狼狈你亲身体会过。C 语言的答案是结构体：把属于同一个
    学生的数据焊成一个整体。

    定义 struct Student { int id; char name[20]; double score; }，
    然后完成：
    1. 用三种方式各创建一个学生：
       s1 按顺序用花括号初始化；
       s2 用指定成员初始化（.id = 1002 这种写法）；
       s3 先定义，再用 . 逐个成员赋值（name 要用 strcpy）。
    2. 用统一的格式把三个学生各打印一行。
    3. 惊喜实验：执行 s2 = s1，再打印 s2——数组不能整体赋值，
       结构体居然可以，连里面的 name 数组都整个拷过去了。

【运行效果示例】
    s1: id=1001 name=WangLei score=88.5
    s2: id=1002 name=LiHua score=92.0
    s3: id=1003 name=ZhaoMin score=79.0
    执行 s2 = s1 之后:
    s2: id=1001 name=WangLei score=88.5

【要求】
    1. 三种初始化方式一种都不能少，数据用示例里的
    2. 打印学生的代码重复了三遍？先忍着，下一题（第 092 题）
       就用结构体数组加循环收拾它
    3. 整体赋值前后都要打印 s2，对比出变化

【提示】
    - 定义变量时 struct 关键字不能省：struct Student s1 = ...
    - s3.name 是数组，不能写 s3.name = "ZhaoMin"，
      要用 strcpy(s3.name, "ZhaoMin")，记得包含 string.h
    - 指定成员初始化时漏掉的成员会被自动清零

【吃透自检】
    1. 结构体解决了第 060 题的什么痛？用一句话说清
    2. s2 = s1 到底拷了什么？name 数组也拷了吗？
       （所有成员按字节整块拷贝——这是数组享受不到的待遇）
    3. name 为什么用 char 数组而不用 char 指针？
       （指针只存一个地址，那块内存归谁管？等第 127 题学了
       动态内存再考虑指针版）
    4. 把 score 改成 float，初始化和打印各要跟着改哪里？
    5. 附加挑战：加一个 char grade 成员（存 A、B、C），
       数数要改几处代码，再回想第 060 题里加属性要改多少处

===================== 在下面写你的代码 =====================
*/


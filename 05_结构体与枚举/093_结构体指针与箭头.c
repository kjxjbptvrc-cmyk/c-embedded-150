/*
=====================================================================
 【第 093 题】结构体指针与箭头：传值还是传指针
 难度：★★★★☆
 知识点：-> 运算符、(*p).m 与 p->m 等价、传结构体 vs 传指针、const
=====================================================================

【题目描述】
    沿用 struct Student { int id; char name[20]; double score; }。
    1. 定义 s 并初始化（score 给 85.0），再定义指针 p = &s。
       分别用 (*p).id 和 p->id 打印，验证两种写法完全等价——
       箭头只是语法糖，但它是你以后敲得最多的运算符之一。
    2. 三版函数做对照实验：
       void print_v(struct Student s)：传值。在函数里打印 &s；
       void print_p(const struct Student *p)：传指针，const 只读，
         在函数里打印 p；
       void raise_score(struct Student *p)：给 p->score 加 5 分，
         通过指针真正改到原结构体。
    3. main 里打印 &s，对照 print_v、print_p 打出的地址：
       传值的地址不同（整个结构体被复制了一份），
       传指针的地址相同（零拷贝）。最后验证涨分前后的 score。

【运行效果示例】（地址每次运行都可能不同，看的是关系）
    main 里 s 的地址:   000000000061FE00
    print_v 收到的地址: 000000000061FDD0   不同，发生了整体复制
    print_p 收到的地址: 000000000061FE00   相同，零拷贝
    涨分前: 85.0
    涨分后: 90.0

【要求】
    1. 三个函数的签名按题面来，一个都不许改
    2. 打印地址用 %p，实参转成 (void *) 最规范
    3. 在 print_p 里试写一句 p->score = 0，看编译器怎么拦你，
       看完删掉——这就是 const 在站岗

【提示】
    - (*p).id 的括号不能省：. 的优先级比 * 高
    - p->score += 5 一句就够

【吃透自检】
    1. p->id 的完全等价写法是什么？括号为什么不能省？
    2. 传值、传 const 指针、传普通指针，三版各适用什么场合？
    3. 嵌入式的工程规矩：结构体一律传指针，只读加 const。
       为什么只读也不用传值？（几十上百字节的结构体传值，
       每次调用都整块拷贝；这事发生在中断服务函数里等于自杀）
    4. 在 print_v 里改 s.score，main 里的原变量会变吗？为什么？
    5. 附加挑战：给结构体加一个 char note[200] 再跑一遍实验，
       想想传值的代价会随结构体膨胀涨成什么样

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 102 题】数据表思维：结构体数组就是嵌入式的数据库
 难度：★★★★☆
 知识点：静态结构体数组当数据表、遍历筛选、多关键字排序、汇总
=====================================================================

【题目描述】
    typedef struct { char name[20]; char category[10];
                     double price; int stock; } Product;

    用下面 6 条数据静态初始化商品表（名字 类别 单价 库存）：
    usb_cable   cable  9.9  120     hdmi_cable  cable 25.0  8
    stm32_board board 89.0   15     esp32_board board 32.5  3
    breadboard  board 12.0   50     iron        tool  59.0  6

    全部用"遍历 + 条件"完成，不许为筛选另建新数组：
    1. 按类别筛选：打印 board 类全部商品；
    2. 价格区间：打印 10 到 60 元（含边界）的商品名和单价；
    3. 库存告警：打印库存低于 10 的商品名和库存；
    4. 多关键字排序：先按 category 字符串序（strcmp，第 053 题），
       同类再按 price 从低到高——第 094 题三级比较的思想用两级；
    5. 汇总：统计 board 类的总库存。

    设备固件里的参数表、菜单项表、LED 闪烁模式表，全是
    "静态结构体数组 + 遍历"这一套——这就是嵌入式的数据库。

【运行效果示例】
    -- board 类 --
    stm32_board 89.0 15
    esp32_board 32.5 3
    breadboard 12.0 50
    -- 价格 10~60 --
    hdmi_cable 25.0
    esp32_board 32.5
    breadboard 12.0
    iron 59.0
    -- 库存告警 --
    hdmi_cable 8
    esp32_board 3
    iron 6
    -- 排序后 --
    board breadboard 12.0
    board esp32_board 32.5
    board stm32_board 89.0
    cable usb_cable 9.9
    cable hdmi_cable 25.0
    tool iron 59.0
    board 类总库存 = 68

【要求】
    1. 五个功能各写一个函数，商品表和条数当参数传入
    2. 类别名、价格区间、告警线全部做成函数参数，不许写死
    3. 排序的两级比较逻辑单独写清楚：先类别，后价格

【提示】
    - 静态初始化用双层花括号一次写完 6 条
    - 两级比较：strcmp 结果不为 0 就直接用它，为 0 才比 price

【吃透自检】
    1. 多关键字比较的先后逻辑怎么组织？两级顺序换了会怎样？
    2. 筛选和排序分开写的好处？（自由组合，先筛后排也行）
    3. 静态配置表对比一堆散装的全局变量，优势在哪？
    4. LED 闪烁模式表该有哪些成员？照 Product 的思路设计一个
    5. 附加挑战：加"按库存从高到低"排序，只许改比较那几行

===================== 在下面写你的代码 =====================
*/


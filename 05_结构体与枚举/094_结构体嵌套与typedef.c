/*
=====================================================================
 【第 094 题】结构体嵌套与 typedef：告别满屏 struct
 难度：★★★★☆
 知识点：嵌套结构体、链式访问、typedef 两种写法、三级比较函数
=====================================================================

【题目描述】
    第 022 题算"某天是当年第几天"时，年、月、日还是三个散装
    变量；现在让它们住进同一个屋檐下，再把日期嵌进学生档案：

    typedef struct { int y, m, d; } Date;
    typedef struct {
        char name[20];
        Date birthday;
    } Student;

    1. 定义 3 个学生并用双层花括号做嵌套初始化（数据见示例）。
    2. 用链式访问打印：s.birthday.y 这样一路点下去。
    3. 写 int date_cmp(const Date *a, const Date *b)：
       按年、月、日三级比较，a 早返回负数、相同返回 0、
       a 晚返回正数——学的就是 strcmp 的返回约定（第 053 题）。
    4. 用 date_cmp 当比较器，按生日从早到晚冒泡排序
       （第 092 题的排序骨架，只换比较那一行），打印结果。

【运行效果示例】（Wang 2004-05-20、Li 2003-11-02、
    Zhao 2004-01-15 三人）
    按生日从早到晚:
    Li 2003-11-02
    Zhao 2004-01-15
    Wang 2004-05-20

【要求】
    1. Date 和 Student 都用 typedef，后面的代码里不许再出现
       满屏的 struct 关键字
    2. date_cmp 必须三级短路：年不同直接出结果，相同才看月，
       月也相同才看日
    3. 排序里判断先后只许调 date_cmp，不许把年月日拆开重比

【提示】
    - typedef 有两种写法：
      typedef struct Date { int y, m, d; } Date;（struct 有名字）
      typedef struct { int y, m, d; } Date;（匿名 struct 直接起别名）
    - 三级比较可以连写：if (a->y != b->y) return a->y - b->y;

【吃透自检】
    1. typedef 两种写法差在哪？匿名版还能写 struct Date 吗？
    2. 如果先比日再比年，2003-12-31 和 2004-01-01 谁会被排前面？
       错在哪一步？
    3. 返回负零正这个约定好在哪？（换一个比较函数，
       排序代码一行不用改——strcmp 也是这个约定）
    4. s.birthday.y 这条链上，两个 . 各自在访问什么？
    5. 用 #define Date struct Date 好像也能少打几个字，它和
       typedef 是一回事吗？先留个疑问，第 138 题拿宏开刀时细讲
    6. 附加挑战：写 int date_valid(const Date *d) 检查日期合法性，
       月份 1~12、日子按月份上限——第 022 题的闰年判断直接搬来

===================== 在下面写你的代码 =====================
*/


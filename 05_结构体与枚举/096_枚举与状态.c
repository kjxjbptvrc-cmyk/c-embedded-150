/*
=====================================================================
 【第 096 题】枚举与状态：消灭魔法数字
 难度：★★★☆☆
 知识点：enum 定义、默认取值与手动指定、用枚举写会说话的代码
=====================================================================

【题目描述】
    先看一段"病例"（真实项目里常见的烂代码）：
        state = 2;
        if (state == 3) stop_motor();
        else if (state == 1) speed_up();
    2 是什么？3 是什么？三个月后连作者自己都不认识。
    这种数字叫魔法数字，解药是枚举。

    1. 定义 enum State { IDLE, RUNNING, PAUSED, STOPPED }，
       把病例重写成人能读懂的样子，打印 state 的值验证。
    2. 取值实验：枚举默认从 0 递增；再定义
       enum Err { ERR_NONE = 0, ERR_TIMEOUT = 10, ERR_CRC, ERR_LOST }，
       打印后三个的值，验证手动指定之后的成员接着递增（11、12）。
    3. 红绿灯循环：enum Light { RED, GREEN, YELLOW }，用 switch
       算下一个状态，RED 到 GREEN 到 YELLOW 再回 RED，连转 6 轮。
       每个灯的名字和"持续秒数"都用查表数组配——第 073 题的
       查表法，枚举值正好当下标。

【运行效果示例】（秒数配置：红 30、绿 25、黄 5）
    RED 亮 30 秒
    GREEN 亮 25 秒
    YELLOW 亮 5 秒
    RED 亮 30 秒
    GREEN 亮 25 秒
    YELLOW 亮 5 秒

【要求】
    1. 名字表 const char *name[] 和秒数表 int dur[] 都用枚举值
       做下标，switch 只负责状态转移
    2. 病例重写、取值实验、红绿灯三部分都要有输出
    3. 全程不许出现裸数字的状态比较

【提示】
    - 枚举常量本质是 int：能当数组下标、能 switch、能直接打印
    - 状态转移也可以写成 (light + 1) % 3——两种都试试，
      体会 switch 版好在哪（每条转移明明白白摆在眼前）

【吃透自检】
    1. 枚举常量的本质是什么类型？
    2. 枚举比一堆 #define 常量好在哪？（成组、有类型语义、
       调试器里直接显示名字）
    3. 把 state 强行赋成 99，编译器拦吗？（C 的枚举很宽松，
       纪律要靠人守）
    4. 正经固件里，设备状态、错误码、命令字全用枚举，
       谁写魔法数字谁挨骂——说说团队为什么要立这条规矩
    5. 附加挑战：红绿灯只是热身，第 101 题要用枚举加 switch
       搭一台真正的状态机（售货机）。先想想："状态"和"事件"
       是一回事吗？

===================== 在下面写你的代码 =====================
*/


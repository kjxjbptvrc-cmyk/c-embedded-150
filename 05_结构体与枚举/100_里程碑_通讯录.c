/*
=====================================================================
 【第 100 题】里程碑：通讯录管理系统
 难度：★★★★★
 知识点：第 100 题里程碑——结构体+数组+函数+菜单大集成
=====================================================================

【题目描述】
    走到第 100 题，你已经越过了绝大多数自学者的弃坑点。
    这一题不学新语法，把攒下的手艺全部上桌，做一个真正能用
    的通讯录——多半是你第一个破百行的程序。别被行数吓住，
    函数一个一个垒，每个都不大。

    typedef struct { char name[20]; char phone[12]; int age; } Contact;
    静态数组 Contact book[100]，加人数计数 int count。

    功能（全部函数化，main 只留菜单分发）：
    1. 添加：先用 strcmp 查重（第 053 题），重名拒绝；
    2. 按姓名查找并打印；
    3. 按姓名删除：后面的人整体前移补坑（第 059 题），count 减一；
    4. 按年龄排序：结构体整体交换（第 092 题）；
    5. 全列表打印：用 %-10s 这类宽度控制对齐成表格；
    0. 退出。菜单用 while(1) 加 switch——第 023 题的骨架。

【运行效果示例】（一次会话节选）
    1.添加 2.查找 3.删除 4.排序 5.列表 0.退出
    > 1
    姓名 电话 年龄: Wang 13800001111 30
    已添加，当前 1 人
    > 1
    姓名 电话 年龄: Wang 13900002222 31
    重名，拒绝添加
    > 5
    Wang       13800001111  30
    > 0
    再见

【要求】
    1. 每个功能一个函数：add_contact、find_by_name、
       delete_by_name、sort_by_age、list_all
    2. find_by_name 返回下标、找不到返回 -1（第 044 题的约定）；
       添加时的查重和删除时的定位都必须复用它，不许再写
       第二个查找循环
    3. 满 100 人时添加要拒绝并提示
    4. 删除后立刻列表，肉眼确认没有留坑

【提示】
    - 一个功能一个功能地加：先让"添加 + 列表"跑通，
      再上查找，最后做删除和排序
    - scanf("%19s", ...) 限制宽度，防止长名字撑爆数组

【吃透自检】
    1. 这题用到了此前哪些题的手艺？自己列清单，至少 5 项
    2. 删除时 count 和数组内容怎么保持同步？只减 count 不前移、
       只前移不减 count，各会出什么鬼？
    3. 为什么先写静态数组版，不直接上动态内存？
       （先把逻辑焊牢，再谈内存从哪来）
    4. 本题的两大遗憾：容量写死 100 个、程序一退全部失忆。
       想想它们分别要等谁来救——第 123 题的 malloc 和
       第 148 题的文件保存
    5. 附加挑战：加一个"按电话查找"，感受函数化之后加功能
       有多轻松——这正是第 060 题求而不得的东西

===================== 在下面写你的代码 =====================
*/


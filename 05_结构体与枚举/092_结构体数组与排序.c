/*
=====================================================================
 【第 092 题】结构体数组与排序：成绩管理系统的雪耻之战
 难度：★★★★☆
 知识点：结构体数组、整体交换、复用冒泡排序与线性查找
=====================================================================

【题目描述】
    用 struct Student { int id; char name[20]; double score; }
    定义数组 struct Student stu[5]，重写成绩管理系统：
    1. 循环读入 5 个学生（每行：id 姓名 成绩）。
    2. 按 score 从高到低冒泡排序（第 045 题的算法原样搬来）。
       交换时 tmp = stu[j]; 一句换走整个学生——对比第 060 题
       多数组同步交换的狼狈样，这就是结构体的第一个爽点。
    3. 打印排序结果。
    4. 再读入一个 id，线性查找（第 044 题：找到返回下标，
       找不到返回 -1），打印该学生的全部信息。

【输入示例】
    101 Wang 78.5
    102 Li 92
    103 Zhao 85.5
    104 Qian 66
    105 Sun 91
    103

【输出示例】
    按成绩从高到低:
    102 Li 92.0
    105 Sun 91.0
    103 Zhao 85.5
    101 Wang 78.5
    104 Qian 66.0
    id=103: name=Zhao score=85.5

【要求】
    1. 排序和查找各写成独立函数，数组和人数当参数传进去
    2. 交换必须是结构体整体交换，不许一个成员一个成员地搬
    3. 查找不到时打印 not found（示例没展示，自己测一次）

【提示】
    - 读一行：scanf("%d %s %lf", &stu[i].id, stu[i].name,
      &stu[i].score)——name 前面为什么没有 & ，想想它是什么
    - 函数参数写成 struct Student a[] 加 int n 即可

【吃透自检】
    1. 结构体整体交换和第 060 题的多数组同步交换，
       对比一下代码量和出错概率
    2. 结构体数组在内存里怎么排？相邻元素地址差多少？（连续
       排列，每格 sizeof(struct Student)——打印地址验证一下）
    3. "排序比较的是成员，交换的是整体"——把这句话展开讲讲
    4. scanf 读 name 时为什么不写 & ？
    5. 附加挑战：改成按 name 字典序排序（strcmp，第 053 题），
       要动几行？这套"录入、排序、查找"的骨架，第 100 题
       通讯录还要再用一遍

===================== 在下面写你的代码 =====================
*/


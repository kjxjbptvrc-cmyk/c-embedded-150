/*
=====================================================================
 【第 099 题】结构体图形库：Point 和 Rect 的几何小工具箱
 难度：★★★★☆
 知识点：结构体作参数与返回值、组合建模、反向思维判相交
=====================================================================

【题目描述】
    给嵌入式 LCD 界面写一组几何工具（LCD 坐标：x 向右、y 向下）：
    typedef struct { double x, y; } Point;
    typedef struct { Point lt, rb; } Rect;   lt 左上角、rb 右下角
    ——结构体套结构体，第 094 题嵌套的又一次上场。

    实现四个函数：
    1. Point make_point(double x, double y)——结构体当返回值；
    2. double rect_area(const Rect *r)——面积；
    3. int rect_contains(const Rect *r, Point p)——点在矩形内
       （压线算在内）返回 1，否则返回 0；
    4. int rect_intersect(const Rect *a, const Rect *b)——
       有重叠返回 1。判相交要"反着想"：先列出四种不相交
       （a 完全在 b 的左侧、右侧、上方、下方），
       四条全不成立，那就是相交。

    main 用这组数据验证：
    A = { {0,0}, {10,8} }    B = { {6,4}, {14,12} }
    C = { {11,9}, {15,13} }  P = make_point(3, 5)

【运行效果示例】
    A 面积 = 80.0
    P 在 A 内? 1
    P 在 B 内? 0
    A 与 B 相交? 1
    A 与 C 相交? 0

【要求】
    1. 函数签名照抄：Rect 一律传 const 指针，小 Point 传值
    2. rect_intersect 先把四种不相交写成四个条件再取反，
       不许一上来硬凑"重叠"的条件
    3. 用示例数据把五行输出全部验证一遍

【提示】
    - "a 完全在 b 左侧"翻译成坐标就是 a->rb.x < b->lt.x，
      剩下三条照着类推
    - 面积 = (rb.x - lt.x) * (rb.y - lt.y)

【吃透自检】
    1. 结构体当返回值也是拷贝吗？（是——整个 Point 拷给接收方）
    2. 判相交为什么反着想简单？（不相交的四种情况彼此独立、
       一条一个比较；正着列重叠的花样，漏一种就是 bug）
    3. 为什么大家伙 Rect 用 const 指针、小 Point 传值也行？
       （尺寸权衡——第 093 题嵌入式传参规矩的活学活用）
    4. rect_contains 把"正好压线"算在内，这个约定不写清楚，
       调用的人会踩什么坑？
    5. 附加挑战：写 double rect_overlap_area(const Rect *a,
       const Rect *b)，返回相交面积，不相交返回 0

===================== 在下面写你的代码 =====================
*/


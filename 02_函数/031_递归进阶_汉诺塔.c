/*
=====================================================================
 【第 031 题】递归进阶：汉诺塔与慢得离谱的斐波那契
 难度：★★★★☆
 知识点：递归拆解思想、递归的代价（重复计算、栈开销）
=====================================================================

【题目描述】
    任务一：汉诺塔。n 个盘从 A 柱借助 B 柱全部搬到 C 柱，
    一次只能搬一个、大盘不许压小盘。读入 n，打印每一步，
    并统计总步数，验证它等于 2 的 n 次方减 1。
    递归想法：把上面 n - 1 个盘看成一个整体先搬去 B，
    最大盘从 A 直达 C，再把那个整体从 B 搬来 C。
    任务二：递归版 int fib(int n)，出口 fib(1) = fib(2) = 1。
    实测 fib(40)，亲身感受"等一会儿才出结果"；回想第 019 题
    循环版的瞬间出活，这个落差就是本题要教的东西。

【输入输出示例】
    输入    3
    输出    1: A -> C
            2: A -> B
            3: C -> B
            4: A -> C
            5: B -> A
            6: B -> C
            7: A -> C
            total = 7
            fib(40) = 102334155   （明显卡顿一下，属正常）

【要求】
    1. void hanoi(int n, char from, char to, char via) 递归实现
    2. 总步数用全局计数器或返回值统计都行，对 n = 3、4、10
       验证等于 2 的 n 次方减 1（n = 10 应为 1023）
    3. 在你代码的注释里解释 fib 慢在哪：fib(40) 拆出 fib(39)
       和 fib(38)，fib(39) 又拆出 fib(38)——于是 fib(38) 被算了
       两遍、fib(37) 被算了三遍……重复量滚雪球

【提示】
    - 汉诺塔想不动就先背口诀再消化：n - 1 个借 C 搬去 B，
      最大盘直奔 C，n - 1 个再借 A 搬去 C
    - 先用 n = 3 对着示例逐行核对，对上了再放大 n

【吃透自检】
    1. "把 n - 1 个盘当一个整体"这句话，你能对着代码展开讲吗？
    2. 画出 fib(5) 的调用树，数数 fib(2) 被算了几次（应是 3 次）
    3. 什么时候递归不可原谅？嵌入式的栈又小又要实时，深度
       不可控的递归就是雷——第 122 题见真章
    4. 附加挑战：给 fib 加个全局计数器，数数 fib(30) 一共发生
       多少次函数调用，被那个数字吓一跳

===================== 在下面写你的代码 =====================
*/


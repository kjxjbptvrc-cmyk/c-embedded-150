/*
=====================================================================
 【第 032 题】作用域与全局变量：变量的地盘规则
 难度：★★☆☆☆
 知识点：作用域、同名遮蔽（就近原则）、块作用域、全局变量利弊
=====================================================================

【题目描述】
    定义全局变量 int g = 100; 然后做三个实验：
    实验一：main 里再定义同名的 int g = 1; 打印 g；另写
            void show_g(void)（内部没有局部 g）也打印 g，
            两处输出对照，验证"就近原则"
    实验二：main 里写一对单独的花括号，里面定义 int t = 42;
            出了花括号再打印 t——编译直接报错。把报错原文
            抄进注释，再删掉那行打印让编译通过
    实验三：写 void beep(void)：每次被调用就把全局计数器
            beep_count 加 1 并打印 BEEP，main 连调 3 次后打印总数
    全局变量的利：跨函数共享状态——嵌入式里中断服务函数和
    主循环交换数据就靠它（第 130 题的 volatile 专为此而生）。
    弊：谁都能改它，出 bug 时全工程都是嫌疑人。所以要节制。

【输出示例】
    main 里的 g = 1
    show_g 看到的 g = 100
    BEEP
    BEEP
    BEEP
    beep_count = 3

【要求】
    1. 三个实验都要做，实验二的报错原文必须留在注释里
    2. beep_count 必须是全局变量，全程序只此一个计数器
    3. 实验一的两个 g 一个都不许改名，就是要它们同名

【提示】
    - 同名遮蔽：块内的同名变量会把外面那个挡住
    - 变量的命是花括号给的，出了定义它的那对花括号就没了

【吃透自检】
    1. 就近原则是什么？实验一里 main 打印 1、show_g 打印 100，
       各自的道理是什么？
    2. 全局变量不初始化时值是多少？局部变量呢？这个差别
       非常重要，说出个所以然
    3. 为什么要节制使用全局变量？"谁都能改"给排查 bug
       带来什么麻烦？
    4. 附加挑战：把 beep_count 挪进 main 变成局部变量，还想
       让 beep() 加到它——试试就知道办不到，出路在第 065 题

===================== 在下面写你的代码 =====================
*/


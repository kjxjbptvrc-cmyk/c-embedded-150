/*
=====================================================================
 【第 027 题】参数与返回值：main 只管收发，计算全归函数
 难度：★★☆☆☆
 知识点：返回值、函数复用、整数除法的坑
=====================================================================

【题目描述】
    写四个函数，让 main 瘦成"只管输入输出"：
    1. int max2(int a, int b)           返回两者较大值
    2. int max3(int a, int b, int c)    内部必须复用 max2
    3. int is_even(int n)               偶数返回 1，奇数返回 0
    4. double c2f(double c)             摄氏转华氏，f = c × 9 / 5 + 32
    main：读入三个整数 a b c 和一个小数 t，依次打印四个结果。

【输入输出示例】（c2f 用 %.1f 保留一位小数）
    输入    3 7 5 37.5
    输出    max2(3,7) = 7
            max3(3,7,5) = 7
            is_even(3) = 0
            c2f(37.5) = 99.5

【要求】
    1. 所有计算只许发生在函数里，main 除了 scanf 和 printf
       不许出现任何比较或算术
    2. max3 不许重写比较逻辑，只许调用 max2 拼出来
    3. c2f 写完先自测两个铁案：c=0 应得 32.0，c=100 应得 212.0
    4. 每个函数必须有 return，返回类型和声明一致

【提示】
    - max3(a, b, c) 可以想成"a、b 先决出胜者，再和 c 比"
    - 整数世界里 9 / 5 等于 1。c2f 里怎么写才能得到 1.8 的
      效果？这是本题埋的坑，掉进去再爬出来印象最深

【吃透自检】
    1. return 执行的瞬间发生了什么？return 之后的语句还会执行吗？
    2. 声明了 int 返回类型却忘写 return，编译器往往只给警告，
       调用处会拿到什么值？为什么这很危险？
    3. max3 复用 max2 好在哪？假如比较规则要改（比如相等时
       打印警告），复用版和重写版各要改几处？
    4. 附加挑战：只靠 max2，一行表达式求出四个数的最大值

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 037 题】数学库与自制函数：会用轮子，也会造轮子
 难度：★★★☆☆
 知识点：math.h（sqrt pow fabs ceil floor）、浮点判等惯例
=====================================================================

【题目描述】
    任务一：包含 math.h，用 sqrt、pow、fabs、ceil、floor
    各算一例并打印，printf 一律用 %f。
    任务二：造轮子。手写 int my_abs(int x) 和
    double my_pow(double base, int n)（n 为非负整数，循环
    累乘），不许调用任何库函数。
    任务三：读入 a b c，解一元二次方程。判别式
    d = b × b - 4 × a × c，按 d 大于零、约等于零、小于零
    分三支输出两根、重根、无实根（求根复用 sqrt）。
    本题埋两个必踩的坑：
    坑一：pow 返回 double，拿 %d 去打印会打出鬼数字——
          亲手试一次，把鬼数字留在你代码的注释里
    坑二：浮点数判等不能写 ==，要写 fabs(x - y) < 1e-6

【输入输出示例】
    sqrt(2.0)=1.414214   pow(2.0,10)=1024.000000
    fabs(-3.5)=3.500000  ceil(3.2)=4.000000  floor(3.8)=3.000000
    my_abs(-7)=7   my_pow(3,4)=81.000000
    输入    1 -3 2
    输出    x1=2.000000 x2=1.000000
    （输入 1 2 1 输出 x=-1.000000；输入 1 0 1 输出 no real root）

【要求】
    1. 判"重根"必须用 fabs(d) < 1e-6，禁止写 d == 0
    2. my_pow 只许循环累乘；my_pow(x, 0) 返回 1
    3. 坑一的实验必须做，鬼数字抄进注释
    4. 若链接报 undefined reference to sqrt，编译命令加 -lm
       （Linux 常见，Windows 的 gcc 一般不用）

【提示】
    - 两根公式里的 sqrt(d) 只算一次，存进变量再用两遍
    - 1e-6 就是 0.000001，"容差"：差距小到这个程度就当相等

【吃透自检】
    1. 为什么浮点不能直接用 == 判等？0.1 加 0.2 为什么不是
       严格等于 0.3？
    2. my_pow 的循环不变量是什么？第 i 圈结束时 result 恰好
       等于 base 的几次方？用它说服自己代码没错
    3. 判别式三分支的边界怎么划？d 算出 0.0000001 这种
       "几乎为零"该归哪支？
    4. 附加挑战：my_pow(2, 31) 的结果拿 int 存会怎样？double 呢？

===================== 在下面写你的代码 =====================
*/


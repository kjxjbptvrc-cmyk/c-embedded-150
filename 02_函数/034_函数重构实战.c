/*
=====================================================================
 【第 034 题】函数重构实战：把 022 的一坨 main 拆成三层
 难度：★★★☆☆
 知识点：函数拆分、单一职责、分层复用
=====================================================================

【题目描述】
    第 022 题"输入年月日算第几天"你是在 main 里一口气写完的。
    现在重构成三层，上层只许调用下层：
    1. int is_leap_year(int y)              闰年返回 1
       （能被 4 整除且不被 100 整除，或能被 400 整除）
    2. int days_in_month(int y, int m)      该月有几天
    3. int day_of_year(int y, int m, int d) 该日期是当年第几天
    main 读入 y m d、调用、打印，十行以内收工。
    重构后的福利：is_leap_year 可以单独批量测试了——
    用 1900、2000、2024、2023 四个用例一次验完。

【输入输出示例】（程序先打印闰年自测，再读日期）
    输出    1900:0  2000:1  2024:1  2023:0
    输入    2024 3 5
    输出    day 65
    再运行一次：输入 2023 3 5，输出 day 64

【要求】
    1. 严格分层：day_of_year 只调 days_in_month，days_in_month
       只调 is_leap_year，闰年逻辑全程序只许出现一次
    2. main 十行以内：读入、调用、打印，别的一概不干
    3. 闰年自测的四个用例输出必须与示例一致——1900 这个
       "整百年陷阱"最能暴露闰年规则写没写全
    4. 用 3 月 5 日在闰年和平年各验一次——65 和 64，
       差的那一天正是 2 月 29 日

【提示】
    - days_in_month 里 2 月可以写成 28 + is_leap_year(y)，
      一行收工
    - 翻出你 022 的老代码对照，感受"好改"两个字值多少钱

【吃透自检】
    1. "一个函数只干一件事"：这三个函数分别只干哪件事？
    2. 三层里哪一层最容易被别的程序原样拿走复用？为什么？
    3. 假如 2 月的天数规则变了，三层版改哪里？022 的一坨版
       要在几个地方动刀？
    4. 附加挑战：加一个 int date_is_valid(int y, int m, int d)，
       把 2023 年 2 月 29 日这类非法日期挡在门外

===================== 在下面写你的代码 =====================
*/


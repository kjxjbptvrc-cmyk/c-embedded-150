/*
=====================================================================
 【第 028 题】值传递的真相：一场注定失败的交换
 难度：★★★☆☆
 知识点：值传递（参数是复制品）、%p 打印地址、取地址符 &
=====================================================================

【题目描述】
    这是一个"做不成"的实验，也是整套题最重要的伏笔之一。
    写 void swap(int a, int b)：用临时变量交换 a 和 b，
    交换完在函数内部打印 a、b 的值和它们的地址。
    main 里定义 x = 3、y = 5：调用 swap(x, y) 之前和之后
    各打印一次 x、y 的值，并打印 x、y 的地址。
    剧透：main 里的 x、y 纹丝不动。这不是你写错了——C 函数
    的参数一律是实参的复制品，函数里改的只是副本。本题任务
    就是把这个现象亲眼看清楚、刻进脑子。

【输出示例】（地址每台机器每次运行都可能不同）
    main 调用前: x=3 y=5   &x=0061FE1C &y=0061FE18
    swap 内部  : a=5 b=3   &a=0061FDF0 &b=0061FDF8
    main 调用后: x=3 y=5   没换！

【要求】
    1. swap 内部必须真的完成交换并打印，证明"函数里确实换了"
    2. 地址用 printf 的 %p 配合取地址符 & 打印，函数内、
       main 里都要打
    3. 对照输出，在你代码的注释里回答：形参 a 和实参 x 的
       地址一样吗？这说明了什么？

【提示】
    - &x 读作"x 的地址"，%p 是专门打印地址的格式符
    - 想让函数真正改到外面的变量，正解在第 065 题（指针）
      揭晓，现在先把"改不到"的现象记牢

【吃透自检】
    1. 为什么 swap 里换成功了，main 里却没换？用"复制品"
       三个字展开讲一遍
    2. 形参 a、b 存在哪里？函数返回后它们的下场是什么？
    3. scanf("%d", &x) 为什么必须写 &？先按"想让别人改你的
       变量，就得把地址交出去"理解，第 065 题彻底讲清
    4. 附加挑战：不许改外部变量，那用 return 把交换结果带
       回来行不行？一个 return 最多带回几个值？这个局怎么破？

===================== 在下面写你的代码 =====================
*/


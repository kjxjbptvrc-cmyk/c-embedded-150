/*
=====================================================================
 【第 039 题】模块化猜拳：兑现第 025 题埋的伏笔
 难度：★★★☆☆
 知识点：模块化重构、职责划分、返回值约定
=====================================================================

【题目描述】
    第 025 题的人机猜拳三局两胜，你的 main 又长又乱——当时
    就说好等着重构。今天兑现：功能一模一样，结构脱胎换骨。
    1. int get_user_move(void)       读用户出拳，内部用 do-while
       死磕非法输入，直到拿到合法值才返回
    2. int get_computer_move(void)   用 rand() % 3 随机出拳
    3. int judge(int u, int c)       只判胜负：用户胜返回 1、
       平局返回 0、用户负返回 -1，一个字都不打印
    4. void print_result(...)        只负责打印，不算胜负
    main 缩成十几行的"剧本"：循环打局、计分、谁先赢两局收场。

【运行效果示例】（出拳约定 0 石头 1 剪刀 2 布）
    请出拳(0石头 1剪刀 2布): 7
    非法输入，重来: 0
    电脑出: 剪刀
    本局你赢    比分 1:0
    ……（若干局之后）……
    最终 2:1，你获胜

【要求】
    1. 判胜负逻辑全程序只许存在于 judge 一处，main 里不许
       再比一次拳
    2. get_user_move 返回的值必须保证合法，调用方零检查
    3. judge 的返回值约定（1、0、-1 各代表什么）必须写在
       函数头的注释里
    4. 验收实验：把规则改成五局三胜，数数你动了几处代码——
       合格答案是只动 main 里的一处

【提示】
    - 随机数种子怎么播（srand），翻你 025 的老代码
    - 把 025 的旧 main 和这次的新 main 摆在一起看，差距就是
      这三十几题练出来的东西

【吃透自检】
    1. 模块化前后，"改需求"的成本差在哪？五局三胜的实验
       说明了什么？
    2. judge 的返回值约定为什么必须写注释？队友把 1 理解成
       "电脑胜"会发生什么？
    3. 四个函数是否都做到"不多不少只干一件事"？哪个函数
       换个游戏还能原样复用？
    4. 附加挑战：新需求"连胜两局起播报"，该改哪个函数还是
       只改 main？改完检验你的分层是不是真的干净

===================== 在下面写你的代码 =====================
*/


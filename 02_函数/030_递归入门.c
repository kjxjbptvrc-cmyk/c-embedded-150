/*
=====================================================================
 【第 030 题】递归入门：函数自己调用自己
 难度：★★★☆☆
 知识点：递归两要素（终止出口、规模缩小）、递归与循环对照
=====================================================================

【题目描述】
    递归 = 函数在自己体内调用自己。它能跑起来只靠两条：
    有出口（某个足够小的 n 直接返回，不再往下调）、
    规模缩小（每次调用的参数离出口更近一步）。
    写四个函数互相对照：
    1. int fact_r(int n)    递归版阶乘
    2. int fact_i(int n)    循环版阶乘
    3. int sum_r(int n)     递归版 1 加到 n
    4. int sum_i(int n)     循环版 1 加到 n
    main 里用 n = 4、5、10 验证两版阶乘一致，用 n = 100
    验证两版求和一致。

【输出示例】
    fact_r(4)=24   fact_i(4)=24
    fact_r(5)=120   fact_i(5)=120
    fact_r(10)=3628800   fact_i(10)=3628800
    sum_r(100)=5050   sum_i(100)=5050

【要求】
    1. 递归版必须能一眼指出两要素：出口写在函数最前面，
       递归调用的参数是 n - 1
    2. 在你代码的注释里手画 fact_r(4) 的展开与回收全过程：
       fact_r(4) 等着 fact_r(3)，fact_r(3) 等着 fact_r(2)……
       到底之后再一层层把值乘着带回来
    3. 阶乘别测超过 12 的数——第 005 题你已经领教过 13!
       会撑爆 int

【提示】
    - 出口长这样：if (n <= 1) return 1;
    - 递归句长这样：return n * fact_r(n - 1);

【吃透自检】
    1. 递归两要素是什么？各自缺了会发生什么？
    2. 漏写出口的真实死法叫"栈溢出"——现象会在第 122 题
       亲手引爆，先记住这个词
    3. 递归和循环怎么选？本题这两个任务你会选哪个？为什么？
    4. 附加挑战：写递归版 digit_sum(n) 求各位数字之和，
       它的出口和缩小方式分别是什么？

===================== 在下面写你的代码 =====================
*/


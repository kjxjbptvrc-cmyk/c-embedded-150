/*
=====================================================================
 【第 033 题】static 局部变量：函数私有的记忆
 难度：★★★☆☆
 知识点：static 局部变量——只初始化一次、跨调用存活
=====================================================================

【题目描述】
    普通局部变量函数一返回就灰飞烟灭，加上 static 就不一样了：
    1. 写 int counter(void)：函数内部写 static int n = 0;
       每次调用先把 n 加 1 再返回 n。main 用循环连调 5 次，
       把返回值打印在一行
    2. 对比实验：把 static 删掉重新编译运行，把输出差异记进
       注释，然后再加回去
    3. 应用：写发号器 int get_next_id(void)，从 1001 开始每调
       一次发一个新编号，main 连发 3 个各打印一行
    嵌入式里这招极常用：static 局部变量是函数私有的持久状态，
    别的函数想碰都碰不到，比全局变量收敛得多——按键去抖计数、
    只执行一次的硬件初始化，都是它的地盘。

【输出示例】
    1 2 3 4 5
    新设备编号 1001
    新设备编号 1002
    新设备编号 1003
    （删掉 static 再跑，第一行会变成 1 1 1 1 1——想想为什么）

【要求】
    1. counter 的 5 次调用用循环完成，输出一行、空格分隔
    2. 去掉 static 的对比实验必须做，差异写成注释
    3. get_next_id 不许用全局变量，编号状态必须锁在函数内部

【提示】
    - static int n = 0; 这行的初始化整个程序只发生一次，之后
      每次进函数，n 都还是上次离开时的值
    - 发号器和 counter 是同一个套路，换个起点罢了

【吃透自检】
    1. static int n = 0; 里的赋 0 执行了几次？普通局部变量的
       初始化又执行几次？
    2. static 局部变量存在内存哪个区？先猜一猜，第 121 题揭晓
    3. 它和全局变量比：谁的作用域小？生命周期谁长谁短？
    4. 附加挑战：两处代码需要两套互不干扰的计数，一个 counter
       还够用吗？该怎么办？

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 029 题】函数与素数工具：is_prime 写一次，用两处
 难度：★★★☆☆
 知识点：工具函数复用、边界集中处理、试除法
=====================================================================

【题目描述】
    把第 016 题的素数判断包装成工具函数：
        int is_prime(int n)    是素数返回 1，否则返回 0
    边界必须在函数内部一次处理干净：n <= 1 不是素数，2 是素数。
    然后用它连干两件事：
    任务一：读入 a 和 b，打印区间 [a, b] 内的所有素数
    任务二：验证哥德巴赫猜想——对 4 到 100 的每个偶数，
            输出一组"素数 + 素数"的拆法

【输入输出示例】
    输入    10 30
    输出    11 13 17 19 23 29
            4 = 2 + 2
            6 = 3 + 3
            8 = 3 + 5
            10 = 3 + 7
            ……（中间省略）……
            100 = 3 + 97

【要求】
    1. 素数判断逻辑全程序只许存在一份，两个任务共用 is_prime
    2. 试除法上限沿用 016 的手艺：i * i <= n
    3. 调用处不许再补 n 是不是 1 或 2 的判断，边界是 is_prime
       自己的责任
    4. 哥德巴赫部分：每个偶数只输出找到的第一组拆法

【提示】
    - 拆法枚举：p 从 2 走到 n / 2，检查 p 和 n - p 是否都是素数
    - 主逻辑写完数数各有几行，工具函数的价值就体现在这

【吃透自检】
    1. 边界集中在函数内处理，比散落在每个调用处好在哪？
       哪天边界规则变了，两种写法各要改几处？
    2. 不看代码，口述哥德巴赫验证的伪代码：外层循环遍历什么？
       内层在找什么？找到后干什么？
    3. is_prime 写一次用两处，说说"一次编写、到处复用"的体感
    4. 附加挑战：任务一结束后追加一行，输出该区间素数的总个数

===================== 在下面写你的代码 =====================
*/


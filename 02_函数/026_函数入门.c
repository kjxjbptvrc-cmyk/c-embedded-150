/*
=====================================================================
 【第 026 题】函数入门：把启动横幅装进函数里
 难度：★★☆☆☆
 知识点：函数的定义 / 声明 / 调用、函数原型
=====================================================================

【题目描述】
    第 001 题的设备启动横幅是直接堆在 main 里打印的，这次把它
    包装成随叫随到的函数：
    1. 写无参数、无返回值的函数 print_banner()，调用一次就打印
       下面三行（一字不差）：
       === MCU Boot v1.0 ===
       CPU: STM32F103
       Freq: 72MHz
    2. 写函数 int add(int a, int b)，返回两数之和
    3. main 里先调 print_banner()，再打印 add(3, 5) 的结果
    实验环节（本题重点）：把两个函数的定义都放在 main 之后，并且
    先不写原型声明，编译一次，把编译器的报错或警告原样抄进你的
    注释里；然后在 main 之前补上两行原型，再编译到通过。

【输出示例】
    === MCU Boot v1.0 ===
    CPU: STM32F103
    Freq: 72MHz
    add(3, 5) = 8

【要求】
    1. 两个函数的定义必须放在 main 之后（本题故意如此）
    2. 先不写原型直接编译，把报错 / 警告抄进注释，再加原型
    3. add 的结果必须通过返回值拿回，不许在 add 里直接 printf
    4. 横幅三行由 print_banner 一手包办，main 里不许再打印横幅

【提示】
    - 原型长这样：void print_banner(void); 注意结尾有分号
    - 没声明就调用，老编译器可能按"隐式声明"硬编过去还只给警告，
      别心存侥幸

【吃透自检】
    1. 函数的定义、声明、调用分别指什么？各发生在你代码的哪一行？
    2. add(3, 5) 里的 3、5 叫实参，a、b 叫形参，两者是什么关系？
    3. 原型的作用是什么？为什么把定义挪回 main 之前就可以不写原型？
    4. 附加挑战：把调用写成 print_banner;（丢了括号），能编译吗？
       会打印吗？动手试，把结论记下来

===================== 在下面写你的代码 =====================
*/


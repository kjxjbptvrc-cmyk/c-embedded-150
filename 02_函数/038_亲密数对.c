/*
=====================================================================
 【第 038 题】亲密数对：一个工具函数撑起一道古典名题
 难度：★★★☆☆
 知识点：函数 + 枚举综合、去重技巧、因子配对优化
=====================================================================

【题目描述】
    真因子 = 除自己以外的所有因子。若 a 的真因子之和恰好
    等于 b，b 的真因子之和又恰好等于 a，且 a ≠ b，则称 a、b
    为一对亲密数（相亲数），最有名的是 220 和 284。
    写 int sum_of_proper_divisors(int n) 返回 n 的真因子之和，
    然后找出 3000 以内的所有亲密数对，每对只输出一次。

【输出示例】
    220 284
    1184 1210
    2620 2924

【要求】
    1. 因子求和逻辑只许存在于 sum_of_proper_divisors 一处
    2. 去重技巧：设 b = sum_of_proper_divisors(a)，只在 a < b
       时才去验证 sum_of_proper_divisors(b) 是否等于 a，
       想清楚这一招为什么能保证每对只报一次
    3. n = 1 的真因子之和按 0 处理
    4. 附加（选做）：因子枚举优化到 i * i <= n，i 和 n / i
       配对累加，小心 i * i == n 时别把同一个因子加两次

【提示】
    - a < b 顺手排除了 a == b 的情况——那是完全数（比如 6
      和 28，自己的真因子和等于自己），不算亲密数对
    - 先拿 220 单测你的函数：真因子和应该正好是 284

【吃透自检】
    1. 只输出 a < b 的道理是什么？不加这条，输出会多出什么？
    2. 手动验证一遍：列出 220 的全部真因子加出 284，
       再列出 284 的真因子（1、2、4、71、142）加回 220
    3. 有了工具函数，主循环还剩几行？口头说出每行在干什么
    4. 附加挑战：把上限从 3000 提到 100000，因子枚举优化前后
       的等待感受差多少？（第 016 题 i * i <= n 的思想搬过来）

===================== 在下面写你的代码 =====================
*/


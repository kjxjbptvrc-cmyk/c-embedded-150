/*
=====================================================================
 【第 036 题】递归与数字：打印语句放哪，顺序天翻地覆
 难度：★★★★☆
 知识点：递归调用栈的展开顺序（先进后出）
=====================================================================

【题目描述】
    核心魔法实验。写两个递归函数打印 n 的各位数字，函数体都
    只有"一句打印 + 一次递归调用"，唯一区别是先后顺序：
    版本 A：先打印 n % 10，再对 n / 10 递归
    版本 B：先对 n / 10 递归，再打印 n % 10
    一个逆序、一个正序！亲手跑出来，再解释为什么。
    趁热打铁：写 void print_bin(int n) 递归打印 n 的二进制。
    第 012 题拆位时你见过这个难题：除 2 取余，余数从低位往
    高位出，直接打印就是反的——而递归的调用栈天然把顺序倒
    了回来，等于免费解决。（第 110 题会用移位法再实现一次
    二进制打印，到时候两法对照。）

【输入输出示例】
    输入    1234
    输出    版本A: 4 3 2 1
            版本B: 1 2 3 4
            二进制: 10011010010
    再运行一次：输入 132，输出 2 3 1、1 3 2、10000100

【要求】
    1. 两个版本的函数体除了语句顺序外一字不差
    2. 在你代码的注释里解释顺序为什么反了，关键词：调用栈、
       先进后出、哪一层先开口打印
    3. print_bin 用 132 验证，输出必须是 10000100
    4. 三个函数都不许用数组、不许先算位数，纯递归解决

【提示】
    - 每层递归只负责一位：n % 10（或 n % 2），剩下的交给
      n / 10（或 n / 2）
    - "打印放在递归之后"的意思是：先等更高位的那些层打完，
      才轮到自己开口

【吃透自检】
    1. 打印语句的位置为什么决定输出顺序？用"调用栈先进后出"
       完整讲一遍版本 B 处理 1234 的过程
    2. print_bin 为什么天然正序？递归到最深的那层打印的是
       最高位还是最低位？
    3. 口算 132 的二进制：132 = 128 + 4，和程序输出对一对
    4. 附加挑战：先猜猜第 110 题的"移位法"可能长什么样，
       把猜想写在注释里，到时候对答案

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 035 题】数字函数三件套：接口是约定，不只是代码
 难度：★★★☆☆
 知识点：函数接口设计、行为约定、复用链
=====================================================================

【题目描述】
    把第 012 题练过的拆位手艺（% 10 取个位、/ 10 砍一位）
    升级成三个正经函数：
    1. int count_digits(int n)         n 是几位数
    2. int reverse_num(int n)          数字反转，12034 变 43021
    3. int is_palindrome_num(int n)    是回文数返回 1；必须
       复用 reverse_num，一行就能写完
    设计决策题：负数怎么办？由你拍板——比如"负数一律按非
    回文处理返回 0""count_digits 先取绝对值"都行，但必须把
    约定用注释写在每个函数头上。接口的行为约定和代码本身
    一样重要：调用你函数的人不读函数体，只读你的注释。
    main：读入一个整数，打印三个函数的结果。

【输入输出示例】
    输入    12321
    输出    count_digits = 5
            reverse_num = 12321
            is_palindrome_num = 1
    再运行一次：输入 120，输出 3、21、0

【要求】
    1. is_palindrome_num 里不许再写拆位循环，只许调 reverse_num
    2. n = 0 时三个函数各返回什么？自己定义、写进注释、亲手测
    3. 负数的约定同样要写进注释，并至少测试一次

【提示】
    - reverse_num(120) 等于 21 而不是 021，想想这为什么反而
      让回文判断顺理成章
    - 约定没有标准答案，能自圆其说并写清楚就是好约定

【吃透自检】
    1. 三个函数的复用链是什么？谁调用了谁？
    2. 你给 n = 0 定的三个返回值分别是什么？能自圆其说吗？
    3. "约定写进注释"为什么和代码同等重要？想想半年后的你
       或者队友拿到这个函数时会怎么用
    4. 附加挑战：reverse_num(1000000003) 会出什么事？
       第 005 题的老朋友又来了

===================== 在下面写你的代码 =====================
*/


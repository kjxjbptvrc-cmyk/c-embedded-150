/*
=====================================================================
 【第 040 题】阶段综合：控制台"示波器"，把 sin 画出来
 难度：★★★★☆
 知识点：函数 + 循环 + math.h 综合、数值映射（偏移与缩放）
=====================================================================

【题目描述】
    不借助任何图形库，在控制台画出正弦波：
    对 x 从 0 到 6.28、步进 0.2，算 y = sin(x)，每行先打印
    (int)((y + 1) * 30) 个空格、再打印一个星号。把屏幕横过来
    看，就是一条正弦波形。
    结构要求：算 y 包装成 double f(double x)，画一行包装成
    void draw_row(double y)，main 里只剩一个循环。
    嵌入式点：往串口逐行打印波形，是调试传感器数据的土办法，
    真有工程师这么干——手头没有示波器时它能救命。
    伏笔：现在换一条曲线还得改代码重新编译；等第 080 题学了
    函数指针，曲线就能当参数传进来，一套代码画所有曲线。

【运行效果】（共 32 行，星号位置随 sin 摆动，节选关键行）
    第 1 行   星号在第 31 列附近（x=0，y=0，居中）
    第 9 行   星号在第 60 列附近（x=1.6，y 接近 1，最右）
    第 17 行  星号在第 29 列附近（x=3.2，y 接近 0，回中）
    第 25 行  星号在第 1 列附近（x=4.8，y 接近 -1，最左）

【要求】
    1. f 只算数、draw_row 只画行，main 的循环体只有一句
       draw_row(f(x));
    2. 换曲线实验：把 f 换成 cos(x)，再换成 x * x / 40 - 1，
       除 f 以外任何地方不许动，重编译看两种新波形
    3. draw_row 里打空格必须用循环，不许手敲一长串空格

【提示】
    - (y + 1) 把 -1 到 1 搬到 0 到 2，再乘 30 放大到 0 到 60 列
    - 若自己另选曲线，先确认 y 落在 -1 到 1 之间，否则星号
      会飞出屏幕（想想怎么把它夹回来）

【吃透自检】
    1. 偏移（加 1）和缩放（乘 30）各自干什么？想让波形宽度
       翻倍占满 120 列，改哪个数？
    2. 为什么拆成 f 和 draw_row？下次改画真实的传感器读数，
       哪个函数一行都不用动？
    3. 步进从 0.2 改成 0.1，图形和总行数各有什么变化？
    4. 附加挑战：记住 080 的伏笔——先想想"把函数当参数传"
       要是能实现，main 会长什么样，把猜想写进注释

===================== 在下面写你的代码 =====================
*/


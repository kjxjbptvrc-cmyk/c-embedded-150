/*
=====================================================================
 【第 127 题】动态链表·创建与遍历：104 题的毕业礼
 难度：★★★★★
 知识点：malloc 版单链表、头插法、二级指针、destroy_all
=====================================================================

【题目描述】
    104 题用数组下标搭静态链表，链表的"形"已经立住；当时
    预告的动态版今天兑现——节点现用现 malloc，要多少有
    多少。定义：

        struct Node {
            int data;
            struct Node *next;
        };

    实现并逐个测试：
    1. struct Node *create_node(int v)：malloc + 初始化 +
       返回指针。083 题"能活着返回的三种内存"里，堆是
       正解的落地：函数返回后节点依然活着，链表因此成立
    2. void push_front(struct Node **head, int v)：头插。
       为什么必须二级指针？要改的是 head 本身——075 题的
       伏笔今天总回收，答不出来就回 075 重修
    3. print_list 遍历打印、count 计数
    4. destroy_all：逐个 free，必须先把 next 存下来再 free
       当前节点——顺序反了就是 125 题第三宗罪：从已释放
       的内存里读 next

【输入输出示例】
    输入：10 20 30 40 -1   （读到 -1 停，逐个头插）
    输出：list: 40 -> 30 -> 20 -> 10 -> NULL
          count = 4

【要求】
    1. push_front 的签名必须带 struct Node **head
    2. 每插一个节点，先在纸上画箭头图：head 指谁、新节点
       的 next 指谁，画完再动手写代码
    3. destroy_all 之后把 head 置 NULL 再遍历一次，应输出
       list: NULL 而不是崩溃

【提示】
    - 头插只有两步：新节点的 next 指向旧 head，head 指向
      新节点。顺序不能反，反了链子就断
    - 空链表就是 head == NULL，它天然是一切操作的起点

【吃透自检】
    1. push_front 为什么传 struct Node ** 而不是一级指针？
    2. 头插法建出的顺序和输入顺序什么关系？为什么？
    3. destroy 顺序错了，犯的是五宗罪里哪一宗？
    4. create_node 里 malloc 失败该返回什么？调用方怎么接？
    5. 附加挑战：写一个 push_back 尾插，体会没有尾指针时
       它为什么比头插费劲

===================== 在下面写你的代码 =====================
*/


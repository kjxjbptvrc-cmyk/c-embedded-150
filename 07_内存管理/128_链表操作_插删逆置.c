/*
=====================================================================
 【第 128 题】链表操作·插删逆置：手术刀法三件套
 难度：★★★★★
 知识点：按值查找、有序插入、按值删除、三指针逆置
=====================================================================

【题目描述】
    在 127 题的链表上做手术。所有操作动刀前先在纸上画箭头
    图，画不出来的代码一定写不对。

    1. 按值查找 find(head, v)：返回第一个 data == v 的节点
       指针，找不到返回 NULL
    2. 有序插入 insert_sorted(struct Node **head, int v)：
       链表始终保持升序。用 prev 和 cur 双指针一前一后推进
       找位置再接入。回想 059 题的有序数组插入：找到位置后
       要把后面的元素整体往后挪一格；链表版找到位置后不用
       挪任何元素，改两根指针就完事——这就是链表的意义
    3. 按值删除 delete_value(struct Node **head, int v)：
       找到目标的前驱、跨接（让前驱的 next 绕过目标）、
       free 目标。注意头节点没有前驱，要单独特判
    4. 逆置 reverse(struct Node **head)：三指针法，prev、
       cur、next 三个指针逐步推进，把每根箭头原地掉头。
       面试头号题，必须画出全程每一步的图

【输入输出示例】
    依次有序插入 30 10 20 40：list: 10 -> 20 -> 30 -> 40 -> NULL
    删除 30 之后：            list: 10 -> 20 -> 40 -> NULL
    逆置之后：                list: 40 -> 20 -> 10 -> NULL

【要求】
    1. 四个操作全部封装成函数，main 里按示例顺序演示
    2. 删除时 free 掉的节点地址不许再碰（125 第三宗罪）
    3. 逆置只许改 next 指针，不许申请新节点、不许搬 data

【提示】
    - 插到头部（比第一个还小）和删除头节点一样要特判，
      先想清楚这两个边界再写主干
    - 逆置循环里 next 必须在改箭头之前保存，想想为什么

【吃透自检】
    1. 删除为什么需要前驱？单向链表少了哪个方向的路？
    2. 有序数组插入和链表插入的成本各是多少？找位置呢？
    3. 逆置三指针每一步各指哪？合上书画出 4 个节点的全程
    4. 附加挑战：用"二级指针遍历法"重写删除——游走的不是
       节点指针，而是"指向 next 域的指针"，头节点特判自然
       消失。Linus 说过这才是有品位的写法

===================== 在下面写你的代码 =====================
*/


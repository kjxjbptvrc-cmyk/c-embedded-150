/*
=====================================================================
 【第 121 题】内存四区地图：查清每个变量住在哪个区
 难度：★★★★☆
 知识点：代码区 / 全局静态区 / 堆 / 栈、%p 打印地址、
         栈和堆的增长方向
=====================================================================

【题目描述】
    程序的内存分四大区：代码区、全局静态区、堆、栈。口诀
    谁都会背，这题要你拿地址当证据，画出本机的内存地图。
    定义并打印下面六类住户的地址：

    1. 一个全局变量、一个 static 全局变量
    2. 一个 static 局部变量——033 题的悬案今天揭晓：
       它到底常驻在哪个区
    3. 两个连续定义的普通局部变量 a、b
    4. 一个字符串字面量——071 题预告的内存地图就是本题
    5. 连续两次 malloc 各得的一块内存（先借用一下，123 题
       才正式讲，本题只打印地址，用完 free 掉）
    6. 任意一个函数的地址（078 题的回声：函数也有地址）

    嵌入式点：单片机的地图不一样——代码和常量在 Flash
    （比如 0x08000000 段），数据、栈、堆在 RAM（比如
    0x20000000 段），一看地址就知道住哪，这是日后读 map
    文件的基本功。

【输出示例】（地址每台机器、每次运行都可能不同，0x... 仅为占位）
    global   : 0x...
    static_g : 0x...
    static_l : 0x...
    local_a  : 0x...
    local_b  : 0x...
    literal  : 0x...
    heap_1   : 0x...
    heap_2   : 0x...
    func     : 0x...

【要求】
    1. 地址一律用 %p 打印，指针先转成 (void *)
    2. 把结果按地址从高到低抄成一张表，用注释画出你本机
       的内存地图，标出四个区各占哪一段
    3. 记录两个方向：a 和 b 谁的地址高？两块 malloc 谁高？
       这就是"栈向下长、堆向上长"的直观证据

【提示】
    - 同一个区的地址会明显扎堆，跨区的差距巨大，一眼能分组
    - 函数地址直接传函数名即可

【吃透自检】
    1. 六类住户各住哪个区？static 局部和普通局部差在哪？
    2. 栈和堆各朝哪个方向生长？拿你的实验数据说话
    3. 为什么字符串字面量改不得？现在能用地图讲清楚了
    4. 附加挑战：加一个 const 全局变量，看它和谁做邻居——
       130 题讲 const 时你会想起今天这一眼

===================== 在下面写你的代码 =====================
*/


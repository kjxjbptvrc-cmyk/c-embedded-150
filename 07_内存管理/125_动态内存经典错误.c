/*
=====================================================================
 【第 125 题】动态内存经典错误：堆的五宗罪
 难度：★★★★★
 知识点：泄漏、double free、use-after-free、堆越界写、
         释放偏移指针
=====================================================================

【题目描述】
    堆错误的共同点：案发时不响，之后某一刻突然爆炸。本题
    当一回法医，五个病例逐个走"制造现场、解释病理、写出
    修正版"三步：

    一 泄漏：malloc 了不 free。123 题看过上涨曲线，归档
    二 double free：同一块内存 free 两次，跑一下看崩法
    三 use-after-free：free 后继续读写。最阴险的一宗——
        常常看起来一切正常，数据悄悄烂掉。因为那块内存
        可能暂时没人动，也可能已被下一次 malloc 分给别人
    四 堆越界写：malloc(10) 却写第 11 个字节。踩坏的是紧
        挨着的堆管理数据，崩溃往往延迟到之后某次 malloc
        或 free 才爆发——远程爆破
    五 释放偏移过的指针：p++ 之后 free(p)。free 只认当初
        发出去的原始地址

    三条守则同时写进注释：谁申请谁释放；free 后立刻置
    NULL；每块内存多大，心里要有账。

    嵌入式点：很多单片机项目干脆全面禁用 malloc——碎片化
    和分配耗时的不确定性，在 7x24 小时运行的设备上不可
    接受。替代方案就是 132 题的静态内存池，先记下这笔账。

【输出示例】（病例二，具体崩法因平台而异）
    free #1 done
    （第二次 free 时崩溃，或被系统报错强制终止）

【要求】
    1. 五个病例各自独立成段，用注释轮流放开，一次只跑一个
    2. 每个病例旁写两行注释：错在哪一行、正确写法是什么
    3. 病例三要设计实验证明"看起来正常"：free 后立刻读旧
       数据，多半还是原值——这正是它难查的原因
    4. 病例四越界写之后再 malloc、free 几次，观察什么时候炸

【提示】
    - 有的病例当场崩，有的多跑几步才崩，全是正常现象
    - 病例五注意 p++ 之后原始地址就丢了，想想怎么留底

【吃透自检】
    1. 五宗罪不看题背出来，各配一句"错在哪、崩在哪"
    2. use-after-free 和堆越界的崩溃现场为什么远离案发现场？
    3. 嵌入式禁 malloc 的两大理由是什么？
    4. free 后立刻置 NULL 能直接防住五宗罪里的哪几宗？
    5. 附加挑战：把病例三改造成"数据真的悄悄烂掉"：free
       后马上 malloc 一块同样大小的写入新值，再读旧指针

===================== 在下面写你的代码 =====================
*/


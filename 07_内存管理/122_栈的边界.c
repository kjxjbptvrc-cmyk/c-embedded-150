/*
=====================================================================
 【第 122 题】栈的边界：亲手把栈撑爆一次
 难度：★★★★☆
 知识点：栈是有限资源、栈溢出、递归深度、粗测栈容量
=====================================================================

【题目描述】
    栈很快很方便，但小得可怜——电脑上通常只有 1~8MB。
    030 题说过递归少写出口会爆栈，还预告了到 122 题亲眼
    看，今天三个实验兑现承诺：

    实验一：在函数里定义 int a[10000000] 并往里写几个数，
        运行——秒崩。算一算这个数组要占多少 MB，就明白
        栈为什么装不下
    实验二：写一个没有出口的递归函数，带一个深度计数器，
        每 10000 层打印一次，看崩溃前能冲到多少层
    实验三：用宏控制局部 char 数组的大小，手工二分逼近
        （044 题二分查找的思想挪来测栈）：1MB 活、16MB 崩，
        那 8MB 呢……粗测出本机的栈容量

    嵌入式军规三条，现在就刻进脑子——单片机的栈往往只有
    1~4KB：
    一、大数组一律 static 或全局，别放栈上
    二、函数内不开大局部数组
    三、递归慎用，很多项目干脆禁用

【输出示例】（实验二，层数因机器而异）
    depth = 10000
    depth = 20000
    ......
    depth = 250000
    （随后无声崩溃，没有任何错误提示）

【要求】
    1. 三个实验分开跑，用注释轮流封存另外两个
    2. 实验一、实验三要真往数组里写数据，防止编译器把
       没人用的数组整个优化掉
    3. 实验三把每次尝试的大小和存活情况记成注释，给出
       结论：本机栈容量约为多少

【提示】
    - 崩溃是本题的预期结果，观察"怎么崩"才是目的

【吃透自检】
    1. 栈溢出为什么常常无声无息直接崩，而不是报一条错误？
    2. 军规三条分别防的是什么场景？
    3. 中断嵌套为什么也吃栈？每进一层中断，现场存到哪了？
    4. 附加挑战：给实验二补上正确出口改成正常递归，确认
       它安全返回——出口对递归来说就是生命线

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 131 题】大小端与对齐总结：两大跨平台坑总账
 难度：★★★★☆
 知识点：字节序检测双法、结构体对齐笔算、跨机传输的坑
=====================================================================

【题目描述】
    数据一旦要离开本机——存文件、走串口、上网络，字节序
    和对齐两笔账就躲不掉。本题把旧账算清，给 143 题的协议
    实战把理论地基打好。

    第一部分，字节序检测双法收进工具箱：
        087 题用 char 指针拆过整数的字节，097 题用 union
        检测过字节序。两法并列实现成两个函数：
        int is_little_endian_by_ptr(void);
        int is_little_endian_by_union(void);
        两个函数的结果必须一致。

    第二部分，结构体 sizeof 笔算五连考：
        按 095 题的对齐规则先笔算，再用 sizeof 验证，
        错一个回 095 重修：
        S1: char a; int b; char c;
        S2: char a; char b; int c;
        S3: int a; char b; char c; short d;
        S4: char a; short b; char c; int d;
        S5: char a; double b; char c;

    第三部分，总账写进注释：跨机传输结构体的两大坑——
    对齐差异（同一个 struct 两台机器 sizeof 不同）加上
    字节序差异（同一个 int 字节顺序相反）。所以协议要么
    逐字节手工打包（115 题的移位法），要么 pack 对齐并
    定死字节序——各有代价，想一想各是什么代价。

【输出示例】
    little endian: 1 1   （两法一致）
    S1: guess=.. sizeof=.. OK
    ......（五个结构体逐个对账，WRONG 的回去重算）

【要求】
    1. 笔算结果先写死进代码或注释，再和 sizeof 对账打印
       OK 或 WRONG，不许先跑出答案再回头改笔算
    2. 两个检测函数必须独立实现，不许互相调用
    3. 五连考每个结构体在注释里画出成员偏移和填充的位置

【提示】
    - 对齐规则回顾：成员对齐到自身大小的整数倍，总大小
      补齐到最宽成员的整数倍
    - 想不起 union 检测法就回 097 看一眼，合上书再写

【吃透自检】
    1. 五连考全对了没有？错的那个错在哪条规则上？
    2. 跨机传 struct 的两大坑分别是什么？各怎么破？
    3. 两种检测法原理差在哪？哪种更能体现"union 所有成员
       共用同一块内存"的本质？
    4. 附加挑战：把 S1 的成员重排到 sizeof 最小。这手"按
       大小排成员"在嵌入式省 RAM 时真的天天用

===================== 在下面写你的代码 =====================
*/


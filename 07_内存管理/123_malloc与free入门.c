/*
=====================================================================
 【第 123 题】malloc 与 free 入门：要多少内存，运行时说了算
 难度：★★★★☆
 知识点：malloc / free 全流程、判 NULL、free 后置 NULL、
         内存泄漏
=====================================================================

【题目描述】
    100 题的通讯录把容量写死成 100，人少浪费、人多装不下，
    当时只能忍。根子是：数组大小编译期就得定死，可"要存
    多少"往往运行时才知道。C99 的变长数组 VLA 能写
    int a[n]，但它开在栈上——122 刚见识过栈就那么点，
    嵌入式不推荐，本题不用它。

    正解是向堆要内存，标准全流程五步走：
    1. 读入整数 n
    2. p = malloc(n * sizeof(int))，立刻判 NULL，失败就
       打印 out of memory 并退出
    3. 读入 n 个整数存进去，倒序打印
    4. free(p)
    5. p = NULL，从第一天就养成习惯

    再做泄漏实验：另写一段死循环，每圈 malloc 1MB 却不
    free，Sleep 半秒再来一圈。开着任务管理器跑它，亲眼看
    内存占用一路上涨——这就是内存泄漏的实感。

【输入输出示例】
    输入：4
          10 20 30 40
    输出：40 30 20 10

【要求】
    1. 判 NULL 必须写，哪怕电脑上几乎不会失败
    2. 泄漏实验每圈打印圈数，方便和内存曲线对照，看够了
       就 Ctrl+C 杀掉
    3. 回答：进程被杀之后，那些没 free 的内存去哪了？

【提示】
    - malloc、free 在 stdlib.h；Sleep(500) 在 windows.h
    - 倒序打印说明这块堆内存和普通数组用法完全一样

【吃透自检】
    1. malloc 返回的 void * 在 C 里要不要强转？（C++ 才必须）
    2. free 后不置 NULL 留下什么隐患？125 题拿它开刀详审
    3. 嵌入式里判 NULL 为什么更重要？堆总共就几 KB，分配
       失败是家常便饭，不是理论风险
    4. 附加挑战：查一查 free(NULL) 安不安全，再想想它和
       "free 后置 NULL"配合起来有什么妙处

===================== 在下面写你的代码 =====================
*/


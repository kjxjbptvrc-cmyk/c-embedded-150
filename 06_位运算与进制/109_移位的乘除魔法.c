/*
=====================================================================
 【第 109 题】移位的乘除魔法：x << 3 真是 x * 8 吗
 难度：★★★☆☆
 知识点：移位与乘除 2 的幂、负数右移与除法的分歧、
         移位量红线
=====================================================================

【题目描述】
    1. 验证等价：打印 13 << 3 与 13 * 8、100 >> 2 与
       100 / 4。正数场合：左移 k 位等于乘 2 的 k 次方，
       右移 k 位等于除以 2 的 k 次方。
    2. 负数的分歧：打印 -7 >> 1 与 -7 / 2，一个 -4 一个 -3！
       右移向下取整（往更小的方向），除法向零取整
       （C99 起明文规定）。把这两句话用自己的话写进注释。
    3. 红线：移位量为负、或大于等于类型位宽，都是未定义
       行为。int 是 32 位时，1 << 32 一个字都不许写。
    4. 态度题，结论写进注释：现代编译器会自动把乘除 2 的幂
       优化成移位，写 x * 8 它自己会变 x << 3。人肉移位
       牺牲可读性，除非性能实测证明有需要。想表达"乘 8"
       就写乘号，想表达"挪位"才写移位。

【输出示例】
    13 << 3  = 104    13 * 8  = 104
    100 >> 2 = 25     100 / 4 = 25
    -7 >> 1  = -4
    -7 / 2   = -3

【要求】
    1. 四组打印全部先笔算预测再运行
    2. 负数那组，注释里写清"向下取整"与"向零取整"
       差在哪，拿 -3.5 举例
    3. 写一行移位量为 32 的代码编译看警告，看完删掉

【提示】
    - -7 是补码形态，右移补进来的是符号位 1
    - 向下取整：-3.5 取 -4；向零取整：-3.5 取 -3

【吃透自检】
    1. 左移 k 位等于乘几？正数右移 k 位等于除以几？
    2. 负数右移为什么和除法对不上？谁向下、谁向零？
    3. 移位量的红线是什么？两条都要说出来
    4. 附加挑战：-1 >> 3 等于几？跟 -1 / 8 一样吗？
       算完就明白负数场合千万别拿移位当除法用

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 116 题】LED 跑马灯模拟：位运算的第一个作品
 难度：★★★★☆
 知识点：u8 状态字管 8 个灯、循环左移 rotate、整体翻转、
         动画帧输出
=====================================================================

【题目描述】
    用一个 unsigned char leds 代表 8 个 LED，1 亮 0 灭，
    位 7 显示在最左。写完你就有了本阶段第一个"作品"。

    1. void print_leds(unsigned char leds)：亮打 #、灭打 -。
       想好看可以换实心圆 / 空心圆字符，Windows 控制台
       乱码就先执行 chcp 65001，或者老实用 # 和 -。
    2. 模式一 全亮全灭：0xFF 与 0x00 各打一帧。
    3. 模式二 单灯跑马：一个亮点从位 0 走到位 7 再走回来，
       打印完整一个来回的每一帧。
    4. 模式三 环绕跑马（rotate 循环左移）：
           leds = (leds << 1 | leds >> 7) & 0xFF;
       最高位不丢弃、绕回位 0。从 0x81 起跑 4 帧体会环绕。
    5. 模式四 交替闪：从 0x55 开始每帧 leds ^= 0xFF，
       0x55 与 0xAA 来回互换，闪若干帧。

【输出示例】（单灯跑马前 4 帧 + 交替闪 2 帧）
    -------#    ( 0x01 )
    ------#-    ( 0x02 )
    -----#--    ( 0x04 )
    ----#---    ( 0x08 )
    -#-#-#-#    ( 0x55 )
    #-#-#-#-    ( 0xAA )

【要求】
    1. 每帧同时打印图案和十六进制值，逐帧核对
    2. rotate 公式先拿 0x81 在纸上推一遍，想清楚 & 0xFF
       挡住的是哪一位的垃圾（108 收窄的老规矩）
    3. 嵌入式点抄进注释：把 leds 换成 GPIO 输出寄存器的
       地址，这段程序原封不动就是真开发板的跑马灯——
       就差一个真地址，139 的寄存器映射宏负责兑现

【提示】
    - 跑马"回头"：走到 0x80 后改成右移往回走
    - 不用真延时，逐行打印就是你的动画

【吃透自检】
    1. rotate 循环左移和普通左移，差在哪一位的命运？
    2. leds ^= 0xFF 和 leds = ~leds & 0xFF 结果一样吗？
       中间过程差在哪？（108 整型提升的回声）
    3. 0x81 连续 rotate 两次分别是多少？笔算
    4. 离真硬件还差哪一步？还差哪个关键字防止编译器
       把"反复写同一地址"优化掉？（130 volatile 预告）
    5. 附加挑战：加"流水填充"模式，从 0x00 起每帧多亮
       一个灯直到全亮：0x01, 0x03, 0x07 ... 帧公式是什么

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 110 题】打印二进制工具：本阶段的显微镜
 难度：★★★★☆
 知识点：逐位提取 (v >> i) & 1、循环方向、每 4 位分组美化、
         工具函数的参数类型选择
=====================================================================

【题目描述】
    printf 没有二进制格式符，自己造一个。036 用递归打印过
    二进制（递归天然正序），今天换移位法，两种都要会。
    这个函数是本阶段每一题的显微镜，写好存好，后面天天用。

    1. void print_bin(unsigned int v, int bits)：
       从最高位打到最低位——i 从 bits - 1 递减到 0，
       每一位用 (v >> i) & 1 取出；每 4 位插一个空格美化。
    2. 用它打印 0~15 的 4 位二进制对照表（bits 传 4）。
       这张表要背到脱口而出，它是 106 心算桥梁的桥墩。
    3. bits 传 32 打印三个特殊值：0xFF、0x80000000、
       (unsigned int)-1。最后一个全是 1——007 的回声：
       -1 转 unsigned 是该类型最大值。

【输出示例】
     0 -> 0000        8 -> 1000
     1 -> 0001        9 -> 1001
     2 -> 0010       10 -> 1010
     3 -> 0011       11 -> 1011
     4 -> 0100       12 -> 1100
     5 -> 0101       13 -> 1101
     6 -> 0110       14 -> 1110
     7 -> 0111       15 -> 1111
    0xFF       : 0000 0000 0000 0000 0000 0000 1111 1111
    0x80000000 : 1000 0000 0000 0000 0000 0000 0000 0000
    -1         : 1111 1111 1111 1111 1111 1111 1111 1111

【要求】
    1. 参数必须是 unsigned int，想清楚为什么再动笔
    2. 每打完 4 位补一个空格，行尾不留多余空格
    3. 对照表排版随意，数值一个不许错

【提示】
    - 判断"一组打完"：i % 4 == 0 且 i 不为 0 时补空格
    - 打印单个位：putchar('0' + ((v >> i) & 1)) 或 %d 都行

【吃透自检】
    1. (v >> i) & 1 为什么能取出第 i 位？两步各干了什么？
    2. 循环为什么从 bits - 1 递减到 0，不从 0 递增？
    3. 参数为什么用 unsigned int？传负数进来会怎样？
       （108 的回声：有符号右移补符号位）
    4. 036 的递归版"天然正序"是怎么来的？
    5. 附加挑战：加个参数支持按 8 位一组用下划线分隔，
       打成 11111111_00000000 风格

===================== 在下面写你的代码 =====================
*/


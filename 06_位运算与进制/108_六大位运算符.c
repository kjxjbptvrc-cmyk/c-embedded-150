/*
=====================================================================
 【第 108 题】六大位运算符：0x5A 与 0x3C 的全套对决
 难度：★★★★☆
 知识点：& | ^ ~ << >>、整型提升陷阱、
         有符号右移与无符号右移的分岔
=====================================================================

【题目描述】
    固定素材：a = 0x5A（0101 1010）、b = 0x3C（0011 1100），
    都定义成 unsigned int。

    1. 先笔算后验证：在你代码区的注释里把 a & b、a | b、
       a ^ b 的二进制竖式一位一位写出来，再预测 ~a、
       a << 1、a >> 2，最后才允许运行程序对答案。
    2. 六种运算各打印一行（用 %X）。
    3. 取反陷阱：~a 打出来是 0xFFFFFFA5，不是 0xA5！
       a 参与运算前先被整型提升成 32 位，高 24 个 0 全部
       翻成了 1。想要 8 位结果，必须 & 0xFF 收窄。
       8 位思维对 32 位现实——操作 8 位寄存器天天遇到。
    4. 右移分岔实验：int s = -8; unsigned int u = 0xFFFFFFF8;
       各右移 1 位。有符号补符号位得 -4，无符号补 0
       得 0x7FFFFFFC，两个世界。

【输出示例】
    a & b  = 0x18
    a | b  = 0x7E
    a ^ b  = 0x66
    ~a     = 0xFFFFFFA5   ( & 0xFF 收窄后 = 0xA5 )
    a << 1 = 0xB4
    a >> 2 = 0x16
    s >> 1 = -4
    u >> 1 = 0x7FFFFFFC

【要求】
    1. 竖式在先、程序在后，顺序不许反
    2. ~a 打印两个版本：原样一个、收窄后一个
    3. 右移分岔实验必做，s 用 %d 打、u 用 %X 打

【提示】
    - & 双方为 1 才 1；| 有 1 就 1；^ 不同才 1
    - 0x5A 逐位取反（8 位内）是 0xA5，剩下的高位去哪了？

【吃透自检】
    1. 六个运算符每个用一句话说清规则
    2. ~ 的整型提升坑怎么收拾？为什么 & 0xFF 能救回来？
    3. 为什么位运算一律建议用 unsigned？（007 的回声：
       有符号右移是实现定义，有符号溢出是未定义行为，
       unsigned 才处处有确定答案）
    4. 附加挑战：不跑程序说出 b << 3 的值——小心有一位
       跑出了 8 位范围，但 32 位 int 里它还活着

===================== 在下面写你的代码 =====================
*/


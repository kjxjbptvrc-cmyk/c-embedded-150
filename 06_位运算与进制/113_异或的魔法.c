/*
=====================================================================
 【第 113 题】异或的魔法：009 埋的伏笔今天兑现
 难度：★★★★☆
 知识点：异或三性质（x ^ x = 0、x ^ 0 = x、交换律）、
         无临时变量交换、找唯一数、XOR 校验和
=====================================================================

【题目描述】
    009 交换变量时预告过"不用临时变量的交换法"，就是它：

        a ^= b;  b ^= a;  a ^= b;

    1. 实现并验证异或交换。在你代码区的注释里用三性质
       逐步推导：把第二步里的 a 展开成 a0 ^ b0 代入，
       用 x ^ x = 0 和 x ^ 0 = x 消项，证明 b 恰好变成
       原来的 a；第三步同理。推导不许跳步。
    2. 翻车警告：写指针版 void xor_swap(int *pa, int *pb)，
       故意传同一个变量的地址——值直接清零！注释里解释
       原因（第一步就是 x ^ x）。然后加 if (pa != pb)
       守卫修好。065 的指针版 swap 若改用异或法，
       这个守卫一步都不能省。
    3. 找唯一数：{3, 7, 3, 9, 7} 里只有一个数出现一次，
       其余成对。全员异或，成对的抵消，剩下即答案。
    4. XOR 校验和：对 {0x12, 0x34, 0x56} 逐字节异或得
       校验字节。143 的协议帧校验就用它，先攒手感。

【输出示例】
    交换前 a = 9, b = 4
    交换后 a = 4, b = 9
    同地址异或交换后 x = 0   <-- 翻车现场
    唯一出现一次的数 = 9
    XOR 校验字节 = 0x70

【要求】
    1. 三性质推导写注释，一步一步来
    2. 翻车实验先做坏版本、再加守卫修好，两次都要打印
    3. 校验字节先笔算二进制竖式再运行核对

【提示】
    - 异或交换是"聊得上来"级别的知识，生产代码老实用
      临时变量，编译器优化得更好
    - 0x12 ^ 0x34 先算出来，再拿结果 ^ 0x56

【吃透自检】
    1. 三步交换的完整推导，不看资料在纸上重写一遍
    2. 同地址翻车的根源是三性质里的哪一条？
    3. XOR 校验查得出单字节错，为什么查不出同一位上
       错两次的"成对错"？
    4. 附加挑战：数组里两个数各出现一次、其余成对，
       全员异或后怎么把两个都揪出来？114 马上教你
       取"结果里最低的那个 1"

===================== 在下面写你的代码 =====================
*/


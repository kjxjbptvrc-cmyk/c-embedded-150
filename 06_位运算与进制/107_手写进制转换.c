/*
=====================================================================
 【第 107 题】手写进制转换：058 的 itoa 与 atoi 全面升级
 难度：★★★★☆
 知识点：除基取余转任意进制、乘基累加解析十六进制、
         A~F 字符映射、strtol 对照验证
=====================================================================

【题目描述】
    058 手写 itoa 和 atoi 时只管十进制，今天推广到任意进制
    （2~16），套路没变，只是"10"换成了"base"。

    1. void to_base(unsigned int n, int base, char buf[])：
       把 n 转成 base 进制字符串。除基取余、逆序收集；
       余数大于 9 时映射成字母：10 -> 'A' ... 15 -> 'F'。
       n 为 0 时要输出 "0"，别给空串。
    2. unsigned int from_hex(const char s[])：
       十六进制字符串转数值，乘基累加：
       result = result * 16 + 当前位的值；
       'A'~'F' 和 'a'~'f' 都要认。
    3. 对照验证：用 printf 的 %x 检查 to_base 的 16 进制
       结果，用 strtol(s, NULL, 16) 检查 from_hex。

【输出示例】
    2026 -> base 2  : 11111101010
    2026 -> base 8  : 3752
    2026 -> base 16 : 7EA
    from_hex("7EA") = 2026
    from_hex("ff")  = 255
    strtol 对照     = 2026   <-- 与自己的实现一致

【要求】
    1. 示例里 2026 的三种写法先笔算再运行对答案
    2. from_hex 大小写通吃
    3. 转换本身不许调库，库只用来对照

【提示】
    - 余数转字符：小于 10 用 '0' + r，否则 'A' + r - 10
    - 逆序收集：先存临时数组，再倒着拷出来

【吃透自检】
    1. 除基取余为什么出来的顺序是反的？先出来的是哪一位？
    2. 字符 'C' 转成数值 12 的表达式怎么写？小写 'c' 呢？
    3. strtol 的 base 参数怎么玩？传 0 时它会自动识别
       0x 和 0 前缀，动手试一次
    4. 附加挑战：写 from_base(s, base)，2~16 进制通用解析

===================== 在下面写你的代码 =====================
*/


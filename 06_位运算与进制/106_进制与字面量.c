/*
=====================================================================
 【第 106 题】进制与字面量：同一个数的四副面孔
 难度：★★★☆☆
 知识点：二 / 八 / 十 / 十六进制、0x 与 0 前缀字面量、
         %d %x %X %o、十六进制与二进制的心算桥梁
=====================================================================

【题目描述】
    从本题起进入位运算与进制阶段。先捅破一层窗户纸：
    内存里只有二进制，十进制、十六进制都只是"显示方式"。

    1. 定义 int n = 90; 分别用 %d、%x、%X、%o 打印，
       看同一个变量的四副面孔。
    2. 字面量前缀实验：int a = 0x5A; int b = 012;
       用 %d 打印两者。0 开头是八进制——012 不是 12，
       这是老 C 留下的经典陷阱。
    3. 心算桥梁：一位十六进制恰好对应四位二进制。
       下面 8 组当堂笔算，答案写在你代码区的注释里，
       算完再用 printf 验证：
           0x3F、0xA5、0x70、0xE8            -> 二进制
           1101 0110、0100 1111、
           0111 0101、1011 1000              -> 十六进制
    4. 想一想并写成注释：嵌入式芯片手册里寄存器值清一色
       用十六进制，为什么？（一位十六对应四位二进制，
       和位段天然对齐；32 位地址写成 8 个十六进制位，
       整整齐齐不占地方）

【输出示例】
    n = 90, hex = 5a, HEX = 5A, oct = 132
    a = 90
    b = 10   <-- 012 是八进制！

【要求】
    1. 四种格式打印的是同一个变量，亲眼确认值没变、只是换装
    2. 8 组心算不许先跑程序，笔算在前、验证在后
    3. 0b1010 这种二进制字面量是编译器扩展（C23 才转正），
       本题先别依赖它

【提示】
    - %x 小写、%X 大写、%o 八进制
    - 心算法：十六进制逐位翻成 4 位二进制再拼接；
      反向就是每 4 位一组查表

【吃透自检】
    1. 一位十六进制对应几位二进制？为什么恰好是这个数？
    2. 寄存器地址 0x40010C0C 怎么快速展开成 32 位二进制？
       口算一遍，一位一位过
    3. %d 和 %x 打出的样子不同，变量在内存里变过吗？
    4. 附加挑战：脱口说出 0xFF、0x80、0x7F 的十进制值，
       以及 255 的八进制写法

===================== 在下面写你的代码 =====================
*/


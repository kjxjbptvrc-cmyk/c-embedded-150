/*
=====================================================================
 【第 148 题】文件操作速成：为"掉电保存"打样
 难度：★★★☆☆
 知识点：fopen/fclose、fgets/fprintf、读写文本配置、错误处理
=====================================================================

【题目描述】
    单片机用 Flash/EEPROM 存配置；在 PC 上先用文件把"持久化"
    的手感练出来。

    程序功能：一个极简配置读写器。

    1. 若当前目录没有 config.txt，则创建它并写入默认内容：
           led=0
           baud=115200
           name=demo
    2. 启动时读取 config.txt，解析成三个变量（可用 144 题的
       拆词思路，或 sscanf）
    3. 提供交互（或命令行参数二选一，交互更简单）：
           show           打印当前配置
           set led 1      修改内存中的值
           save           写回 config.txt
           quit
    4. 再次启动程序，确认上次 save 的值还在

【交互示例】
    loaded: led=0 baud=115200 name=demo
    > set led 1
    ok
    > save
    saved
    > quit

    （第二次运行）
    loaded: led=1 baud=115200 name=demo

【要求】
    1. 每次操作后都要检查 fopen 是否成功，失败打印原因并安全返回
    2. save 时整文件重写即可（先写临时内容思路了解即可，本题直接写）
    3. 解析要能容忍空行；未知键打印警告但不崩溃

【提示】
    - fopen(path, "r") / "w"；读完记得 fclose
    - 嵌入式对应物：把一行配置换成"写 Flash 一页"

【吃透自检】
    1. "w" 和 "a" 打开同一文件，行为差在哪？
    2. 为什么写配置失败时不能只改一半就断电？（引出双缓冲/CRC 思路）
    3. 文本配置 vs 二进制结构体直接 fwrite，各有什么优劣？
    4. 附加挑战：给 config 加一个 checksum 行，load 时校验

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 140 题】阶段综合：铸造你自己的工具头文件
 难度：★★★★★
 知识点：阶段毕业考——多文件工程 + 本阶段全部宏手艺集大成
=====================================================================

【题目描述】
    毕业考：把一路攒下的手艺熔成一份真正的私人库。
    新建三个文件：mytools.h + mytools.c + main.c。

    mytools.h 内容清单（每一项都是旧手艺的升华）：
    - 卫士宏 MYTOOLS_H 三件套                （第 133 题）
    - 类型别名 u8 / u16 / u32                （第 138 题）
    - ARRAY_LEN(a)：第 042 题的求长度表达式正式宏化
          #define ARRAY_LEN(a) (sizeof(a) / sizeof((a)[0]))
    - 位操作四将 SET_BIT / CLR_BIT / TOGGLE_BIT / GET_BIT：
      第 111 题函数版宏化，全括号军规执行到位（第 134 题）
    - 字段宏 GET_FIELD / SET_FIELD           （第 112 题）
    - MIN(a, b) 与 MAX(a, b)：军规再实践     （第 134 题）
    - SWAP(type, a, b)：带类型参数，do-while(0) 包裹
                                             （第 137 题）
    - LOG(msg)：文件、行号、函数名三件套     （第 136 题）
    - print_bytes、print_bin 两个函数的原型

    mytools.c 放函数实现：print_bytes（第 087 题）与
    print_bin（第 110 题）。分工标准就此立下：一行写得完的
    进宏，带循环带逻辑的进函数。

    main.c 把清单逐项测试并打印，一项不许漏。

    写进宣言：这份头文件从今往后跟着你——第 141~150 题综合
    实战全程允许 include 它；将来进嵌入式项目，它就是你的
    第一个私人库。

【运行效果示例】（编译 gcc mytools.c main.c -o toolbox，节选）
    ARRAY_LEN(arr) = 8
    SET_BIT 后 reg = 0x08，TOGGLE_BIT 后 reg = 0x09
    SET_FIELD 后 reg = 0xA9，GET_FIELD 读回 0xA
    MIN(3, 7) = 3   MAX(3, 7) = 7
    SWAP 后 a = 20, b = 10
    [main.c:25 main] all tools ok
    print_bin(0xA5): 10100101

【要求】
    1. 三条军规一处不许违反：卫士宏、全括号、do-while(0)
    2. main.c 里清单每一项至少调用一次并打印结果
    3. SWAP 放进 if else 两分支各用一次，证明 137 手法到位
    4. gcc -Wall 编译，一条警告都不许有

【提示】
    - 位操作宏的参数 n 同样要括号：(1u << (n))
    - 先搭三文件骨架编译通过，再一项一项往里加，加一项测一项

【吃透自检】
    1. 清单里每个宏各对应哪条军规、哪道旧题？口头过一遍
    2. 这份库里宏和函数的分工标准是什么？print_bin 为什么
       不适合做成宏？
    3. 这份库以后怎么长大？新工具进 .h 还是 .c 怎么定？
       什么时候该拆成多个头文件？
    4. 附加挑战：把第 136 题的可变参数版 LOGF 也收进库里

===================== 在下面写你的代码 =====================
*/


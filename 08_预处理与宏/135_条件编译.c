/*
=====================================================================
 【第 135 题】条件编译：一套代码，多种形态
 难度：★★★★☆
 知识点：#ifdef / #else / #endif、#if 表达式、gcc -D 注入宏、
         #if 0 禁用代码
=====================================================================

【题目描述】
    同一份源码，编出调试版和发布版、Windows 版和 STM32 版，
    靠的全是条件编译。四连练：

    1) 调试开关：调试打印全部包进 #ifdef DEBUG ... #endif。
       源文件一字不改，用编译选项切换形态，两编两跑对比：
           gcc main.c -o release          干干净净
           gcc -DDEBUG main.c -o debug    调试信息全出
    2) 功能分级：用 -DVERSION=2 注入带值的宏，
       #if VERSION >= 2 包住"高级功能"的打印
    3) 平台分支：#ifdef PLATFORM_WIN 打印一种"点灯"实现，
       #ifdef PLATFORM_STM32 打印另一种（电脑上用两行不同的
       文字模拟，意思到位），编译时 -D 二选一
    4) 大段禁用：挑几行暂不想跑的代码用 #if 0 ... #endif 包住。
       它比块注释强：可以嵌套，连自身带注释的代码也能整段包
       （块注释不能嵌套，里层的结束符会把外层提前截断）

    这就是嵌入式日常：同一份驱动代码编到不同的板子、发布版
    把串口调试日志一刀切干净——不靠手删，全靠 -D。

【运行效果示例】（同一份源码编译两次）
    release 版输出 2 行：system start 和 hello, user
    debug 版输出 4 行：多出 [DBG] adc raw = 1023 和
                       [DBG] loop end 两行调试信息

【要求】
    1. 四个练习全做，调试开关必须两次编译、两次运行对比
    2. DEBUG 宏不许写在源文件里，只许 -D 从命令行注入
    3. 平台分支在两个宏都没定义时，用 #else 打印 unknown
       platform 兜底

【提示】
    - #ifdef X 只看 X 有没有被定义，不看它的值是什么
    - -DVERSION=2 等价于在源码开头写 #define VERSION 2

【吃透自检】
    1. #ifdef DEBUG 和 #if defined(DEBUG) 什么关系？后者强在
       能写 defined(A) && !defined(B) 这类组合条件
    2. -D 让脚本和 CI 改配置不动源码，这在工程上为什么值钱？
    3. #if 0 比注释大法强的两大优势分别是什么？
    4. 附加挑战：平台分支改成 #elif 链，末尾用 #error 兜底，
       不带 -D 编译一次，看编译器怎么骂你

===================== 在下面写你的代码 =====================
*/


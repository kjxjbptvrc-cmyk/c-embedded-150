/*
=====================================================================
 【第 138 题】typedef 与 stdint 类型：给类型正名
 难度：★★★★☆
 知识点：stdint.h 定宽整数、typedef 自建别名、#define 别名的坑、
         函数指针 typedef（兑现第 086 题预告）
=====================================================================

【题目描述】
    先说惊悚事实：C 标准只保证 int 至少 16 位——16 位单片机
    上 int 就是 2 字节，你在电脑上摸出的溢出边界到板子上全部
    作废。嵌入式铁律：协议和寄存器代码禁用裸 int，一律定宽：

    1) #include <stdint.h>，sizeof 全家福：打印 uint8_t、
       int8_t、uint16_t、uint32_t、int64_t 各占几个字节
    2) 厂商 SDK 惯例——typedef 铸造短别名：
           typedef uint8_t  u8;
           typedef uint16_t u16;
           typedef uint32_t u32;
       各定义一个变量并用 sizeof 验证
    3) 对比坑——#define 也能"造类型"，但它只是文本替换：
           #define uchar unsigned char
           uchar *a, b;
       展开后星号只贴给 a，b 是普通 unsigned char 不是指针！
       打印 sizeof(a)、sizeof(b) 实锤，再用 typedef 版对比
    4) 兑现第 086 题预告——typedef 驯服复杂声明：
           typedef int (*op_t)(int, int);
       第 079 题的声明地狱 int (*ops[4])(int, int) 从此写成
       op_t ops[4]。两种写法都写出来跑通，前后对比

【输出示例】（64 位电脑上）
    sizeof(uint8_t) = 1 ... sizeof(int64_t) = 8
    sizeof(a) = 8, sizeof(b) = 1    （a 是指针，b 不是！）
    ops[0](3, 4) = 7

【要求】
    1. 四步全做，第 3 步两个版本的 sizeof 对比必须打印
    2. op_t 版数组装 4 个函数（可搬第 079 题的加减乘除），
       循环调用一遍
    3. 立个规矩：从本题起，寄存器和协议相关代码一律定宽类型

【提示】
    - typedef 是编译器登记的真类型别名，一次声明多个变量时
      每一个都是完整类型
    - 函数指针 typedef：把原声明里的数组名位置换成 (*别名)

【吃透自检】
    1. 为什么嵌入式禁裸 int？int 存 0 到 70000 的计数在
       16 位机上会发生什么？
    2. #define 版类型别名坑人的机制？typedef 为什么没事？
    3. op_t 美化前后的数组声明各默写一遍，哪个一眼能懂？
    4. 附加挑战：typedef 一个"指向 u8 的指针"叫 pu8，
       声明 pu8 a, b; 验证这回两个都是指针

===================== 在下面写你的代码 =====================
*/


/*
=====================================================================
 【第 133 题】头文件与卫士宏：把第 129 题的工程正式化
 难度：★★★★☆
 知识点：自建 .h 头文件、卫士宏 #ifndef / #define / #endif、
         引号与尖括号 include、头文件纪律
=====================================================================

【题目描述】
    第 129 题的多文件工程靠在 main.c 里手写 extern 声明——
    能跑，但声明散落各处，改一处漏一处。正式做法是头文件。
    在本题目录下新建三个文件：

    mathutil.h  只放"对外说明书"：函数原型、类型定义、宏，
                全部内容包进卫士宏三件套：
                    #ifndef MATHUTIL_H
                    #define MATHUTIL_H
                    ...头文件内容...
                    #endif
    mathutil.c  放函数实现（至少 add、mymax 两个），
                第一行 #include "mathutil.h"
    main.c      只写 #include "mathutil.h"，然后放心调用

    再做两个破坏性实验：
    实验一：在 main.c 里故意把 mathutil.h include 两次——
            有卫士编译照常通过；删掉卫士三行再编译，看报错
    实验二：往 mathutil.h 里放变量定义 int g_count = 0; 让
            两个 .c 都 include，看链接期 multiple definition 报错

【运行效果示例】（编译：gcc mathutil.c main.c -o app）
    add(3, 4) = 7
    mymax(10, 20) = 20

【要求】
    1. 卫士宏名用 MATHUTIL_H 这种"文件名大写"惯例
    2. 两个实验都要真做，报错信息各记一行，做完恢复工程
    3. 记牢纪律表——头文件该放：原型、类型定义、宏、extern
       声明；不该放：函数实现、变量定义

【提示】
    - #ifndef X 意为"若宏 X 尚未定义，才保留到 #endif 为止
      的内容"；第二次 include 时 X 已存在，整段被跳过
    - #include "x.h" 先搜源文件所在目录，<x.h> 直接去系统
      目录找——标准库用尖括号，自己的头用引号

【吃透自检】
    1. 卫士宏三行逐行在干什么？第二次 include 时预处理器看到什么？
    2. 头文件里放 int g_count = 0; 为何编译不报错、到链接才
       炸？改成 extern int g_count; 后，定义该写在哪个文件？
    3. #pragma once 是什么？（非 C 标准但编译器普遍支持）
       与卫士宏相比各有什么优缺点？
    4. 附加挑战：给 mathutil.h 加一个宏和一个 typedef，验证工程照常跑

===================== 在下面写你的代码 =====================
*/


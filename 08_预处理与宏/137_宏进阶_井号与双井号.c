/*
=====================================================================
 【第 137 题】宏进阶：# 字符串化、## 连接与 do-while(0)
 难度：★★★★★
 知识点：# 操作符、## 操作符、多语句宏惨案、do while(0) 惯用法
=====================================================================

【题目描述】
    三样进阶兵器，最后一样是工业级多语句宏的标配。

    1) # 把参数原文变成字符串字面量：
           #define PRINT_INT(x) printf(#x " = %d\n", (x))
       int count = 5; PRINT_INT(count); 打印出 count = 5，
       变量名自动进日志——调试黑魔法
    2) ## 把两段文字拼成一个标识符，小型代码生成器：
           #define DEF_LED_ON(n) \
               void led_on_##n(void) { printf("LED %d on\n", n); }
       DEF_LED_ON(1) DEF_LED_ON(2) 生成 led_on_1、led_on_2，main 里调用验证
    3) 重头戏——多语句宏惨案（main 里先声明 int tmp;）：
           #define SWAP_BAD(a, b) tmp = (a); (a) = (b); (b) = tmp;
       把它放进不带花括号的 if 里：
           if (x > y)
               SWAP_BAD(x, y);
       展开后只有第一条语句归 if 管，后两条无条件执行——用
       x = 3, y = 7 实验：不该交换的数据被动了手脚。唯一正解：
           #define SWAP(a, b) \
               do { int t = (a); (a) = (b); (b) = t; } while (0)
       既把多条语句捆成一条语句，又能吃掉调用行尾的分号。

【输出示例】
    count = 5
    LED 1 on 与 LED 2 on 各一行
    坏版：x = 3, y = 7 经 if "保护"后仍被改动（打印实锤）
    好版：x = 3, y = 7 保持原样；x = 9, y = 4 正确交换

【要求】
    1. 三样全部亲手实验，坏版惨案必须真跑出错误结果
    2. 解释为什么只加花括号 { } 还不够：放进 if 与 else 之间
       时，调用行尾的分号会让 else 认不到 if——构造出编译错误
    3. 好版 SWAP 放进 if else 两个分支里各调一次，证明安全

【提示】
    - 看不清展开结果就请出第 134 题的真相之眼 gcc -E
    - do while(0) 恰好执行一次，编译器直接优化掉，零开销

【吃透自检】
    1. # 和 ## 各自干什么？各举一个题目之外的新例子
    2. do-while(0) 一口气解决了哪两个问题？
    3. 花括号版具体死在哪个场景？把那条报错复述一遍
    4. 附加挑战：#define N 5 后，#N 拿到的是 "N" 不是 "5"——
       查"字符串化两层宏中转"，写出能打印 5 的 STR 宏

===================== 在下面写你的代码 =====================
*/

